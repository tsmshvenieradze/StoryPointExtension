---
phase: 01-calc-engine-audit-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/calc/levels.ts
  - src/calc/fibonacci.ts
  - src/calc/engine.ts
  - src/calc/index.ts
  - src/calc/.gitkeep
  - tests/calc/calcEngine.test.ts
  - tests/calc/fixtures/all-cases.ts
  - tests/smoke.test.ts
autonomous: true
requirements: [CALC-01, CALC-02, CALC-03, CALC-04, CALC-05]
tags: [calc, pure-typescript, vitest, fibonacci, ado-extension]

must_haves:
  truths:
    - "Importing `calculate({c, u, e})` from `src/calc/index.ts` returns `{w, rawSp, sp, input}` for any (Level, Level, Level) triple"
    - "`roundFib(rawSp)` returns the same FibonacciSp value for every threshold edge as the xlsx F22 IF-chain (≤0.75→0.5, ≤1.5→1, ≤2.5→2, ≤4→3, ≤6.5→5, ≤10.5→8, else→13)"
    - "`weightedSum(c, u, e)` matches xlsx G18 (`0.4·c + 0.4·u + 0.2·e`) for every Score triple"
    - "`rawSp(w)` matches xlsx G19 (`0.5 × 26^((w−1)/4)`) for every numeric W"
    - "`LEVELS`, `LEVEL_TO_SCORE`, `SCORE_TO_LEVEL`, `levelToScore`, `scoreToLevel`, `roundFib`, `FIB_THRESHOLDS`, `calculate`, `weightedSum`, `rawSp` are all importable from `src/calc/index.ts`"
    - "Type exports `Level`, `Score`, `FibonacciSp`, `CalcInput`, `CalcResult` are importable from `src/calc/index.ts`"
    - "`npm run typecheck` exits 0 and `npm test` reports the calc test suite passing for all 125 cases plus every threshold boundary"
    - "No file under `src/calc/**` imports from `react`, `react-dom`, `azure-devops-extension-sdk`, or `azure-devops-extension-api`"
  artifacts:
    - path: "src/calc/levels.ts"
      provides: "LEVELS, Level, Score, LEVEL_TO_SCORE, SCORE_TO_LEVEL, levelToScore, scoreToLevel"
      contains: "export const LEVELS"
    - path: "src/calc/fibonacci.ts"
      provides: "FibonacciSp, FIB_THRESHOLDS, roundFib"
      contains: "export function roundFib"
    - path: "src/calc/engine.ts"
      provides: "CalcInput, CalcResult, weightedSum, rawSp, calculate"
      contains: "export function calculate"
    - path: "src/calc/index.ts"
      provides: "Public API barrel"
      contains: "export"
    - path: "tests/calc/calcEngine.test.ts"
      provides: "Vitest table-driven tests covering CALC-01..05"
      contains: "describe"
    - path: "tests/calc/fixtures/all-cases.ts"
      provides: "Hand-curated 125-case fixture exporting ALL_CASES with expected sp per (c,u,e)"
      contains: "ALL_CASES"
  key_links:
    - from: "src/calc/engine.ts"
      to: "src/calc/levels.ts and src/calc/fibonacci.ts"
      via: "import { levelToScore } and import { roundFib }"
      pattern: "from './levels'|from './fibonacci'"
    - from: "src/calc/index.ts"
      to: "all three implementation files"
      via: "barrel re-exports"
      pattern: "export \\{.*\\} from './(levels|fibonacci|engine)'"
    - from: "tests/calc/calcEngine.test.ts"
      to: "src/calc/* and tests/calc/fixtures/all-cases.ts"
      via: "import statements"
      pattern: "from '\\.\\./\\.\\./src/calc/"
  decision_coverage:
    - "D-01: honored — c/u/e fields will use Level labels (consumed by audit module in Plan 01-02); calc engine produces Level-typed result.input"
    - "D-02: honored — calc engine does not serialize JSON; sentinel JSON shape enforced in Plan 01-02"
    - "D-03: implemented by Task 1 — LEVELS array uses exact Title Case strings 'Very Easy', 'Easy', 'Medium', 'Hard', 'Very Hard' mapping to scores 1..5"
    - "D-04: not directly relevant to calc engine; honored by Plan 01-02 parser"
    - "D-05: implemented by Task 1 — LEVELS, LEVEL_TO_SCORE, SCORE_TO_LEVEL all exported from levels.ts and re-exported from index.ts"
    - "D-06: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-07: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-08: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-09: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-10: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-11: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-12: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-13: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-14: not relevant to calc engine; honored by Plan 01-02 parser"
    - "D-15: implemented by Task 4 — src/calc/index.ts re-exports calculate, levelToScore, weightedSum, rawSp, roundFib + types"
    - "D-16: implemented by Task 3 — CalcInput = {c, u, e}; CalcResult = {w, rawSp, sp, input}"
    - "D-17: implemented by Tasks 1, 2, 3 — Level string-literal union, Score = 1|2|3|4|5, FibonacciSp = 0.5|1|2|3|5|8|13"
    - "D-18: not relevant to calc engine; honored by Plan 01-02 audit barrel"
    - "D-19: not relevant to calc engine; honored by Plan 01-02 types"
    - "D-20: implemented by Task 5 — vitest `it.each` object-form tables, no fast-check"
    - "D-21: not directly in this plan — round-trip property test lives in Plan 01-02 (consumes calculate from this plan)"
    - "D-22: implemented by Task 5 — explicit threshold boundary tests for 0.75, 1.5, 2.5, 4.0, 6.5, 10.5 (≤ and > sides)"
    - "D-23: not relevant to calc engine; honored by Plan 01-02 parser tests"
    - "D-24: implemented by Task 5 — test file at tests/calc/calcEngine.test.ts mirrors src/ layout"
    - "D-25: enforced by Task 6 acceptance check — zero React/browser imports under src/calc/**"
    - "D-26: enforced by Task 6 acceptance check — zero azure-devops-extension-sdk/api imports under src/calc/**"
    - "D-27: implemented by Task 5 — tests/smoke.test.ts is deleted in the SAME commit that creates tests/calc/calcEngine.test.ts (the first real test in the project)"
---

<objective>
Build the pure-TypeScript calculation engine that ports `sp_calculator.xlsx` cell F22 (Fibonacci threshold IF-chain), G18 (weighted sum), G19 (raw SP) into four files under `src/calc/` with full table-driven Vitest coverage and zero ADO/SDK/React imports.

Purpose: Lock the calculation wire format that Phase 3's modal will display and Plan 01-02's audit module will round-trip-test. Every Fibonacci bucket and every threshold edge must match the xlsx exactly so manual QA can read the test table as a spec against the workbook.

Output: `src/calc/{levels,fibonacci,engine,index}.ts`, `tests/calc/calcEngine.test.ts`, `tests/calc/fixtures/all-cases.ts`. After this plan, `npm run typecheck && npm test` exits 0 with the calc test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md
@.planning/phases/01-calc-engine-audit-parser/01-RESEARCH.md
@.planning/phases/00-bootstrap-prerequisites/00-01-SUMMARY.md
@./CLAUDE.md
@sp_calculator.xlsx
@tsconfig.json
@vitest.config.ts
@package.json

<interfaces>
<!-- Phase 0 environment surface (already on disk; do not change). -->

From tsconfig.json:
- `strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`, `noImplicitOverride: true`, `skipLibCheck: true`
- TS module resolution and JSX settings inherited from Phase 0; calc module produces no `.tsx`

From vitest.config.ts (Phase 0 baseline; this plan does NOT modify it — Plan 01-02 owns the coverage threshold edit):
```typescript
test: {
  environment: 'node',
  globals: true,
  include: ['tests/**/*.test.ts'],
  coverage: {
    provider: 'v8',
    reporter: ['text', 'html', 'lcov'],
    include: ['src/**/*.ts'],
    exclude: ['src/entries/**', 'src/**/*.tsx'],
  },
},
```

From sp_calculator.xlsx (verified extraction in RESEARCH.md):
- `D5..D9 = 1,2,3,4,5` (level scores)
- `G18 = 0.4*G14 + 0.4*G15 + 0.2*G16` (weighted sum W)
- `G19 = 0.5 * 26^((G18-1)/4)` (Raw SP)
- `F22 = IF(G19<=0.75, 0.5, IF(G19<=1.5, 1, IF(G19<=2.5, 2, IF(G19<=4, 3, IF(G19<=6.5, 5, IF(G19<=10.5, 8, 13))))))` (final SP)

125-case bucket distribution verified live (Node 24.15) per RESEARCH §Summary:
`{0.5: 5, 1: 23, 2: 28, 3: 34, 5: 21, 8: 12, 13: 2}` (sum = 125)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/calc/levels.ts (CALC-01, D-03, D-05, D-17)</name>
  <files>src/calc/levels.ts, src/calc/.gitkeep</files>
  <read_first>
    - .planning/phases/01-calc-engine-audit-parser/01-RESEARCH.md (sections "Code Examples Example 1" and "Pattern 1")
    - .planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md (D-03, D-05, D-17)
    - tsconfig.json (verify `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` are still on)
    - src/calc/.gitkeep (delete after this task creates real files)
  </read_first>
  <behavior>
    - LEVELS is a 5-element readonly tuple in dropdown display order: 'Very Easy', 'Easy', 'Medium', 'Hard', 'Very Hard'
    - Type Level = (typeof LEVELS)[number] resolves to the union of those five string literals
    - Type Score = 1 | 2 | 3 | 4 | 5
    - LEVEL_TO_SCORE['Very Easy'] === 1, LEVEL_TO_SCORE['Very Hard'] === 5
    - SCORE_TO_LEVEL[1] === 'Very Easy', SCORE_TO_LEVEL[5] === 'Very Hard'
    - levelToScore('Hard') === 4
    - scoreToLevel(3) === 'Medium'
    - LEVEL_TO_SCORE and SCORE_TO_LEVEL are frozen at runtime (Object.freeze) AND `as const`/Readonly at compile time
  </behavior>
  <action>
    Delete the placeholder `src/calc/.gitkeep` (it has served its purpose) and create `src/calc/levels.ts` with this EXACT content (copied verbatim from RESEARCH.md Example 1, including the source-comment header):

    ```typescript
    // src/calc/levels.ts — Source: D-03, D-05, D-17; verified xlsx D5..D9 score values
    export const LEVELS = [
      'Very Easy',
      'Easy',
      'Medium',
      'Hard',
      'Very Hard',
    ] as const;

    export type Level = (typeof LEVELS)[number];
    export type Score = 1 | 2 | 3 | 4 | 5;

    export const LEVEL_TO_SCORE: Readonly<Record<Level, Score>> = Object.freeze({
      'Very Easy': 1,
      'Easy':      2,
      'Medium':    3,
      'Hard':      4,
      'Very Hard': 5,
    });

    export const SCORE_TO_LEVEL: Readonly<Record<Score, Level>> = Object.freeze({
      1: 'Very Easy',
      2: 'Easy',
      3: 'Medium',
      4: 'Hard',
      5: 'Very Hard',
    });

    export function levelToScore(label: Level): Score {
      return LEVEL_TO_SCORE[label];
    }

    export function scoreToLevel(score: Score): Level {
      return SCORE_TO_LEVEL[score];
    }
    ```

    If `tsc --noEmit` complains that `LEVEL_TO_SCORE[label]` returns `Score | undefined` under `noUncheckedIndexedAccess` (RESEARCH §Pattern 1 says it should NOT for `Record<UnionType, V>`, but if the installed TS version differs), append `!` (non-null assertion) — `return LEVEL_TO_SCORE[label]!;` and `return SCORE_TO_LEVEL[score]!;`. Do NOT change the public type signature; the `!` is correct because `Level` and `Score` are closed unions.

    Bundle the deletion of `src/calc/.gitkeep` into this commit.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const {LEVELS, LEVEL_TO_SCORE, SCORE_TO_LEVEL, levelToScore, scoreToLevel} = require('./src/calc/levels.ts'); console.assert(LEVELS.length === 5); console.assert(levelToScore('Hard') === 4); console.assert(scoreToLevel(3) === 'Medium'); console.assert(Object.isFrozen(LEVEL_TO_SCORE)); console.log('OK');" 2>$null; echo done</automated>
    <!-- Note: the node -e check is illustrative; the canonical gate is `npx tsc --noEmit` which compiles the file. The Vitest run in Task 5 exercises every export. -->
  </verify>
  <acceptance_criteria>
    - `src/calc/levels.ts` exists and contains the literal string `'Very Easy', 'Easy', 'Medium', 'Hard', 'Very Hard'` (in that order) — verify with: `grep -E "Very Easy.*Easy.*Medium.*Hard.*Very Hard" src/calc/levels.ts | grep -v '^#'` returns at least 1 hit
    - `src/calc/.gitkeep` no longer exists (`test ! -e src/calc/.gitkeep`)
    - `npx tsc --noEmit` exits 0
    - File contains `export const LEVELS`, `export type Level`, `export type Score`, `export const LEVEL_TO_SCORE`, `export const SCORE_TO_LEVEL`, `export function levelToScore`, `export function scoreToLevel` (verify each with: `grep -c "^export " src/calc/levels.ts` returns 7)
    - File contains `Object.freeze` (verify: `grep -c "Object.freeze" src/calc/levels.ts` >= 2)
    - File contains NO import from `react`, `react-dom`, `azure-devops-extension-sdk`, or `azure-devops-extension-api` (verify: `grep -E "from ['\"](react|react-dom|azure-devops)" src/calc/levels.ts | grep -v '^#'` returns zero hits)
  </acceptance_criteria>
  <done>
    Levels module compiled and importable; calc engine and audit module can both import `Level`, `Score`, `LEVELS`, `levelToScore`, `scoreToLevel` from `./levels`. Phase 3 modal can later import `LEVELS` for dropdown options.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/calc/fibonacci.ts (CALC-04, D-17, D-22)</name>
  <files>src/calc/fibonacci.ts</files>
  <read_first>
    - .planning/phases/01-calc-engine-audit-parser/01-RESEARCH.md (Pattern 2 and Code Examples Example 2)
    - .planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md (D-17, D-22)
    - sp_calculator.xlsx (xl/worksheets/sheet1.xml F22 already extracted in RESEARCH §Sources)
  </read_first>
  <behavior>
    - FibonacciSp is the literal union 0.5 | 1 | 2 | 3 | 5 | 8 | 13
    - FIB_THRESHOLDS is a readonly array of 6 [number, FibonacciSp] tuples in ascending threshold order: [0.75, 0.5], [1.5, 1], [2.5, 2], [4.0, 3], [6.5, 5], [10.5, 8]
    - roundFib(0) === 0.5; roundFib(0.75) === 0.5; roundFib(0.7500001) === 1
    - roundFib(1.5) === 1; roundFib(2.5) === 2; roundFib(4.0) === 3; roundFib(6.5) === 5; roundFib(10.5) === 8
    - roundFib(11.0) === 13; roundFib(1000) === 13
    - All `<=` boundaries inclusive (matches xlsx F22 IF-chain `<=` semantics verbatim)
  </behavior>
  <action>
    Create `src/calc/fibonacci.ts` with this EXACT content (verbatim from RESEARCH.md Code Examples Example 2; the F22 IF-chain is reproduced as a header comment so a reviewer can diff it against the xlsx):

    ```typescript
    // src/calc/fibonacci.ts — Source: xlsx F22 IF-chain (verified extracted from xl/worksheets/sheet1.xml line F22)
    // IF(G19<=0.75, 0.5, IF(G19<=1.5, 1, IF(G19<=2.5, 2, IF(G19<=4, 3, IF(G19<=6.5, 5, IF(G19<=10.5, 8, 13))))))

    export type FibonacciSp = 0.5 | 1 | 2 | 3 | 5 | 8 | 13;

    export const FIB_THRESHOLDS: ReadonlyArray<readonly [number, FibonacciSp]> = [
      [0.75, 0.5],
      [1.5,  1],
      [2.5,  2],
      [4.0,  3],
      [6.5,  5],
      [10.5, 8],
    ] as const;

    export function roundFib(rawSp: number): FibonacciSp {
      for (const [threshold, value] of FIB_THRESHOLDS) {
        if (rawSp <= threshold) return value;
      }
      return 13;
    }
    ```

    Do not introduce a switch statement, do not add intermediate rounding (no `Number.toFixed` before comparison — RESEARCH §"Anti-Patterns to Avoid" forbids it; the input space has no near-threshold cases so floating-point precision is a non-issue).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/calc/fibonacci.ts` exists and contains the literal threshold tuple sequence `[0.75, 0.5]`, `[1.5,  1]`, `[2.5,  2]`, `[4.0,  3]`, `[6.5,  5]`, `[10.5, 8]` (all six, in ascending order). Verify with: `grep -c "0.75, 0.5\|1.5,  1\|2.5,  2\|4.0,  3\|6.5,  5\|10.5, 8" src/calc/fibonacci.ts` returns 6
    - File exports `FibonacciSp`, `FIB_THRESHOLDS`, `roundFib`. Verify: `grep -c "^export " src/calc/fibonacci.ts` returns 3
    - File contains the comment line referencing the xlsx F22 IF-chain (verify: `grep "G19<=0.75" src/calc/fibonacci.ts` returns 1 hit — proves provenance is preserved for manual QA diff)
    - `npx tsc --noEmit` exits 0
    - No imports from `react`, `react-dom`, `azure-devops-extension-sdk`, or `azure-devops-extension-api` (verify: `grep -E "from ['\"](react|react-dom|azure-devops)" src/calc/fibonacci.ts | grep -v '^#'` returns zero hits)
    - No `Number.toFixed` or `Math.round` calls before threshold comparison (verify: `grep -E "(toFixed|Math\.round|Math\.floor|Math\.ceil)" src/calc/fibonacci.ts | grep -v '^#'` returns zero hits)
  </acceptance_criteria>
  <done>
    Fibonacci rounding module compiled. `roundFib` is the single source of truth for the threshold table; matches xlsx F22 byte-for-byte semantically.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/calc/engine.ts (CALC-02, CALC-03, D-15, D-16)</name>
  <files>src/calc/engine.ts</files>
  <read_first>
    - .planning/phases/01-calc-engine-audit-parser/01-RESEARCH.md (Pattern 3 and Code Examples Example 3)
    - .planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md (D-15, D-16)
    - src/calc/levels.ts (created in Task 1; engine.ts imports from it)
    - src/calc/fibonacci.ts (created in Task 2; engine.ts imports from it)
  </read_first>
  <behavior>
    - weightedSum(c, u, e) === 0.4*c + 0.4*u + 0.2*e (full IEEE-754 precision; do NOT round)
    - rawSp(w) === 0.5 * Math.pow(26, (w-1)/4) (full precision; matches xlsx G19)
    - calculate({c: 'Hard', u: 'Medium', e: 'Easy'}) returns CalcResult with: w = 3.2, rawSp ≈ 3.226..., sp = 3, input = {c:'Hard', u:'Medium', e:'Easy'}
    - calculate({c:'Very Easy', u:'Very Easy', e:'Very Easy'}).sp === 0.5
    - calculate({c:'Very Hard', u:'Very Hard', e:'Very Hard'}).sp === 13
    - calculate exposes intermediate values w and rawSp for the modal to display (D-16); does NOT pre-round them
  </behavior>
  <action>
    Create `src/calc/engine.ts` with this EXACT content (verbatim from RESEARCH.md Code Examples Example 3):

    ```typescript
    // src/calc/engine.ts — Source: xlsx G18 (W formula) + G19 (Raw SP formula); D-15, D-16
    import { levelToScore, type Level, type Score } from './levels';
    import { roundFib, type FibonacciSp } from './fibonacci';

    export type CalcInput = { c: Level; u: Level; e: Level };
    export type CalcResult = {
      w: number;
      rawSp: number;
      sp: FibonacciSp;
      input: CalcInput;
    };

    /** xlsx G18: 0.4*C + 0.4*U + 0.2*E */
    export function weightedSum(c: Score, u: Score, e: Score): number {
      return 0.4 * c + 0.4 * u + 0.2 * e;
    }

    /** xlsx G19: 0.5 * 26^((W-1)/4) */
    export function rawSp(w: number): number {
      return 0.5 * Math.pow(26, (w - 1) / 4);
    }

    export function calculate(input: CalcInput): CalcResult {
      const c = levelToScore(input.c);
      const u = levelToScore(input.u);
      const e = levelToScore(input.e);
      const w = weightedSum(c, u, e);
      const r = rawSp(w);
      return {
        w,
        rawSp: r,
        sp: roundFib(r),
        input,
      };
    }
    ```

    Do NOT add `Number.toFixed(2)` anywhere — the modal handles display rounding (Phase 3 concern; RESEARCH §"Anti-Patterns to Avoid" explicitly forbids it here).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/calc/engine.ts` exists and contains exactly these 5 exports: `CalcInput`, `CalcResult`, `weightedSum`, `rawSp`, `calculate`. Verify: `grep -c "^export " src/calc/engine.ts` returns 5
    - File imports from `./levels` and `./fibonacci`. Verify: `grep -c "from './\\(levels\\|fibonacci\\)'" src/calc/engine.ts` returns 2
    - The expression `0.4 * c + 0.4 * u + 0.2 * e` appears literally in `weightedSum`. Verify: `grep -F "0.4 * c + 0.4 * u + 0.2 * e" src/calc/engine.ts` returns 1 hit
    - The expression `0.5 * Math.pow(26, (w - 1) / 4)` appears literally in `rawSp`. Verify: `grep -F "0.5 * Math.pow(26, (w - 1) / 4)" src/calc/engine.ts` returns 1 hit
    - No `toFixed` or `Math.round` calls (verify: `grep -E "(toFixed|Math\.round|Math\.floor|Math\.ceil)" src/calc/engine.ts | grep -v '^#'` returns zero hits)
    - No imports from `react`, `react-dom`, `azure-devops-*` (verify: `grep -E "from ['\"](react|react-dom|azure-devops)" src/calc/engine.ts | grep -v '^#'` returns zero hits)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Engine module compiles. `calculate({c, u, e})` is the single entry point Phase 3 modal will call; intermediate `w` and `rawSp` are exposed so the modal can display them without recomputing.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create src/calc/index.ts barrel (D-15, D-17)</name>
  <files>src/calc/index.ts</files>
  <read_first>
    - .planning/phases/01-calc-engine-audit-parser/01-RESEARCH.md (Code Examples Example 4)
    - .planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md (D-15, D-17)
    - src/calc/levels.ts, src/calc/fibonacci.ts, src/calc/engine.ts (the files this barrel re-exports)
  </read_first>
  <action>
    Create `src/calc/index.ts` with this EXACT content (verbatim from RESEARCH.md Code Examples Example 4):

    ```typescript
    // src/calc/index.ts — Source: D-15, D-17 public API
    export { LEVELS, LEVEL_TO_SCORE, SCORE_TO_LEVEL, levelToScore, scoreToLevel } from './levels';
    export type { Level, Score } from './levels';
    export { roundFib, FIB_THRESHOLDS } from './fibonacci';
    export type { FibonacciSp } from './fibonacci';
    export { calculate, weightedSum, rawSp } from './engine';
    export type { CalcInput, CalcResult } from './engine';
    ```

    The barrel is a no-logic re-export: every name it lists is defined in one of the three sibling files. If `npx tsc --noEmit` errors on a missing name, it means a previous task did not export that symbol — fix the corresponding file, do not delete the export from the barrel.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/calc/index.ts` exists. Verify: `test -f src/calc/index.ts`
    - Re-exports the full D-15/D-17 surface. Verify each symbol appears in the file (one shell: `for s in LEVELS LEVEL_TO_SCORE SCORE_TO_LEVEL levelToScore scoreToLevel Level Score roundFib FIB_THRESHOLDS FibonacciSp calculate weightedSum rawSp CalcInput CalcResult; do grep -q "$s" src/calc/index.ts || { echo "MISSING $s"; exit 1; }; done; echo OK`)
    - File has 6 export statements (3 value-export + 3 type-export). Verify: `grep -c "^export " src/calc/index.ts` returns 6
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Public API barrel ready. Plan 01-02 audit module imports `Level`/`LEVELS`/`FibonacciSp` from `../calc/levels` and `../calc/fibonacci` directly (per RESEARCH Examples 5–7); the modal in Phase 3 imports from `src/calc/index.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create tests/calc/calcEngine.test.ts + 125-case fixture + DELETE tests/smoke.test.ts (CALC-05, D-20, D-22, D-24, D-27)</name>
  <files>tests/calc/calcEngine.test.ts, tests/calc/fixtures/all-cases.ts, tests/smoke.test.ts (deleted)</files>
  <read_first>
    - .planning/phases/01-calc-engine-audit-parser/01-RESEARCH.md (Pattern 8 + Code Examples Example 10 + Open Question 1 hybrid recommendation)
    - .planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md (D-20, D-22, D-24)
    - src/calc/index.ts (the public API under test)
    - sp_calculator.xlsx (canonical reference for fixture values)
  </read_first>
  <behavior>
    - Vitest discovers tests/calc/calcEngine.test.ts and runs it
    - Test suite uses `it.each` object-form with `$keyName` placeholders for readable test names
    - Suite asserts levelToScore for all 5 labels (CALC-01 coverage)
    - Suite asserts weightedSum for at least 5 representative Score triples including (4,4,4)→4.0 (xlsx default) (CALC-02)
    - Suite asserts rawSp for at least 4 W values including W=4 → 5.7570501854989171 (verified live in RESEARCH §Example 10) (CALC-03)
    - Suite asserts roundFib for every threshold edge — 0.75, 1.5, 2.5, 4.0, 6.5, 10.5 — both ≤ side (just at threshold) and > side (threshold + 1e-7) (CALC-04, D-22)
    - Suite asserts calculate({c, u, e}).sp matches the fixture for ALL 125 (Level × Level × Level) combinations (CALC-04, CALC-05)
    - Suite asserts that the bucket distribution of the 125-case fixture is exactly `{0.5: 5, 1: 23, 2: 28, 3: 34, 5: 21, 8: 12, 13: 2}` (CALC-05 bucket coverage)
    - All tests pass when run with `npx vitest run tests/calc/`
  </behavior>
  <action>
    First, create the fixture file `tests/calc/fixtures/all-cases.ts`. The 125 expected `sp` values are derived from the xlsx formula and verified against RESEARCH's bucket distribution. The fixture is hand-curated as recommended by RESEARCH §Open Questions #1 (hybrid approach: hand-curate calc fixtures, generate audit round-trip cases).

    Use this generator approach during planning, then commit the resulting array verbatim:

    ```typescript
    // tests/calc/fixtures/all-cases.ts — Source: derived from sp_calculator.xlsx F22/G18/G19 formulas
    // Bucket distribution (verified Node 24.15 in RESEARCH §Summary): {0.5:5, 1:23, 2:28, 3:34, 5:21, 8:12, 13:2}.
    // Generation: const c,u,e ∈ {1..5}; w = 0.4c + 0.4u + 0.2e; raw = 0.5 * 26^((w-1)/4); sp = F22(raw).
    // The hand-curated table below is committed so manual QA can diff against the xlsx row by row.

    import type { Level } from '../../../src/calc/levels';
    import type { FibonacciSp } from '../../../src/calc/fibonacci';

    export type CalcCase = { c: Level; u: Level; e: Level; sp: FibonacciSp };

    export const ALL_CASES: ReadonlyArray<CalcCase> = [
      // ... 125 entries ordered by (c, u, e) iteration over LEVELS in the order: Very Easy, Easy, Medium, Hard, Very Hard
      // Each row is the result of: { c, u, e, sp: roundFib(0.5 * 26^(((0.4*ci + 0.4*ui + 0.2*ei) - 1) / 4)) }
      // where ci/ui/ei are the score 1..5 for the level label.
      // Generate by Node:
      //   const LEVELS=['Very Easy','Easy','Medium','Hard','Very Hard'];
      //   const SCORE={['Very Easy']:1,['Easy']:2,['Medium']:3,['Hard']:4,['Very Hard']:5};
      //   const TH=[[0.75,0.5],[1.5,1],[2.5,2],[4,3],[6.5,5],[10.5,8]];
      //   const fib=r=>{for(const[t,v]of TH)if(r<=t)return v;return 13;};
      //   for(const c of LEVELS)for(const u of LEVELS)for(const e of LEVELS){
      //     const w=0.4*SCORE[c]+0.4*SCORE[u]+0.2*SCORE[e];
      //     const r=0.5*Math.pow(26,(w-1)/4);
      //     console.log(`  { c: '${c}', u: '${u}', e: '${e}', sp: ${fib(r)} },`);
      //   }
      // Run that snippet once and paste the 125 lines below.
    ];
    ```

    EXECUTOR INSTRUCTION: Run the generator snippet (e.g., via `node -e "..."`) at execution time, capture stdout, paste those 125 lines into `ALL_CASES`. Then verify by counting buckets:
    `node -e "const {ALL_CASES}=require('./tests/calc/fixtures/all-cases.ts');const b={};for(const r of ALL_CASES)b[r.sp]=(b[r.sp]||0)+1;console.log(JSON.stringify(b));"` must print `{"0.5":5,"1":23,"2":28,"3":34,"5":21,"8":12,"13":2}` (key order may vary; counts must match).

    Then create `tests/calc/calcEngine.test.ts` with this content (extending RESEARCH Code Examples Example 10 to consume the fixture and add explicit bucket-count assertion):

    ```typescript
    // tests/calc/calcEngine.test.ts — Source: D-20, D-22, D-24; replaces tests/smoke.test.ts (D-27 — deleted in this same commit)
    import { describe, it, expect } from 'vitest';
    import { LEVELS, levelToScore } from '../../src/calc/levels';
    import { roundFib } from '../../src/calc/fibonacci';
    import { weightedSum, rawSp, calculate } from '../../src/calc/engine';
    import { ALL_CASES } from './fixtures/all-cases';

    describe('levelToScore: every label maps correctly (CALC-01)', () => {
      it.each([
        { label: 'Very Easy' as const, expected: 1 },
        { label: 'Easy'      as const, expected: 2 },
        { label: 'Medium'    as const, expected: 3 },
        { label: 'Hard'      as const, expected: 4 },
        { label: 'Very Hard' as const, expected: 5 },
      ])('levelToScore($label) = $expected', ({ label, expected }) => {
        expect(levelToScore(label)).toBe(expected);
      });
    });

    describe('weightedSum: matches xlsx G18 formula (CALC-02)', () => {
      it.each([
        { c: 1, u: 1, e: 1, expected: 1.0 },
        { c: 5, u: 5, e: 5, expected: 5.0 },
        { c: 3, u: 3, e: 3, expected: 3.0 },
        { c: 4, u: 3, e: 2, expected: 3.2 },
        { c: 4, u: 4, e: 4, expected: 4.0 },
      ])('weightedSum($c, $u, $e) = $expected', ({ c, u, e, expected }) => {
        expect(weightedSum(c as 1|2|3|4|5, u as 1|2|3|4|5, e as 1|2|3|4|5)).toBeCloseTo(expected, 10);
      });
    });

    describe('rawSp: matches xlsx G19 formula (CALC-03)', () => {
      it.each([
        { w: 1.0, expected: 0.5 },
        { w: 5.0, expected: 13.0 },
        { w: 4.0, expected: 5.7570501854989171 },
        { w: 3.0, expected: 2.5495097567963922 },
      ])('rawSp($w) = $expected', ({ w, expected }) => {
        expect(rawSp(w)).toBeCloseTo(expected, 10);
      });
    });

    describe('roundFib: threshold boundaries (CALC-04 + D-22)', () => {
      it.each([
        { rawSp: 0,         expected: 0.5 },
        { rawSp: 0.5,       expected: 0.5 },
        { rawSp: 0.75,      expected: 0.5 },
        { rawSp: 0.7500001, expected: 1   },
        { rawSp: 1.0,       expected: 1   },
        { rawSp: 1.5,       expected: 1   },
        { rawSp: 1.5000001, expected: 2   },
        { rawSp: 2.0,       expected: 2   },
        { rawSp: 2.5,       expected: 2   },
        { rawSp: 2.5000001, expected: 3   },
        { rawSp: 3.0,       expected: 3   },
        { rawSp: 4.0,       expected: 3   },
        { rawSp: 4.0000001, expected: 5   },
        { rawSp: 5.0,       expected: 5   },
        { rawSp: 6.5,       expected: 5   },
        { rawSp: 6.5000001, expected: 8   },
        { rawSp: 8.0,       expected: 8   },
        { rawSp: 10.5,      expected: 8   },
        { rawSp: 10.5000001, expected: 13 },
        { rawSp: 11.0,      expected: 13  },
        { rawSp: 1000.0,    expected: 13  },
      ])('roundFib($rawSp) = $expected', ({ rawSp, expected }) => {
        expect(roundFib(rawSp)).toBe(expected);
      });
    });

    describe('calculate: 125-case xlsx parity (CALC-04 + CALC-05)', () => {
      it.each(ALL_CASES)(
        'calculate({c:$c, u:$u, e:$e}).sp = $sp',
        ({ c, u, e, sp }) => {
          const result = calculate({ c, u, e });
          expect(result.sp).toBe(sp);
          expect(typeof result.w).toBe('number');
          expect(typeof result.rawSp).toBe('number');
          expect(result.input).toEqual({ c, u, e });
        },
      );

      it('exhausts the level cross-product (5×5×5 = 125 cases)', () => {
        expect(ALL_CASES).toHaveLength(125);
        // Defensive: ensure the fixture iterates LEVELS × LEVELS × LEVELS without dupes.
        const seen = new Set<string>();
        for (const r of ALL_CASES) seen.add(`${r.c}|${r.u}|${r.e}`);
        expect(seen.size).toBe(125);
      });

      it('every Fibonacci bucket has the expected case count (CALC-05 bucket coverage)', () => {
        const counts: Record<string, number> = {};
        for (const r of ALL_CASES) counts[String(r.sp)] = (counts[String(r.sp)] ?? 0) + 1;
        expect(counts).toEqual({
          '0.5': 5,
          '1': 23,
          '2': 28,
          '3': 34,
          '5': 21,
          '8': 12,
          '13': 2,
        });
      });
    });
    ```

    Delete the placeholder file: `rm tests/smoke.test.ts` (D-27 — same commit as first real test). `tests/calc/calcEngine.test.ts` IS the first real test in the project, so its creation commit also removes the smoke placeholder. Verify after the commit: `git show --stat HEAD` shows both `tests/smoke.test.ts` (deleted) and `tests/calc/calcEngine.test.ts` (added) in a single commit.

    Hint for the executor on iteration order: the generator `for(const c of LEVELS)for(const u of LEVELS)for(const e of LEVELS)` produces rows ordered first by `c`, then `u`, then `e`. Use that exact ordering so the test names appear in a predictable order in Vitest's runner output.
  </action>
  <verify>
    <automated>npx vitest run tests/calc/</automated>
  </verify>
  <acceptance_criteria>
    - `tests/calc/fixtures/all-cases.ts` exists and contains exactly 125 case entries. Verify: `grep -cE "^\s+\{ c: '" tests/calc/fixtures/all-cases.ts` returns 125
    - Bucket counts match the verified distribution. Verify: ALL_CASES rows where sp===0.5 count to 5, sp===1 to 23, sp===2 to 28, sp===3 to 34, sp===5 to 21, sp===8 to 12, sp===13 to 2. Per-bucket greps: `grep -cE "sp: 0\.5 }," tests/calc/fixtures/all-cases.ts` (5), `grep -cE "sp: 1 }," tests/calc/fixtures/all-cases.ts` (23), `grep -cE "sp: 2 }," tests/calc/fixtures/all-cases.ts` (28), `grep -cE "sp: 3 }," tests/calc/fixtures/all-cases.ts` (34), `grep -cE "sp: 5 }," tests/calc/fixtures/all-cases.ts` (21), `grep -cE "sp: 8 }," tests/calc/fixtures/all-cases.ts` (12), `grep -cE "sp: 13 }," tests/calc/fixtures/all-cases.ts` (2)
    - `tests/calc/calcEngine.test.ts` exists and contains 5 `describe(` blocks (verify: `grep -c "^describe(" tests/calc/calcEngine.test.ts` returns 5)
    - Test file imports from `../../src/calc/levels`, `../../src/calc/fibonacci`, `../../src/calc/engine`, `./fixtures/all-cases` (verify: `grep -c "^import " tests/calc/calcEngine.test.ts` returns 4)
    - `npx vitest run tests/calc/` exits 0 with all tests passing — the executor must paste the run output into the task summary; the bucket-count assertion's pass is definitive proof of fixture correctness
    - The 21 threshold-boundary `it.each` rows are present (verify: `grep -cE "rawSp: [0-9]" tests/calc/calcEngine.test.ts` returns 21)
    - `tests/smoke.test.ts` no longer exists (verify: `[ ! -f tests/smoke.test.ts ]` exits 0). The smoke placeholder is deleted in the same commit as `tests/calc/calcEngine.test.ts` per D-27.
  </acceptance_criteria>
  <done>
    Calc test suite green. CALC-01 (level mapping), CALC-02 (weighted sum), CALC-03 (raw SP), CALC-04 (Fibonacci rounding with every threshold edge), and CALC-05 (zero-deps + every-bucket coverage) are all proven by automated tests against the xlsx-derived fixture.
  </done>
</task>

<task type="auto">
  <name>Task 6: Final invariants — zero ADO/React imports across src/calc/** (CALC-05, D-25, D-26)</name>
  <files>(verification only — no file changes)</files>
  <read_first>
    - All files created in this plan (src/calc/*.ts, tests/calc/*.ts, tests/calc/fixtures/*.ts)
    - .planning/phases/01-calc-engine-audit-parser/01-CONTEXT.md (D-25, D-26)
  </read_first>
  <action>
    Run a hard greps gate to prove the calc engine has zero ADO SDK / React contact (CALC-05 + D-25 + D-26). This task creates no files; it is a single shell-level invariant check. If any grep returns more than 0 hits (filtered for non-comment lines), the plan is broken — fix the offending file before declaring the plan done.

    Specifically run these four greps and assert each returns 0:
    1. `grep -rE "from ['\"](react|react-dom)['\"]" src/calc/ tests/calc/ | grep -v '^[^:]*:#'`
    2. `grep -rE "from ['\"]azure-devops-extension-(sdk|api)" src/calc/ tests/calc/ | grep -v '^[^:]*:#'`
    3. `grep -rE "(window\.|document\.|navigator\.|crypto\.subtle)" src/calc/ tests/calc/ | grep -v '^[^:]*:#'` (no browser-only APIs)
    4. `grep -rE "import .* from ['\"]\\.\\./(audit|field|ado|ui|entries)" src/calc/` (calc must not depend on any other src/ subsystem; tests/calc/ is allowed to import its own fixtures only)

    Each must print no lines (or only `# comment` lines that the `grep -v` filter strips). If any prints output, that file violates the boundary and must be fixed. After fixing, rerun all four.

    The `grep -v` filter is critical (per the planner's "self-invalidating grep gate" warning): a comment that mentions `react` or `azure-devops-extension-sdk` should NOT trigger the gate — only a real `import` statement should.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/calc/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "from ['\"]react" src/calc/` returns 0 lines (no React imports anywhere in calc engine)
    - `grep -rE "from ['\"]azure-devops" src/calc/` returns 0 lines
    - `grep -rE "(\\bwindow\\b|\\bdocument\\b|\\bnavigator\\b)" src/calc/ | grep -v '^[^:]*:[[:space:]]*//' | grep -v '^[^:]*:[[:space:]]*\\*'` returns 0 lines (no browser globals; comment lines are excluded)
    - `grep -rE "from ['\"]\\.\\./(audit|field|ado|ui|entries)" src/calc/` returns 0 lines (calc engine is a leaf; doesn't import from sibling subsystems)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run tests/calc/` exits 0 with all tests green
  </acceptance_criteria>
  <done>
    All boundary invariants enforced. CALC-05 zero-deps requirement is mechanically verified. Plan 01-01 is complete and Plan 01-02 can build the audit module on top, importing `Level`/`LEVELS` from `src/calc/levels` and `FibonacciSp` from `src/calc/fibonacci`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Phase 1) | Phase 1 has zero runtime trust boundaries. All inputs are TypeScript-typed at compile time; no network, no filesystem, no SDK, no DOM. Untyped runtime input only enters the system via Plan 01-02's `parse(commentBody: string)` (parser threat model in that plan). The calc engine's inputs are `Level` (closed string-literal union) and `Score` (closed numeric literal union); a caller can only pass values TypeScript already validated at compile time, and JavaScript callers from the future ADO bridge map ADO REST results into these typed inputs in Phase 4 (their boundary, not ours). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `roundFib` threshold table drift between xlsx and code | mitigate | Source provenance comment in `src/calc/fibonacci.ts` reproduces the xlsx F22 IF-chain verbatim; Task 5's 125-case fixture asserts every bucket count matches the verified distribution `{0.5:5, 1:23, 2:28, 3:34, 5:21, 8:12, 13:2}`. A drift between code and xlsx fails the bucket-count test in CI. |
| T-01-02 | Information Disclosure | Floating-point precision near threshold edges | accept | RESEARCH §Summary verifies the closest input is `W=3 → Raw=2.5495 → threshold 2.5` (diff 0.05); IEEE-754 error from `Math.pow(26, x)` is ≤10⁻¹⁵ relative — six orders of magnitude away from a bucket flip. No `Number.toFixed` rounding before threshold comparison (Anti-Pattern in RESEARCH); Task 5's threshold-boundary table tests the exact ≤/> behavior. |
| T-01-03 | Denial of Service | Catastrophic regex backtracking in calc engine | accept | Calc engine has no regexes. Audit module's regex `[^{}]*` is bounded (Plan 01-02's threat model); Phase 1's calc engine has no string parsing whatsoever. |
| T-01-04 | Elevation of Privilege | Calc engine importing browser-only or SDK APIs by mistake | mitigate | Task 6 hard-greps for `react`, `react-dom`, `azure-devops-extension-sdk`, `azure-devops-extension-api`, `window`, `document`, `navigator` and asserts zero hits across `src/calc/**` and `tests/calc/**`. CALC-05 is mechanically enforced. |
| T-01-05 | Repudiation | Test fixture diverging from xlsx and silently passing | mitigate | Task 5's bucket-count assertion is independent of the calc engine itself — it asserts the FIXTURE has the expected distribution. A bug in the fixture would fail this gate. The 125-case fixture is hand-curated from the documented generator snippet; the snippet is committed in the fixture file's header so any reviewer can regenerate. |
</threat_model>

<verification>
End-of-plan checks (automatable; no human required):

1. **Type-check passes:** `npx tsc --noEmit` exits 0.
2. **Calc tests pass:** `npx vitest run tests/calc/` exits 0; all `it.each` rows green.
3. **Public API reachable:** `node -e "const m = require('./src/calc/index.ts'); for (const k of ['LEVELS','LEVEL_TO_SCORE','SCORE_TO_LEVEL','levelToScore','scoreToLevel','roundFib','FIB_THRESHOLDS','calculate','weightedSum','rawSp']) { if (!(k in m)) { console.error('MISSING ' + k); process.exit(1); } } console.log('OK');"` (or equivalent ESM check via the test runner).
4. **Boundary invariant:** Task 6 greps return zero hits for ADO SDK / React / browser-globals imports.
5. **Bucket distribution matches RESEARCH:** `tests/calc/fixtures/all-cases.ts` row counts per `sp` value exactly match `{0.5: 5, 1: 23, 2: 28, 3: 34, 5: 21, 8: 12, 13: 2}` (asserted by Task 5's last `it()` block).

If any check fails, that's a blocker — do not declare the plan done.
</verification>

<success_criteria>
- All 6 tasks completed with their `<acceptance_criteria>` satisfied
- Files committed: `src/calc/{levels,fibonacci,engine,index}.ts`, `tests/calc/calcEngine.test.ts`, `tests/calc/fixtures/all-cases.ts`
- Files deleted: `src/calc/.gitkeep`, `tests/smoke.test.ts` (smoke deleted in same commit as `tests/calc/calcEngine.test.ts` per D-27)
- `npm run typecheck` (= `tsc --noEmit`) exits 0
- `npx vitest run tests/calc/` reports all tests passing (level mapping × 5, weighted sum × 5, raw SP × 4, threshold boundaries × 21, calculate × 125, plus 2 fixture-shape sanity checks and 1 bucket-count assertion)
- Zero imports from `react`, `react-dom`, `azure-devops-extension-sdk`, or `azure-devops-extension-api` anywhere under `src/calc/**` or `tests/calc/**`
- Phase requirements `CALC-01`, `CALC-02`, `CALC-03`, `CALC-04`, `CALC-05` all proven by tests
- Plan 01-02 can `import { calculate, type Level, type FibonacciSp } from '../calc/index'` (or directly from sibling files) without modification
</success_criteria>

<output>
After completion, create `.planning/phases/01-calc-engine-audit-parser/01-01-SUMMARY.md` documenting:
- Files created (with line counts)
- Vitest output snapshot (test count + pass count)
- Bucket distribution verified against RESEARCH
- Any TypeScript edge cases encountered (e.g., `noUncheckedIndexedAccess` interaction with `Record<Level, Score>` — RESEARCH §Pattern 1 says this should NOT widen, but record actuality if it did)
- Confirmation that `tests/smoke.test.ts` was deleted in the same commit as `tests/calc/calcEngine.test.ts` (D-27)
</output>
