---
phase: 05-polish-marketplace-publish
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
autonomous: false
requirements: [PKG-04, PKG-07]
must_haves:
  truths:
    - "The cezari org has at least three projects, each running a different process: the existing `Cezari` (Scrum), a new Agile-process project (e.g., `Agile-Test`), a new CMMI-process project (e.g., `CMMI-Test`) — added per RESEARCH §Code Examples 'Adding Agile + CMMI projects to cezari'"
    - "The smoke test per non-Scrum process per D-6 has been executed end-to-end on cezari: open modal on the appropriate work-item type → pick trio C=Hard, U=Medium, E=Easy → click Apply → verify SP field updates + Discussion comment posted, in DevTools console verify `[sp-calc/apply] postComment ok` precedes `[sp-calc/apply] setFieldValue start refName=<expected ref>` — Agile uses `Microsoft.VSTS.Scheduling.StoryPoints`, CMMI uses `Microsoft.VSTS.Scheduling.Size` (FieldResolver fallback path)"
    - "The 05-VERIFICATION.md `## Cross-Process Smoke` section is populated with verbatim DevTools console transcripts per process, the resolved SP field reference name (proving FieldResolver chose correctly), and explicit PASS / PARTIAL / FAIL verdicts per process — the file becomes the canonical record for PKG-04 (PARTIAL per D-7 — Contributor not explicitly tested) and PKG-07 (PARTIAL per D-5 — single org, multi-process)"
    - "If the smoke surfaces a real-world bug (e.g., FieldResolver fails on an unanticipated work item type, or the comment-first → field-write atomicity breaks on CMMI), it's back-ported via an atomic `fix(05-04): patch <plan-id> regression` commit to the originating plan, mirroring the Phase 4 fix-back pattern (Plan 04-06 §Real-world Corrections). This pattern preserves the Phase 4 invariant: the verification plan is allowed to fix bugs in earlier plans' files, not refactor scope."
    - "PKG-04 and PKG-07 deviations are explicitly documented in 05-VERIFICATION.md per CONTEXT D-5 / D-7 — `PKG-04: PARTIAL — install + Apply verified at the publishing user's level; Contributor flow unverified per D-7` and `PKG-07: PARTIAL — process coverage met (Scrum + Agile + CMMI); single-org coverage per D-5`"
  artifacts:
    - path: ".planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md"
      provides: "Cross-process smoke verification record (PKG-04 / PKG-07 PARTIAL); appends to the file Plan 05-01 created"
      contains: "## Cross-Process Smoke"
  key_links:
    - from: ".planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md ## Cross-Process Smoke"
      to: ".planning/REQUIREMENTS.md PKG-04 / PKG-07"
      via: "Plan 05-05 reads this section to decide whether to flip checkboxes; the verifier uses it to evaluate the PARTIAL classifications"
      pattern: "PKG-0[47]: PARTIAL"
---

<objective>
Empirically prove the FieldResolver fallback path on Azure DevOps Agile and CMMI processes by running the D-6 smoke test on cezari with a multi-process project setup. Per CONTEXT D-5 (use cezari with multiple project templates instead of two distinct trial orgs) and D-6 (smoke test only — no D-17 8-scenario re-run on non-Scrum), this plan exercises the modal end-to-end on a User Story (Agile) and a Requirement (CMMI), capturing console evidence that proves `Microsoft.VSTS.Scheduling.StoryPoints` resolves on Agile and `Microsoft.VSTS.Scheduling.Size` resolves on CMMI. Mirrors the Phase 4 verification pattern (Plan 04-06): user runs the smoke, transcripts are captured, fix-backs land atomically if bugs surface, results recorded in 05-VERIFICATION.md.

Purpose: PKG-04 (installable on a fresh ADO trial org; Contributor can complete open-modal → Apply) and PKG-07 (public listing; install + Apply on Agile + CMMI processes) both require cross-process evidence. With D-5 / D-7 deviations declared in CONTEXT.md, the verdict is PARTIAL on both — but the empirical evidence (a real Apply succeeding on each process) is the reason the verifier accepts PARTIAL instead of FAIL. Without this plan, Wave 3's public publish is built on Phase 4's Scrum-only verification and the Agile/CMMI claims would be entirely speculative.

Output: Two new cezari projects (Agile-Test + CMMI-Test); two passing smokes (one per process); a populated `## Cross-Process Smoke` section in 05-VERIFICATION.md with verbatim console transcripts; any fix-back commits if bugs surfaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-marketplace-publish/05-CONTEXT.md
@.planning/phases/05-polish-marketplace-publish/05-RESEARCH.md
@.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
@.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md

<interfaces>
<!-- Plan 05-04 reads from 05-VERIFICATION.md (created by Plan 05-01) and appends a new section. -->

**Cezari project setup procedure (verbatim from RESEARCH §Code Examples — "Adding Agile + CMMI projects to cezari"):**

```
1. Navigate to https://cezari.visualstudio.com.
2. Top-right: "+ New project" (or Organization settings → Projects → "New project").
3. Project name: "Agile-Test" (or similar).
4. Visibility: Private.
5. Advanced → Work item process: Agile.
6. Click Create. Wait ~30 seconds for provisioning.
7. Repeat for "CMMI-Test" with Work item process: CMMI.
8. The extension is already installed at the org level (cezari has it shared
   from Phase 4, version walked through Plan 05-03 to 0.2.6); no per-project
   install required.
```

**Per-process D-6 smoke procedure (verbatim from RESEARCH §Code Examples — "Verification per process"):**

```
Per process (Agile, then CMMI):
1. Create a new work item (Agile: User Story; CMMI: Requirement).
2. Click "Calculate Story Points" in the toolbar.
3. Pick: Complexity=Hard, Uncertainty=Medium, Effort=Easy (resolves to 2 SP).
4. Click Apply.
5. Verify (in DevTools console):
   - [sp-calc/apply] postComment ok commentId=<N>
   - [sp-calc/apply] setFieldValue start refName=<expected ref>
       - Agile: Microsoft.VSTS.Scheduling.StoryPoints
       - CMMI:  Microsoft.VSTS.Scheduling.Size
   - [sp-calc/apply] both writes succeeded
6. Verify (in the work item form):
   - Story Points (Agile) / Size (CMMI) field shows 2.
   - Discussion tab has new comment: "Story Points: 2 (Complexity=Hard, Uncertainty=Medium, Effort=Easy)"
7. Record evidence in 05-VERIFICATION.md.
```

**Spike-locked invariants from Phase 4 that this plan re-confirms in production on Agile + CMMI:**
- A1 STRIPPED-FALLBACK: comment is human-readable-only (no `<!-- -->` sentinel) — confirms via Discussion view inspection on each process.
- A3 LAZY-FALLBACK-ONLY: `getIsReadOnly` returns `{ isReadOnly: false, probeFailed: true }`; calculator is fully usable; FieldFailBanner only fires on actual rejection — no read-only attempt expected on cezari since the user is org admin.
- Atomicity: `[sp-calc/apply] postComment ok` MUST precede `[sp-calc/apply] setFieldValue start` in the console transcript per Phase 0 D-01.

**Scope-prevention reminders (do NOT do these — out of Phase 5 scope per CONTEXT):**
- Do NOT run the D-17 8-scenario battery on non-Scrum (D-6: smoke only).
- Do NOT explicitly test Contributor (non-admin) license (D-7: skip, document deviation).
- Do NOT acquire a separate trial ADO org (D-5: cezari multi-process is the substitute).
- Do NOT re-exercise Scenarios 3/5/7 (offline/Stakeholder/slow-3G) — punted to v2 backlog per CONTEXT recommendation.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User adds Agile + CMMI projects to cezari (manual ADO portal action)</name>
  <files>(none — human-action checkpoint; user creates projects in cezari ADO portal)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Adding Agile + CMMI projects to cezari (manual UI flow)" — verbatim procedure
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Environment Availability → cezari ADO project quota" — Free-tier 5-project ceiling caveat
    - .planning/phases/05-polish-marketplace-publish/05-CONTEXT.md D-5 (cezari multi-process substitution rationale)
  </read_first>
  <action>
    This is a human-action checkpoint. Claude cannot create ADO projects (requires authenticated portal access). The user runs the project-creation flow per the verbatim procedure in the checkpoint details below.
  </action>
  <checkpoint>
    <type>human-action</type>
    <details>
      Steps for the user (per RESEARCH §Code Examples — "Adding Agile + CMMI projects to cezari"):

      1. Navigate to https://cezari.visualstudio.com (or https://dev.azure.com/cezari) in the logged-in browser.

      2. Verify project-quota headroom: cezari Free-tier allows 5 projects. Confirm the org has fewer than 4 projects already (so 2 new ones fit without quota issues). If at the ceiling, either delete an unused project (Org Settings → Projects → ⋯ → Delete) OR upgrade to Basic tier — but the user is in control of this decision.

      3. Create the Agile project:
         - Top-right or main page: "+ New project" affordance.
         - Project name: `Agile-Test` (or any name signaling Agile process — record the literal name in chat for Task 3 to reference).
         - Visibility: Private.
         - Advanced → Work item process: **Agile**.
         - Click Create. Wait ~30 seconds for provisioning to complete.

      4. Create the CMMI project:
         - Repeat steps 3a-c.
         - Project name: `CMMI-Test` (or similar — record in chat).
         - Advanced → Work item process: **CMMI**.
         - Wait for provisioning.

      5. Verify the Story Point Calculator extension is automatically available in both new projects:
         - Open `Agile-Test` → Boards → Work Items → "+ New Work Item" → User Story.
         - On the work item form, confirm the toolbar (the `…` menu on smaller screens) shows "Calculate Story Points".
         - Repeat for `CMMI-Test` → New Work Item → Requirement.

         **Plan-Check M-3 fix — propagation note:** ADO extensions installed at the org level are available across all projects in the org by default, no per-project activation required. **However**, when adding a NEW project after the install, the toolbar contribution may not appear immediately on the first work-item-form load (caching delay). If the toolbar button is not visible on the first form load, **hard-refresh (Ctrl+Shift+R) and wait 30–60 seconds** — extension contributions can take a moment to propagate to newly-created projects' work-item form caches. Try the form load 2–3 times before treating it as a real problem.

         If the toolbar entry is STILL missing after the propagation wait + hard-refresh: org-level install state is wrong. Resolve via Organization Settings → Extensions → confirm the extension is listed and shared. Common fix: Install the extension fresh on the new project via the Marketplace listing (private install URL in the cezari extension list).

      6. Paste back into chat:
         - The two project names you created (e.g., `Agile-Test`, `CMMI-Test`).
         - Confirmation that the toolbar button appears on both new projects' work items.
         - Any quota / install-availability issues encountered.
    </details>
    <awaiting>
      User confirms both projects exist + toolbar button is visible on the relevant work-item types. Resume keyword: `cezari-projects-ready`.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>MISSING — human-action gate; user-supplied confirmation is the evidence.</automated>
  </verify>
  <done>cezari has Scrum (existing Cezari project) + Agile-Test (Agile process) + CMMI-Test (CMMI process) projects. The Story Point Calculator extension's toolbar button is visible on the relevant work-item types in all three projects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User runs the D-6 smoke per process (Agile + CMMI) and captures DevTools console transcripts</name>
  <files>(none — human-verify checkpoint; user runs the smoke and pastes transcripts back into chat for Task 3 to transcribe)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Verification per process (D-6 smoke test)" — verbatim per-process steps
    - .planning/phases/05-polish-marketplace-publish/05-CONTEXT.md D-6 (smoke only; no D-17 re-run)
    - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md §"Manual Verification Checklist (D-17 — Plan 04-06) → 1. Happy path, no current SP — PASS" (the console-transcript shape and capture protocol; reuse the same DevTools workflow)
  </read_first>
  <action>
    This is a human-verify checkpoint. The user runs the D-6 smoke per the verbatim procedure in the checkpoint details and captures DevTools console output, Network tab evidence, and one screenshot of each post-Apply work-item form (showing the SP / Size field updated to 2 + Discussion comment posted). The transcripts feed Task 3.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>
      The user runs the D-6 smoke on the Agile-Test and CMMI-Test projects created in Task 1. Per RESEARCH §"Verification per process":

      **Per process (Agile-Test first, then CMMI-Test):**

      1. Open the project's Boards. Create a new work item:
         - Agile-Test → New Work Item → **User Story**.
         - CMMI-Test → New Work Item → **Requirement**.

         Give the work item a placeholder title (e.g., "SP smoke test") and Save.

      2. Open Chrome DevTools (F12). Go to Console tab. Filter by `[sp-calc/`.

      3. On the work item form, click "Calculate Story Points" in the toolbar (or the `…` menu).

      4. The modal opens. Set:
         - Complexity = **Hard**
         - Uncertainty = **Medium**
         - Effort = **Easy**

         (Same trio as Phase 4 verification + Plan 05-03 screenshot capture — keeps cross-process comparisons clean.)

      5. Verify the Calc panel shows W=2.40, Raw SP=1.95, Final=2 SP. (If the values differ, the calc engine has regressed — STOP and report.)

      6. Click Apply.

      7. **CRITICAL VERIFICATION — capture DevTools Console output verbatim.** The console MUST show the following log lines in order (capture all `[sp-calc/...]` prefix lines from the time of the click; copy verbatim):

         Expected for Agile (User Story):
         ```
         [sp-calc/apply] postComment start fieldRefName=Microsoft.VSTS.Scheduling.StoryPoints payloadSp=2
         [sp-calc/apply] postComment ok commentId=<N>
         [sp-calc/apply] setFieldValue start refName=Microsoft.VSTS.Scheduling.StoryPoints value=2
         [sp-calc/apply] both writes succeeded
         ```

         Expected for CMMI (Requirement):
         ```
         [sp-calc/apply] postComment start fieldRefName=Microsoft.VSTS.Scheduling.Size payloadSp=2
         [sp-calc/apply] postComment ok commentId=<N>
         [sp-calc/apply] setFieldValue start refName=Microsoft.VSTS.Scheduling.Size value=2
         [sp-calc/apply] both writes succeeded
         ```

         The **`refName=` argument is the empirical proof of FieldResolver fallback** — Agile must show `StoryPoints`; CMMI must show `Size`. Anything else is a regression.

      8. Verify on the work item form:
         - The Story Points (Agile) / Size (CMMI) field shows `2`.
         - The Discussion tab has a new comment with the human-readable line "Story Points: 2 (Complexity=Hard, Uncertainty=Medium, Effort=Easy)" — no `<!-- -->` markup visible (corroborates Phase 4 spike A1 STRIPPED-FALLBACK).

      9. (Optional but recommended) Close the modal. Re-open it. The dropdowns should pre-fill with the just-applied values (Hard / Medium / Easy) — this is the Phase 3 read-path pre-fill from current SP working on the new process.

      10. Capture for the SUMMARY:
          - Verbatim console transcripts (per process).
          - Network-tab screenshot or summary (POST `/comments?api-version=7.0-preview.3` 200 → PATCH/save 200 — same shape as Phase 4 evidence).
          - One work-item-form screenshot per process (showing the SP/Size field = 2 + Discussion comment).
          - Per-process verdict: PASS (everything works as expected) / PARTIAL (works but with a caveat — describe) / FAIL (something broke — describe + which Phase 5 plan owns it).

      Per CONTEXT D-6, the smoke is END HERE for non-Scrum processes. Do NOT re-run the full D-17 8-scenario battery (offline / overwrite confirm / read-only / saving overlay). Phase 4 covered those on Scrum; the FieldResolver-fallback path is the only new variable on Agile / CMMI, and that's exercised by step 7's `refName=` log line.

      If a real-world bug surfaces (e.g., FieldResolver picks the wrong refName, the modal fails to open, the comment doesn't post), report it in chat — Task 4 fix-back handles it per the Phase 4 pattern.
    </details>
    <awaiting>
      User pastes both per-process verbatim console transcripts + Network-tab evidence + per-process verdicts (PASS / PARTIAL / FAIL with one-line reason). Resume keyword: `cross-process-smoke-captured`.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>MISSING — human-verify gate; the build is automated by Plan 05-02; the cezari run is human-driven; user-supplied transcripts are consumed by Tasks 3 + 4.</automated>
  </verify>
  <done>The user has run the D-6 smoke on Agile-Test and CMMI-Test, captured verbatim console transcripts proving FieldResolver chose the correct refName per process, and reported per-process PASS/PARTIAL/FAIL verdicts.</done>
</task>

<task type="auto">
  <name>Task 3: Append the ## Cross-Process Smoke section to 05-VERIFICATION.md with the captured evidence</name>
  <files>
    .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md (current state — Plan 05-01 created the skeleton with the `## Cross-Process Smoke (Plan 05-04 — PKG-04 / PKG-07)` placeholder)
    - The user-pasted Task 2 transcripts + screenshots
    - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md §"Manual Verification Checklist" — formatting precedent for per-scenario subheadings + verbatim transcript blockquotes + verdict lines
  </read_first>
  <action>
    Use the Edit tool to replace the `*(Populated by Plan 05-04 ...)*` placeholder in `05-VERIFICATION.md` with the populated section. The section follows this exact structure (mirror Phase 4 §"Manual Verification Checklist" formatting):

    ```markdown
    ## Cross-Process Smoke (Plan 05-04 — PKG-04 / PKG-07)

    **Verified:** <today's date YYYY-MM-DD>
    **Org:** cezari.visualstudio.com (Phase 5 multi-project setup per D-5)
    **Projects exercised:**
    - `Cezari` (Scrum) — covered by Phase 4 verification (see `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md`)
    - `<Agile project name from Task 1>` (Agile process) — exercised in this plan
    - `<CMMI project name from Task 1>` (CMMI process) — exercised in this plan
    **Run by:** Tsezari (cezo777@gmail.com)
    **VSIX version:** <whatever Plan 05-03 published, typically 0.2.6 or higher>

    ### Per-process smoke verdicts

    | Process | Work item type | Resolved SP field refName | Apply outcome | Verdict |
    |---|---|---|---|---|
    | Scrum (Phase 4 baseline) | Product Backlog Item | Microsoft.VSTS.Scheduling.StoryPoints | comment + field both succeed | PASS (Phase 4) |
    | Agile | User Story | Microsoft.VSTS.Scheduling.StoryPoints | <user-reported outcome> | <PASS | PARTIAL | FAIL> |
    | CMMI | Requirement | Microsoft.VSTS.Scheduling.Size | <user-reported outcome> | <PASS | PARTIAL | FAIL> |

    ### 1. Agile (User Story) — <verdict>

    **What was tested:** D-6 smoke on `<Agile project name>` → New User Story → toolbar Calculate SP → trio C=Hard, U=Medium, E=Easy → Apply. Verifies FieldResolver returns `Microsoft.VSTS.Scheduling.StoryPoints` (the StoryPoints branch — same field as Scrum, but the work item type is `User Story` instead of `Product Backlog Item`, exercising the FIELD-02 pure-priority path).

    **Verbatim console transcript** (from user's Task 2 capture):

    ```
    <paste the user-supplied [sp-calc/apply] log lines verbatim — typically 4-6 lines starting with "postComment start" and ending with "both writes succeeded">
    ```

    **Network tab evidence:** <user-supplied summary — typically 1 POST /comments → 200 followed by 1 PATCH /workItems/<N> → 200>

    **Form state evidence:** Story Points field on the User Story shows `2` after Apply (no page reload required). Discussion tab has new comment "Story Points: 2 (Complexity=Hard, Uncertainty=Medium, Effort=Easy)" — no `<!-- -->` markup (Phase 4 spike A1 STRIPPED-FALLBACK confirmed in production on Agile).

    **Verdict: <PASS | PARTIAL | FAIL>** — <one-line reason>

    ### 2. CMMI (Requirement) — <verdict>

    **What was tested:** D-6 smoke on `<CMMI project name>` → New Requirement → toolbar Calculate SP → same trio → Apply. **The empirical proof of the FieldResolver fallback path:** CMMI work-item types do NOT have `Microsoft.VSTS.Scheduling.StoryPoints`; FieldResolver MUST fall through to `Microsoft.VSTS.Scheduling.Size` per FIELD-02 priority order. The console transcript's `refName=` argument is the literal evidence.

    **Verbatim console transcript** (from user's Task 2 capture):

    ```
    <paste the user-supplied [sp-calc/apply] log lines verbatim — same 4-6 lines but with refName=Microsoft.VSTS.Scheduling.Size>
    ```

    **Network tab evidence:** <user-supplied summary>

    **Form state evidence:** Size field on the Requirement shows `2` after Apply. Discussion tab has new comment with the same human-readable line.

    **Verdict: <PASS | PARTIAL | FAIL>** — <one-line reason>

    ### Cross-process invariants confirmed in production

    - **FieldResolver fallback (FIELD-02):** Agile → StoryPoints; CMMI → Size. Both resolved correctly per the verbatim `refName=` console arguments above.
    - **Atomicity (Phase 0 D-01 / APPLY-07):** `[sp-calc/apply] postComment ok` precedes `[sp-calc/apply] setFieldValue start` on both processes. Comment-first → field-write order holds across all three processes (Scrum, Agile, CMMI).
    - **A1 STRIPPED-FALLBACK (Phase 4 spike):** Discussion view rendering on both new projects shows ONLY the human-readable line; the `<!-- -->` sentinel is stripped by ADO storage regardless of process.
    - **A3 LAZY-FALLBACK-ONLY (Phase 4 spike):** Calculator opens fully usable on both new projects (the user is org admin on cezari; no read-only path exercised — that's the spike-locked design).

    ### PKG-04 / PKG-07 verdicts with declared deviations

    - **PKG-04** ("Extension installable on a fresh ADO trial org; Contributor non-admin can complete open-modal → Apply"): **PARTIAL** — install + Apply verified at the publishing user's level on cezari (which serves as the multi-process substitute per D-5). Contributor (non-admin) explicit test SKIPPED per CONTEXT D-7 (rationale: vso.work_write delegated work-item permissions are the only gate; the publishing user's success path proves the SDK + REST calls work).

    - **PKG-07** ("Public listing on Marketplace; install + Apply succeeds on at least two different ADO organizations — one Agile, one CMMI"): **PARTIAL — process coverage met, single-org coverage** per CONTEXT D-5. The literal "two different ADO organizations" wording is met by FUNCTION (Agile + CMMI processes verified) but not by ORG COUNT (single org `cezari` runs all three processes). Phase 5 verifier accepts PARTIAL with this declared deviation; if escalated to a hard fail, the v2 backlog item "create a fresh trial org for CMMI" is the resolution.

    ### Real-world fix-backs (if any)

    <Task 4 fills this — either with the fix-back inventory if bugs surfaced, or with the literal note "*(No real-world corrections required — Phase 4 baseline carried cleanly to Agile + CMMI on first cezari verification.)*" if no bugs>
    ```

    Use the Edit tool with exact-match `old_string` / `new_string` to replace the placeholder line `*(Populated by Plan 05-04 — Agile + CMMI smoke verdicts on cezari per D-5/D-6.)*` with the full populated section.

    Substitute `<...>` placeholders in the template with the user-supplied data from Task 2:
    - Project names from Task 1.
    - Verbatim console transcripts from Task 2.
    - Network-tab evidence summary from Task 2.
    - Per-process verdicts from Task 2.

    Commit message: `docs(05-04): record cross-process smoke evidence — Agile + CMMI verdicts`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` no longer contains the placeholder string `*(Populated by Plan 05-04` (greppable: `! grep -F "*(Populated by Plan 05-04" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md`).
    - File contains the literal substring `Microsoft.VSTS.Scheduling.StoryPoints` AND `Microsoft.VSTS.Scheduling.Size` (greppable: `grep -F "Microsoft.VSTS.Scheduling.StoryPoints" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "Microsoft.VSTS.Scheduling.Size" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md`) — proves FieldResolver evidence is captured for both processes.
    - File contains explicit PARTIAL verdicts for PKG-04 and PKG-07 with declared deviations (greppable: `grep -F "PKG-04" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "PKG-07" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "PARTIAL" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md`).
    - File contains at least 4 verbatim `[sp-calc/` log line excerpts (greppable: `grep -c "\\[sp-calc/" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` returns >= 4 — at least the postComment + setFieldValue lines per process).
    - File commits cleanly via `docs(05-04):` commit message.
  </acceptance_criteria>
  <verify>
    <automated>! grep -F "*(Populated by Plan 05-04" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "Microsoft.VSTS.Scheduling.StoryPoints" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "Microsoft.VSTS.Scheduling.Size" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "PKG-04" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "PKG-07" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "PARTIAL" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md</automated>
  </verify>
  <done>The Cross-Process Smoke section is populated with verbatim per-process evidence; PKG-04 / PKG-07 PARTIAL verdicts are explicit; the file is committed.</done>
</task>

<task type="auto">
  <name>Task 4: Real-world fix-backs (per Phase 04-06 pattern, only if Task 2 surfaced bugs)</name>
  <files>
    (depends on which Phase 5 plan owns the bug — typically src/ado/fieldResolver.ts (FIELD-02), src/apply/apply.ts (atomicity), src/ado/postComment.ts (comment write), or src/ado/comments.ts (read path) — but no files are modified if zero bugs surface)
    .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
    vss-extension.json
  </files>
  <read_first>
    - The user-pasted Task 2 transcripts — identify FAIL or PARTIAL verdicts that point to a code defect rather than an environment quirk
    - `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` §"Real-world Corrections" — the back-port pattern (origin file, symptom, fix, phase implication, commit hash)
    - `.planning/phases/04-write-path-edge-cases/04-06-PLAN.md` Task 3 — the atomic commit naming pattern: `fix(05-04): patch <plan-id> regression — <reason>`
    - The originating plan(s) for any failing scenario:
      - FieldResolver wrong refName (Agile picked Size or vice versa) → Phase 3's FieldResolver pure module (touch `src/field/fieldResolver.ts` if a real bug; cite the Phase 3 plan but the fix-back commit lives in Phase 5).
      - Atomicity broken on CMMI (setFieldValue precedes postComment) → Phase 04-05 apply.ts orchestrator.
      - Comment doesn't post on a process → Phase 04-03 postComment.ts or comments.ts.
      - Modal fails to open on a process → Phase 02-01 toolbar.tsx OR Phase 04-05 CalcModal mode machine.
  </read_first>
  <action>
    For each FAIL or PARTIAL scenario from Task 2 that points to a code defect (NOT an environment quirk like "the new project's permissions weren't fully provisioned"):

    1. **Identify the originating plan + file.** Map the symptom to the plan that owns the buggy code (read_first hints above). The fix-back commit applies to that file.

    2. **Diagnose root cause.** State symptom + cause + fix in 2-3 sentences. Reference the verbatim console transcript line that surfaces the bug.

    3. **Apply the fix.** Minimum-change patch — do NOT refactor, rename, or expand scope. Phase 04-06 fix-backs were 1-10 line patches; Phase 5 follows the same discipline.

    4. **Commit atomically:**
       ```
       fix(05-04): patch <plan-id> regression — <one-line reason>
       ```
       Examples:
       - `fix(05-04): patch 03-02 regression — FieldResolver missed Microsoft.VSTS.Scheduling.Size on CMMI process` (touches `src/field/fieldResolver.ts`).
       - `fix(05-04): patch 04-05 regression — atomicity inverted on CMMI Requirement` (touches `src/apply/apply.ts`).

    5. **Bump manifest version + republish to cezari.** Each fix-back walks the patch number (e.g., 0.2.6 → 0.2.7 → 0.2.8). Per Plan 05-02 / Plan 05-03 publish flow: edit `vss-extension.json` `version`, `npm run build`, `npm run publish:cezari`.

    6. **Re-run the affected process smoke.** User repeats Task 2's per-process steps for the failing process; confirm PASS.

    7. **Loop:** if a fix surfaces another bug, repeat steps 1-6. Document the loop in 05-VERIFICATION.md `### Real-world fix-backs` (the section Task 3 sets aside for Task 4 to fill).

    **If Task 2 produced ZERO failures (both processes PASS or attestable PARTIAL):** SKIP this task. In Task 5 (or as part of Task 3's same commit), update the `### Real-world fix-backs` section with the literal note: `*(No real-world corrections required — Phase 4 baseline carried cleanly to Agile + CMMI on first cezari verification.)*`. The full local gate chain (`npm run typecheck && npm test && npm run build && npm run check:size`) is re-confirmed before this task is marked done — its only post-condition is "no regressions introduced."

    The task's verify command runs the full gate chain regardless of whether fix-backs landed (defensive: even if fix-backs are empty, `npm test` confirms the codebase is still green).
  </action>
  <acceptance_criteria>
    - If fix-backs were applied: each fix-back commit exists with the `fix(05-04): patch <plan-id> regression` label (greppable: `git log --oneline | grep -E "^\\w+ fix\\(05-04\\): patch 0[0-5]-\\d+ regression"` returns at least 1 match per fix-back applied).
    - If fix-backs were applied: each affected scenario re-runs PASS on cezari (user-confirmed in chat).
    - In all cases: `npm run typecheck && npm test && npm run build && npm run check:size` exits 0 (codebase remains green).
    - In all cases: `05-VERIFICATION.md`'s `### Real-world fix-backs` subsection is populated (either with the fix-back inventory or the no-corrections-needed note); no `<TBD>` or `<unfilled>` placeholders remain.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test && npm run build && npm run check:size && grep -F "### Real-world fix-backs" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md</automated>
  </verify>
  <done>Any real-world bugs surfaced by the cross-process smoke are fixed via atomic `fix(05-04)` commits to the originating plans; affected scenarios re-run PASS; codebase stays green; or, if no bugs surfaced, the verification record explicitly documents that.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User cezari project creation → cezari org state | The user adds Agile-Test + CMMI-Test projects via the ADO portal. The new projects are private (visibility: Private). No external surface. |
| User cezari run → 05-VERIFICATION.md | The user pastes verbatim console transcripts; Claude transcribes into the verification record. Same shape as Phase 4 verification record. |
| Fix-back commits → originating plan's files | Each `fix(05-04)` commit modifies files belonging to the targeted plan only. The git audit trail preserves which plan owns which fix. |
| Cross-process smoke → PKG-04/07 PARTIAL verdict | The PARTIAL verdict (D-5/D-7 deviations) is a deliberate scope decision. The verifier consumes this; if escalated to FAIL, the v2 backlog items kick in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | I (Information disclosure) | Pasted console transcripts in 05-VERIFICATION.md include cezari org name, project IDs, work item IDs | accept | Same shape as Phase 4 verification record. cezari is the user's dev org; project IDs are not PII. |
| T-05-04-02 | T (Tampering) | A fix-back commit could expand scope beyond the originating plan | mitigate | Acceptance criterion: each fix-back commit modifies files belonging to the originating plan only. The commit-message format `fix(05-04): patch <plan-id> regression — <reason>` is searchable for review. |
| T-05-04-03 | T (Tampering) | The PARTIAL verdict could be silently downgraded to PASS to ease Wave 3's checkbox-flipping | mitigate | The 05-VERIFICATION.md verdicts are EXPLICIT (PKG-04: PARTIAL, PKG-07: PARTIAL); Plan 05-05's REQUIREMENTS.md / ROADMAP.md updates must reflect them as PARTIAL or with declared-deviation footnotes. The verifier independently reads CONTEXT.md D-5/D-7 and 05-VERIFICATION.md and re-confirms the PARTIAL status. |
| T-05-04-04 | E (Elevation of privilege) | The cezari org admin can add and remove projects without restriction; deleting Agile-Test or CMMI-Test post-verification would invalidate the audit trail | accept | The user owns cezari and is in control. The verification record is the canonical evidence; once committed to git, the empirical state at run-time is preserved even if cezari state changes later. |
| T-05-04-05 | D (Denial of service) | A FieldResolver bug surfaced on CMMI could fail every CMMI customer's first Apply | mitigate | This is the EXACT failure mode the cross-process smoke is designed to surface. If a bug exists, Task 4 fix-back is the response. If no bug surfaces (Phase 3 unit tests covered it), the production confirmation closes the loop. |
</threat_model>

<verification>
- cezari has 3 projects (Scrum, Agile, CMMI); the toolbar button appears on the relevant work-item types in each.
- D-6 smoke executed end-to-end on Agile + CMMI: console transcripts captured verbatim, FieldResolver refName proven correct per process, Apply succeeded on both, Discussion comment posted on both.
- 05-VERIFICATION.md `## Cross-Process Smoke` section is populated with per-process verdicts + cross-process invariants table + PKG-04/07 PARTIAL verdicts with declared deviations.
- Any real-world fix-backs are committed atomically with `fix(05-04):` labels referencing the originating plan; affected scenarios re-run PASS.
- Codebase remains green: `npm run typecheck && npm test && npm run build && npm run check:size` exits 0.
</verification>

<success_criteria>
- PKG-04 (installable on a fresh ADO trial org; Contributor non-admin completes open-modal → Apply): **PARTIAL** per declared D-7 deviation — verified at publishing user's level; Contributor flow unverified; documented in 05-VERIFICATION.md.
- PKG-07 (public listing; install + Apply on Agile + CMMI processes): **PARTIAL** per declared D-5 deviation — process coverage met (Scrum, Agile, CMMI all exercised on cezari); single-org coverage; documented in 05-VERIFICATION.md.
- The empirical FieldResolver-fallback evidence (Agile → StoryPoints; CMMI → Size) is captured in console transcripts and is the foundation Wave 3's public publish rests on.
- Wave 3 (Plan 05-05) is unblocked from the cross-process side.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-marketplace-publish/05-04-SUMMARY.md` per the standard template. Capture:
- The two new cezari project names + provisioning notes (any quota issues encountered).
- Per-process verdict table (Scrum baseline + Agile + CMMI).
- The verbatim refName evidence for FieldResolver fallback (Agile=StoryPoints, CMMI=Size).
- Fix-back inventory (or the explicit "no fix-backs needed" note).
- Any version walks during the fix-back loop (manifest 0.2.6 → 0.2.7 → ...).
- Confirmation that Wave 3 (Plan 05-05) is unblocked.
</output>
