---
phase: 08-cleanup-and-runbooks
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/OPERATIONS.md
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "OPERATIONS.md documents the Marketplace PAT rotation procedure: 1-year cadence, step-by-step on Azure DevOps user settings (the Marketplace publisher account), scope Marketplace (publish), 'All accessible organizations', update the TFX_PAT repo secret, revoke the old token — DOC-01"
    - "OPERATIONS.md documents the release-branch model: how a master -> release promotion PR fires publish.yml, how the GitHub App is created/installed, how master's ruleset is configured (full protection + the release App on the bypass list), what the release branch's ruleset is, how to re-tighten or relax rules"
    - "OPERATIONS.md documents the rulesets-aware branch-protection probe correction: the Phase 6 probe checked GET /repos/.../branches/{b}/protection (legacy) only and missed the master ruleset; future probes MUST also query GET /repos/.../rules/branches/{b}"
    - "OPERATIONS.md has a named section '## Recovery: publish OK, commit-back failed' generalizing the Phase 7 recovery and linking 07-VERIFICATION.md — D-2"
    - "OPERATIONS.md documents the SC #5 / Option B reversibility controlled-exercise procedure (revoke TFX_PAT -> workflow_dispatch on release -> confirm safe failure -> restore -> re-run -> confirm clean recovery)"
    - "OPERATIONS.md keeps section 2 (the DOC-02 emergency-publish runbook from Plan 01) intact"
  artifacts:
    - path: ".planning/OPERATIONS.md"
      provides: "comprehensive auto-publish operations doc (all 6 sections per CONTEXT D-1)"
      contains: "aex.dev.azure.com"
  key_links:
    - from: ".planning/OPERATIONS.md section 5"
      to: ".planning/phases/07-bump-publish-tag/07-VERIFICATION.md"
      via: "markdown link to the worked recovery example"
      pattern: "07-VERIFICATION"
---

<objective>
Complete `.planning/OPERATIONS.md` — add the five sections not written in Plan 01: (1) Marketplace PAT rotation (DOC-01), (3) release-branch model + ruleset configuration + GitHub App creation steps, (4) rulesets-aware branch-protection probe note, (5) partial-failure recovery runbook (D-2), (6) SC #5 / Option B reversibility controlled-exercise procedure. Section 2 (the DOC-02 emergency-publish runbook) was written in Plan 01 and must stay intact. Per CONTEXT D-1, OPERATIONS.md is THE single comprehensive ops doc — a future maintainer opens this one file and learns the whole surface.

Purpose: durable operational knowledge — PAT rotation cadence, the release-branch model + the GitHub-UI steps the user performs (App creation, ruleset config — Claude can't do these but must document them so they're reconstructable), the Phase 6 probe-gap correction, how to recover from a publish-OK / commit-back-failed split, and how to run the Option B reversibility exercise. Per Phase 6/7 D-3, this is the home for all the "why" the workflow YAML deliberately omits.

Output: complete `.planning/OPERATIONS.md` (all 6 sections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-and-runbooks/08-CONTEXT.md
@.planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md
@.planning/phases/07-bump-publish-tag/07-VERIFICATION.md
@.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md
@.planning/OPERATIONS.md
@.github/workflows/publish.yml
@.github/workflows/ci.yml
</context>

<discretion_decisions>
Planner decisions (from CONTEXT.md "Claude's Discretion") to honor in the doc:

- **Section order:** `## 1. Marketplace PAT rotation`, `## 2. Manual emergency-publish runbook` (already present from Plan 01 — do not rewrite, only verify), `## 3. Release-branch model & ruleset configuration`, `## 4. Rulesets-aware branch-protection probe (Phase 6 correction)`, `## 5. Recovery: publish OK, commit-back failed`, `## 6. SC #5 / Option B reversibility — controlled exercise`. (Section 5's heading text `## Recovery: publish OK, commit-back failed` is mandated by CONTEXT D-2 — keep that exact phrase, optionally prefixed with `5.`.)
- **App name / secret names / pins / release branch / back-merge mechanism:** must match Plan 01's decisions exactly — App `story-point-release-bot`, secrets `APP_ID` + `APP_PRIVATE_KEY`, `actions/create-github-app-token@v2`, branch `release`, back-merge via `gh pr create --base master --head release`. Document these as the chosen design.
- **`release` ruleset:** document it as "light or none — recommended: only 'Require linear history' (or nothing); do NOT add 'Require signed commits' or 'Require a pull request' to `release` unless the App is also on `release`'s bypass list, because the publish workflow pushes the bump commit + tag directly to `release` with the App token."
- **PAT scope wording:** document `Marketplace (publish)` as the minimal scope with the flagged caveat from RESEARCH A2/Q2: "some Microsoft docs say `Marketplace (Manage)`; the current `TFX_PAT` works with whatever scope it has — if a rotated `Publish`-only PAT 401s on `tfx extension publish`, widen to `Manage` and record which scope worked."
</discretion_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Add OPERATIONS.md section 1 — Marketplace PAT rotation (DOC-01)</name>
  <files>.planning/OPERATIONS.md</files>
  <read_first>
    - .planning/OPERATIONS.md (the file Plan 01 created — read it so you ADD section 1 above section 2, not overwrite section 2)
    - .planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md (the "### PAT Rotation Procedure (DOC-01 content — for OPERATIONS.md)" code block — use it almost verbatim; RESEARCH A2/Q2 for the scope caveat)
    - .planning/research/STACK.md? (only if present — for the `Marketplace (publish)` / "All accessible orgs" / 1-year lifespan auth model confirmation; otherwise rely on RESEARCH which already cites it)
  </read_first>
  <action>
Insert a `## 1. Marketplace PAT rotation` section BEFORE the existing `## 2. Manual emergency-publish runbook` section. Use this content (the executor may lightly reword surrounding prose but the numbered steps + the scope/org/lifespan facts must be present):

```markdown
## 1. Marketplace PAT rotation (1-year cadence)

The `TFX_PAT` repo secret authenticates `tfx extension publish` against the Visual
Studio Marketplace. Azure DevOps caps PAT lifespan at 1 year. Rotate proactively —
set a calendar reminder for ~11 months after the last rotation. (The current `TFX_PAT`
last worked on the v1.0.8 publish, run #25641329824.)

1. Sign in to https://dev.azure.com/ with the Microsoft account that owns the
   Marketplace publisher `TsezariMshvenieradzeTfsAiReviewTask`. (The Marketplace
   publisher-management surface is reachable via https://aex.dev.azure.com/ — but the
   PAT itself is created in Azure DevOps user settings, not on aex.dev.azure.com.)
2. User settings (top-right avatar) -> Personal access tokens -> + New Token.
3. Name: e.g. `marketplace-publish-2027`. Organization: **All accessible organizations**
   (NOT a single org — tfx-cli's publish API operates outside org context; a single-org
   PAT 401s). Expiration: **Custom defined -> 1 year** (the maximum).
4. Scopes: **Custom defined** -> scroll to **Marketplace** -> check **Publish** (the
   minimal scope `tfx extension publish` needs; do NOT add Manage or Acquire).
   NOTE: some Microsoft docs say `Marketplace (Manage)`. The current `TFX_PAT` works with
   whatever scope it has (it published v1.0.8 green). If a rotated `Publish`-only PAT
   401s on the publish step, widen to `Manage` — and record in this doc which scope
   worked.
5. Create -> copy the token (shown once).
6. GitHub repo -> Settings -> Secrets and variables -> Actions -> `TFX_PAT` -> Update
   secret -> paste the new value.
7. Back in Azure DevOps PAT settings -> revoke the OLD token.
8. Verify: trigger `publish.yml` via `workflow_dispatch` on the `release` branch (or wait
   for the next `master -> release` promotion) and confirm the publish step authenticates.
```
  </action>
  <verify>
    <automated>grep -q '## 1. Marketplace PAT rotation' .planning/OPERATIONS.md && grep -q 'aex.dev.azure.com' .planning/OPERATIONS.md && grep -qi 'All accessible organizations' .planning/OPERATIONS.md && grep -q '## 2. Manual emergency-publish runbook' .planning/OPERATIONS.md && grep -qF 'npx tfx extension publish --vsix dist/*.vsix --auth-type pat --token "$TFX_PAT" --no-prompt --no-wait-validation' .planning/OPERATIONS.md && echo SEC1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/OPERATIONS.md` contains a `## 1. Marketplace PAT rotation` heading.
    - It contains `aex.dev.azure.com` and `All accessible organizations` and `1 year` and `Marketplace` + `Publish` (the scope) and `TFX_PAT` + `Update secret` (the GitHub side) and a `revoke` step for the old token.
    - The pre-existing `## 2. Manual emergency-publish runbook` section and its `npx tfx extension publish --vsix dist/*.vsix --auth-type pat --token "$TFX_PAT" --no-prompt --no-wait-validation` line are STILL present (Plan 01's capture is not clobbered).
  </acceptance_criteria>
  <done>OPERATIONS.md has the DOC-01 PAT-rotation section above the DOC-02 section; the DOC-02 capture is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add OPERATIONS.md sections 3 + 4 — release-branch model, ruleset config, GitHub App steps, rulesets-aware probe note</name>
  <files>.planning/OPERATIONS.md</files>
  <read_first>
    - .planning/OPERATIONS.md (current state — append sections 3 and 4 after section 2)
    - .planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md (`## Architecture Patterns` -> the System Architecture Diagram + Patterns 1-4; `## Environment Availability` -> "User actions required (Claude can't do these — UI only)" list (steps 1-6); `## Don't Hand-Roll` -> the "Detecting branch-protection / ruleset state in a probe" row; the DOC-02 row's Pitfall 6 cross-reference)
    - .planning/phases/07-bump-publish-tag/07-VERIFICATION.md (the GH013 ruleset-rejection finding + the "Phase 6 probe checked legacy not rulesets" critical finding — section 4 documents the correction)
    - .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md (the probe artifact whose legacy-only check missed the master ruleset)
    - .github/workflows/publish.yml (the now-refactored file — section 3 describes the flow it implements)
  </read_first>
  <action>
Append two sections after section 2.

`## 3. Release-branch model & ruleset configuration` — must cover:
- **The model:** feature PRs target `master` (fully protected); when ready to ship, a maintainer opens a `master -> release` promotion PR; `ci.yml` runs on it; merging it via the Web UI pushes to `release`, which fires `publish.yml` (minus `paths-ignore`); the workflow bumps the version, packages + uploads the `.vsix`, publishes to Marketplace, commits `chore(release): vX.Y.Z [skip ci]` to `release` via the GitHub App token, tags the release, and opens a `release -> master` back-merge PR; the maintainer merges that PR via the Web UI (the merge commit's verified signature satisfies master's signed-commits rule) and `master` catches up.
- **Why a `release` branch:** master gets frequent feature merges; publishing on every one is too noisy — batch several into one release by promoting when ready (CONTEXT D-3 rationale).
- **GitHub App creation steps (the user does this in the GitHub UI):**
  1. GitHub -> Settings (your account, top-right avatar) -> Developer settings -> GitHub Apps -> New GitHub App.
  2. Name: `story-point-release-bot`. Homepage URL: the repo URL is fine. Uncheck "Active" under Webhook (no webhook needed).
  3. Repository permissions: `Contents` = Read and write; `Pull requests` = Read and write; `Metadata` = Read-only (auto-selected). Nothing else.
  4. "Where can this GitHub App be installed?" -> Only on this account.
  5. Create the App. On the App's page: note the **App ID** (a number); click **Generate a private key** -> downloads a `.pem` file.
  6. Install App -> install on the `tsmshvenieradze/StoryPointExtension` repository (Only select repositories -> pick it).
  7. GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret: `APP_ID` = the App ID number; `APP_PRIVATE_KEY` = the entire contents of the `.pem` file (including the `-----BEGIN/END-----` lines). Delete the `.pem` file locally afterward.
- **`release` branch creation (user, UI or CLI):** create `release` off `master` (`git checkout master && git pull && git checkout -b release && git push -u origin release`, or via the GitHub branch UI). It is long-lived; never delete it.
- **master ruleset configuration (user, GitHub UI):** repo -> Settings -> Rules -> Rulesets -> the existing master ruleset (or create one) targeting `master`. Re-enable: "Require a pull request before merging", "Require status checks to pass" (add the two `ci.yml` checks — the `Build & verify` job, which produces 2 contexts as observed by Phase 7), "Require signed commits". Under "Bypass list": add the `story-point-release-bot` GitHub App, mode "Always allow". (The design's master path is human-merge, so the App bypass on master is belt-and-suspenders for any future App-driven push — but add it so a `gh pr merge` via the App token would also work.)
- **`release` ruleset configuration (user, GitHub UI):** keep it LIGHT — recommended: a ruleset on `release` with only "Require linear history" (or no ruleset at all). Do NOT add "Require signed commits" or "Require a pull request before merging" to `release` UNLESS you also add the `story-point-release-bot` App to `release`'s bypass list — because `publish.yml` pushes the bump commit + the tag DIRECTLY to `release` with the App token, and an unsigned/PR-required rule there would block it (the GH013 trap Phase 7 hit on master). Recommendation: light is simpler — `release` is the automation lane, not the human-protected lane.
- **How to re-tighten / relax:** to relax temporarily (e.g. emergency), edit the master ruleset and toggle the rule off; remember to re-enable. The bypass-list App entry should normally stay.
- **One user-asserted, not-doc-verified item (RESEARCH A1/Q1):** "a GitHub App on a ruleset's bypass list bypasses ALL rules in that ruleset including signed-commits" is asserted by the user, not unambiguously stated in GitHub's docs. The current design does NOT rely on the App pushing directly to a signed-commits-protected branch (master is human-merged; `release` is kept light). If a future change wants the App to push directly to a signed-commits-protected branch, verify empirically first (one test push).

`## 4. Rulesets-aware branch-protection probe (Phase 6 correction)` — must cover:
- The Phase 6 probe (`.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md`) checked only `GET /repos/{owner}/{repo}/branches/{branch}/protection` (the LEGACY branch-protection API) and concluded "NOT PROTECTED". That was correct for legacy branch protection but MISSED the repository **ruleset** on `master`, which then rejected the Phase 7 commit-back bot push with `GH013` (see `07-VERIFICATION.md`).
- The correction: any future branch-protection probe MUST query BOTH endpoints — `GET /repos/{owner}/{repo}/branches/{branch}/protection` (legacy) AND `GET /repos/{owner}/{repo}/rules/branches/{branch}` (rulesets). The `publish.yml` probe step is informational only (it can't get admin scope from `GITHUB_TOKEN`); this OPERATIONS.md note is the durable source-of-truth correction.
- A copy-pasteable `gh` snippet:
  ```bash
  gh api repos/tsmshvenieradze/StoryPointExtension/branches/master/protection || echo "(legacy branch protection: none or no scope)"
  gh api repos/tsmshvenieradze/StoryPointExtension/rules/branches/master   # <- the one Phase 6 missed
  gh api repos/tsmshvenieradze/StoryPointExtension/rules/branches/release
  ```
  </action>
  <verify>
    <automated>grep -q '## 3. Release-branch model' .planning/OPERATIONS.md && grep -q '## 4. Rulesets-aware branch-protection probe' .planning/OPERATIONS.md && grep -q 'story-point-release-bot' .planning/OPERATIONS.md && grep -q 'APP_PRIVATE_KEY' .planning/OPERATIONS.md && grep -q 'rules/branches/master' .planning/OPERATIONS.md && grep -q 'GH013' .planning/OPERATIONS.md && echo SEC34_OK</automated>
  </verify>
  <acceptance_criteria>
    - `## 3. Release-branch model & ruleset configuration` exists and contains: `story-point-release-bot`, `APP_ID`, `APP_PRIVATE_KEY`, the GitHub App creation steps (Developer settings -> GitHub Apps -> New GitHub App), the master ruleset re-tighten steps (PR-required + status checks + signed commits + App on bypass), the `release` ruleset "keep it light" guidance, and the A1/Q1 "user-asserted, not doc-verified" caveat.
    - `## 4. Rulesets-aware branch-protection probe (Phase 6 correction)` exists and contains: `rules/branches/master` (the rulesets endpoint), a reference to the Phase 6 probe artifact, `GH013`, and a `gh api` snippet querying BOTH the legacy and rulesets endpoints.
    - Sections 1 and 2 still present and unmodified in substance.
  </acceptance_criteria>
  <done>OPERATIONS.md documents the release-branch model + all the GitHub-UI user actions + the Phase 6 probe-gap correction.</done>
</task>

<task type="auto">
  <name>Task 3: Add OPERATIONS.md sections 5 + 6 — partial-failure recovery runbook (D-2) and SC #5 / Option B controlled-exercise procedure</name>
  <files>.planning/OPERATIONS.md</files>
  <read_first>
    - .planning/OPERATIONS.md (current state — append sections 5 and 6 after section 4)
    - .planning/phases/07-bump-publish-tag/07-VERIFICATION.md (the worked recovery example: PR #7 `chore(release): v1.0.8 [skip ci]` hand-bump squash-merge + manual annotated tag — section 5 generalizes this and links here)
    - .planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md (`## Common Pitfalls` Pitfall 5 on the SC #5 exercise; the SC #5 procedure under CONTEXT D-4; `## Architecture Patterns` Option B / "POINT OF NO RETURN" line)
    - .github/workflows/publish.yml (Option B ordering: bump in-memory -> publish -> commit -> tag; publish failing leaves `release` untouched)
  </read_first>
  <action>
Append two sections.

`## 5. Recovery: publish OK, commit-back failed` (use this EXACT heading text per CONTEXT D-2; the `5.` prefix is fine) — must cover:
- **When this happens:** `publish.yml` published the `.vsix` to Marketplace successfully, but a later step failed — the App-token commit-back to `release` was rejected (e.g. a misconfigured `release` ruleset → `GH013`), or the tag push failed (tag step is best-effort so the workflow stays green, but `release`'s `version` field is now behind the published Marketplace version). Note: per Option B, if the PUBLISH step itself failed, there is NOTHING to recover — the bump is in-memory only, `release` is untouched, no orphan commit/tag (this is the SC #5 happy-failure case, section 6); just re-run via `workflow_dispatch`.
- **The recovery procedure (generalizes Phase 7's recovery):**
  1. Confirm the published version on the Marketplace listing's Versions table (call it `vX.Y.Z`).
  2. On a branch off `release` (or `master`): hand-edit `package.json` `version` AND `vss-extension.json` `version` to `X.Y.Z` (both files, atomic).
  3. Commit with `chore(release): vX.Y.Z [skip ci]` — the `[skip ci]` is mandatory so the recovery PR's merge doesn't re-fire `publish.yml`.
  4. Open a PR (`-> release` if `release` is behind; `-> master` if you're syncing master). Merge it via the GitHub Web UI (verified-signature merge commit).
  5. Push the annotated tag manually if it's missing: `git tag -a vX.Y.Z -m "Release vX.Y.Z" && git push origin vX.Y.Z`.
  6. If `release` and `master` `version` fields diverged, also open a `release -> master` PR so the bump propagates (the workflow normally does this automatically — see section 3).
- **Link to the worked example:** "For the concrete Phase 7 instance (v1.0.8 — commit-back blocked by the master ruleset, recovered via PR #7 + manual tag), see [`.planning/phases/07-bump-publish-tag/07-VERIFICATION.md`](phases/07-bump-publish-tag/07-VERIFICATION.md)."
- **Never hand-edit `version` outside this runbook** (RESEARCH Pitfall 8): the workflow's `bump-version.mjs` owns the `version` field on `release` (max-wins); `master` receives it via the back-merge PR; a `## Drift reconciled` block in a run summary signals divergence.

`## 6. SC #5 / Option B reversibility — controlled exercise` — must cover:
- **Purpose:** prove that a failed publish leaves Marketplace + `release` untouched (no orphan bump commit, no orphan tag) and that re-running after fixing the cause recovers cleanly. This is ROADMAP Phase 7 SC #5; Phase 7 only observed the recoverable-state half (a post-publish commit-back failure left the repo clean) — this exercise covers the publish-step-failure variant deliberately.
- **The procedure (the user revokes/restores the PAT — Claude can't):**
  1. Azure DevOps -> User settings -> Personal access tokens -> revoke the `TFX_PAT` token (or temporarily set the `TFX_PAT` repo secret to an obviously-invalid value — revoking is more realistic).
  2. GitHub -> Actions -> `Publish` workflow -> Run workflow -> branch `release` (`workflow_dispatch`).
  3. Confirm: the `Publish to Marketplace` step FAILS; the Marketplace listing's Versions table is UNCHANGED (still the prior version); `release` has NO new bump commit; NO new tag was pushed. (The bump ran in-memory only — Option B — so nothing leaked.)
  4. Mint a fresh `TFX_PAT` per section 1; update the `TFX_PAT` repo secret.
  5. Re-run the `Publish` workflow on `release` (`workflow_dispatch`). Confirm: publish succeeds, the bump commit lands on `release`, the tag is pushed, the `release -> master` PR is opened. Merge the back-merge PR via the Web UI.
- **Cost note:** the re-run in step 5 ships a real patch version. If a separate re-verification run already shipped one (e.g. v1.0.9), the SC #5 re-run ships the next (v1.0.10). The exercise's clean re-run can double as a re-verification of the new model.
- **Where the evidence goes:** capture the failure-then-recovery in `.planning/phases/08-cleanup-and-runbooks/08-SC5-EXERCISE.md` (this is evidence-of-event; this OPERATIONS.md section is the how-to procedure). Plan 08-03 produces that artifact.
  </action>
  <verify>
    <automated>grep -q '## 5. Recovery: publish OK, commit-back failed' .planning/OPERATIONS.md && grep -q '## 6. SC #5' .planning/OPERATIONS.md && grep -q '07-VERIFICATION' .planning/OPERATIONS.md && grep -qi 'workflow_dispatch' .planning/OPERATIONS.md && grep -q '08-SC5-EXERCISE' .planning/OPERATIONS.md && echo SEC56_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/OPERATIONS.md` contains a heading with the exact phrase `Recovery: publish OK, commit-back failed` (a `5.` prefix is acceptable) — D-2's mandated section.
    - That section: describes the hand-bump-both-manifests + `[skip ci]` recovery-PR + manual annotated-tag procedure, and links `07-VERIFICATION.md`.
    - `.planning/OPERATIONS.md` contains a `## 6. SC #5` (Option B reversibility) section describing: revoke `TFX_PAT` -> `workflow_dispatch` on `release` -> confirm safe failure (Marketplace + `release` untouched, no orphan commit/tag) -> restore -> re-run -> confirm clean recovery; and references `08-SC5-EXERCISE.md` as the evidence artifact.
    - All six sections (1-6) are now present in OPERATIONS.md, in order; section 2's DOC-02 capture is intact.
  </acceptance_criteria>
  <done>OPERATIONS.md is the complete 6-section comprehensive ops doc per CONTEXT D-1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> readers | OPERATIONS.md is `.planning/` (not shipped, but committed to the public repo) — it must reference secret *names* (`TFX_PAT`, `APP_ID`, `APP_PRIVATE_KEY`), never secret *values*; the PAT-rotation steps must say "copy the token, paste into the secret" — never embed a real token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | OPERATIONS.md content | mitigate | The doc references `TFX_PAT` / `APP_ID` / `APP_PRIVATE_KEY` only by name; the PAT-rotation and App-creation steps describe the flow ("copy", "paste into the secret", "delete the .pem locally") without embedding any real credential. Acceptance criteria forbid a literal token-looking value. |
| T-08-08 | Repudiation / integrity | the relaxed-master interim state | mitigate (documented) | Section 3 documents that master MUST end fully protected (PR + status-checks + signed-commits) with the `story-point-release-bot` App on the bypass list — closing the Phase 7 interim relaxation. The "how to re-tighten/relax" subsection makes the regression-and-recovery explicit. The actual re-tighten is a user action verified in Plan 03. |
| T-08-09 | Tampering | the A1 "App bypasses signed-commits" assumption | accept (flagged) | Section 3 explicitly flags this as user-asserted, not doc-verified, and notes the current design does not depend on it. Residual risk: low (the design is robust to A1 being wrong). |

No `high`-severity unmitigated threats. No application code is touched (docs-only plan).
</threat_model>

<verification>
- `.planning/OPERATIONS.md` has all 6 sections in order: 1 (PAT rotation), 2 (emergency-publish — from Plan 01), 3 (release-branch model + ruleset config + App steps), 4 (rulesets-aware probe note), 5 (`Recovery: publish OK, commit-back failed`), 6 (SC #5 / Option B exercise).
- Section 2's DOC-02 capture (`npx tfx extension publish --vsix dist/*.vsix ...`) is intact.
- `grep -c '^## ' .planning/OPERATIONS.md` is >= 6.
- No literal credential-looking string (a bare 52-char base32-ish token, or `-----BEGIN` followed by key material) appears: `! grep -qE '^-----BEGIN (RSA )?PRIVATE KEY-----[A-Za-z0-9+/=]' .planning/OPERATIONS.md && echo NO_KEY_MATERIAL`.
</verification>

<success_criteria>
- OPERATIONS.md is the single comprehensive auto-publish ops doc (CONTEXT D-1, all 6 sections).
- DOC-01 (PAT rotation) satisfied with the `aex.dev.azure.com` / `Marketplace (publish)` / "All accessible orgs" / 1-year facts.
- The user-action steps (App creation, ruleset config, `release` branch creation, PAT revoke/restore) are documented so they're reconstructable.
- No secret values in the doc.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-and-runbooks/08-02-SUMMARY.md`.
</output>
