---
phase: 06-workflow-scaffold-and-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - README.md
  - webpack.config.cjs
  - .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md
autonomous: false
requirements:
  - CI-01
  - CI-02
  - CI-03
  - GATE-06
  - FAIL-03
must_haves:
  truths:
    - "A docs-only commit (README.md whitespace edit) merged to master does NOT trigger publish.yml — paths-ignore filter works (D-1a negative case, success criterion #3)"
    - "A code-side commit (webpack.config.cjs whitespace edit) merged to master DOES trigger publish.yml; gates pass; dry-run echoes `would publish v1.0.8` (D-1a positive case, success criteria #2 + #5)"
    - "Neither merge triggers ci.yml on the master push side (CI-02 runtime proof, success criterion #2)"
    - "TFX_PAT-resolves probe step prints `tfx-pat-present=true` in the positive-case run (success criterion #6)"
    - "Branch-protection probe result is captured to `.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md` as a durable phase artifact for Phase 7 to consume (D-5)"
  artifacts:
    - path: ".planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md"
      provides: "Master branch-protection state captured from gh api, durable across phases for P7 commit-back token decision"
      contains: "branch protection"
    - path: "README.md"
      provides: "Negative-case test — whitespace edit (single trailing newline or single trailing space) that exercises paths-ignore"
    - path: "webpack.config.cjs"
      provides: "Positive-case test — single whitespace/comment edit that does NOT match paths-ignore patterns"
  key_links:
    - from: "Negative-case master push (README.md whitespace)"
      to: "publish.yml SKIPPED (paths-ignore matches `**.md`)"
      via: "GitHub Actions trigger evaluator"
      pattern: "Actions tab shows NO publish.yml run for the README-only merge commit"
    - from: "Positive-case master push (webpack.config.cjs whitespace)"
      to: "publish.yml RUNS, gates pass, dry-run echo emits `would publish v1.0.8`"
      via: "GitHub Actions trigger evaluator + sequential job"
      pattern: "Actions tab shows publish.yml run, all 10 steps green, dry-run step log contains `would publish v1.0.8`"
    - from: "Branch-protection probe step output (`gh api` 200 or 404)"
      to: "branch-protection-probe-result.md committed in phase dir"
      via: "human-paste from Actions UI run page OR local `gh api` invocation"
      pattern: "branch-protection-probe-result.md exists and contains either the protection JSON object OR the literal string `Branch not protected`"
---

<objective>
Live-verify on the real master branch that the two-workflow split works end-to-end. Run both halves of the D-1a verification dance: a negative-case docs-only commit (must SKIP publish.yml) and a positive-case code commit (must RUN publish.yml, pass all gates, dry-run echo the next version). Capture the branch-protection-probe result as a durable phase artifact for Phase 7's planner.

Purpose: Pure YAML inspection cannot prove that `secrets.TFX_PAT` actually resolves at runtime, that `paths-ignore` actually filters a real merge commit, or that the dry-run echo actually fires the expected stdout line on the real ubuntu-latest runner. Only a force-merge to master proves the plumbing — and D-1a explicitly mandates BOTH sides of the test (negative AND positive) so that P6 doesn't ship with only the skip-path verified.

Output: 2 trivial whitespace edits merged to master via PR (each via the GSD branch-protection-respecting "feature branch + PR + merge" path); 1 phase artifact (`branch-protection-probe-result.md`); confirmation in 06-03-SUMMARY.md that both runs went green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-workflow-scaffold-and-gates/06-CONTEXT.md
@.planning/phases/06-workflow-scaffold-and-gates/06-RESEARCH.md
@.github/workflows/ci.yml
@.github/workflows/publish.yml
@README.md
@webpack.config.cjs
@package.json
@vss-extension.json

<interfaces>
<!-- Current package.json version (verified): "1.0.7" — so the dry-run echo MUST print `would publish v1.0.8` on the positive-case run. -->

<!-- vss-extension.json version (verified): "1.0.7" — the asset audit reads this file's `files[]` (dist, images, overview.md), all three must exist on the runner after `npm run build`. -->

<!-- paths-ignore patterns from D-6 (locked): `**.md`, `.planning/**`, `.claude/**`, `docs/**`. README.md sits at repo root and matches `**.md`. webpack.config.cjs sits at repo root and does NOT match any of the four patterns — it's a positive-case target. -->

<!-- Negative-case edit shape: a trailing-newline addition or a single trailing space inside README.md that produces a 1-line diff with `git diff --shortstat` showing 1 file, 1 insertion (or 1 line changed). Goal: minimal but real change so GitHub commits and pushes; not so trivial that `git push` reports "nothing to commit". -->

<!-- Positive-case edit shape: a similarly trivial whitespace edit inside webpack.config.cjs (e.g., a trailing newline at EOF, or one extra space at end of an existing comment line). Must not change webpack semantics — the production build must still succeed (otherwise GATE-04 fails the run for the wrong reason). -->

<!-- IMPORTANT: per CLAUDE.md "no comments by default" rule, do NOT add a "// test commit" comment. The whitespace-only nature is the entire point — minimal-blast-radius edit that exercises the trigger filter. -->

<!-- The branch-protection probe result file at `.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md` is a NEW file. Format is human-readable markdown wrapping either the gh-api JSON body (HTTP 200) or the "Branch not protected" body (HTTP 404). Pattern from RESEARCH.md "Verified pattern: separate phase-artifact capture for branch-protection result" (line 791-800). -->

<!-- Default expectation per STATE.md / ROADMAP.md cross-phase note 1: branch-protection-probe-result is "NOT protected" (404) — the working assumption from commit-history inspection. The probe is what makes that assumption durable. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Negative-case verification — README.md whitespace edit (D-1a, CI-03, success criterion #3)</name>
  <files>README.md</files>
  <read_first>
    - .github/workflows/publish.yml — confirm the file is committed to master (not just to milestone1.1) BEFORE running this verification. If publish.yml is not yet on master, this test cannot fire because there's no workflow on master to evaluate the trigger.
    - .planning/phases/06-workflow-scaffold-and-gates/06-CONTEXT.md decisions D-1, D-1a — verification approach + both-cases mandate.
    - .planning/phases/06-workflow-scaffold-and-gates/06-RESEARCH.md "Pitfall A3" — paths-ignore filters only when ALL changed files match; the verification commit MUST touch ONLY README.md (no .gitignore changes, no incidental file rename).
    - README.md — the existing file's last line, so the executor knows what the trailing-newline shape currently is.
  </read_first>
  <what-built>
    Plan 06-01 removed `push: master` from ci.yml; Plan 06-02 created publish.yml with `paths-ignore: ['**.md', '.planning/**', '.claude/**', 'docs/**']`. This task verifies that a docs-only commit merged to master is filtered out — neither workflow should fire on the master push side.
  </what-built>
  <how-to-verify>
    PRECONDITION: Plans 06-01 and 06-02 must already be merged to master (this is Wave 2; both Wave-1 plans must have landed via PR). If they're still on the milestone1.1 branch, merge that branch to master first via PR before starting this task. The `paths-ignore` filter is evaluated against publish.yml AS IT EXISTS ON MASTER at the time of the push, so publish.yml must already be on master for this test to mean anything.

    Step 1 — Prepare a tiny README.md edit on a feature branch:
      a. From master tip, create a fresh feature branch: `git checkout master && git pull && git checkout -b chore/p6-verify-negative-case`.
      b. Make a single trivial whitespace edit to README.md. Recommended: add a trailing newline at end of file if absent, OR remove a single trailing space if present, OR add+remove the same trailing newline so `git diff --shortstat README.md` shows `1 file changed, 1 insertion(+), 0 deletions(-)` (or equivalent 1-line diff). DO NOT touch any other file. DO NOT add a `// test commit` comment.
      c. `git status` MUST show only `modified: README.md`. If it shows anything else (e.g. an accidental .DS_Store, a webpack lockfile change), abort and revert.

    Step 2 — Push and PR:
      a. `git add README.md && git commit -m "chore(p6): negative-case verify — README whitespace edit"`.
      b. `git push -u origin chore/p6-verify-negative-case`.
      c. Open a PR from `chore/p6-verify-negative-case` → master via `gh pr create` or the GitHub UI. ci.yml SHOULD fire on the PR (since ci.yml is now PR-only after Plan 06-01).
      d. Wait for ci.yml to go green on the PR.

    Step 3 — Merge to master and observe the trigger filter:
      a. Merge the PR via squash-merge or merge-commit (either is fine; both result in a `push` event on master with only README.md changed).
      b. Open the GitHub Actions tab and watch the master-tip event for ~30 seconds.

    EXPECTED OBSERVATIONS:
      a. ci.yml shows NO new run for the master push (because Plan 06-01 removed `push: master` from ci.yml's `on:` block).
      b. publish.yml shows NO new run for the master push (because the merge commit's only changed file is README.md, which matches the `**.md` paths-ignore pattern, and `paths-ignore` skips a run only when ALL changed paths match — they do).
      c. The Actions tab tells you "No workflows ran for this push" or shows older unrelated runs only.

    Step 4 — Capture the result:
      a. Screenshot or copy-paste the Actions tab view showing no run fired for this master push, and paste it into the 06-03-SUMMARY.md (Plan output).

    FAILURE MODES (each indicates a P6 plumbing bug, not a test design issue):
      - publish.yml fires anyway → paths-ignore is misconfigured (most likely cause: a typo in one of the four patterns, e.g. `'.md'` instead of `'**.md'`, or quotes mismatched). Fix Plan 06-02's publish.yml and re-run.
      - ci.yml fires on the master push → Plan 06-01's edit didn't actually land on master (master copy of ci.yml still has `push: master`). Re-merge Plan 06-01 to master.
      - The merge commit included files beyond README.md (rare; typically a stale .gitignore or an editor sidecar) → revert and start the verification over with a clean diff.
  </how-to-verify>
  <acceptance_criteria>
    - A merge commit lands on master via PR with `git diff --name-only HEAD~1` (or equivalent for squash) showing exactly `README.md`.
    - GitHub Actions tab shows ZERO new workflow runs triggered by this master push (neither ci.yml nor publish.yml). Equivalent CLI check: `gh run list --workflow=publish.yml --branch=master --limit=5` returned before this push and again after — count is unchanged for the push event.
    - The PR run of ci.yml (which fired BEFORE the merge, on the PR open) DID go green — confirms ci.yml's PR-only behavior still works after Plan 06-01.
    - 06-03-SUMMARY.md (created at end of plan) records the merge commit SHA, the fact that no workflow fired on master push, and a screenshot or text capture of the Actions tab state.
  </acceptance_criteria>
  <resume-signal>Type "negative-case-verified" once both ci.yml AND publish.yml have stayed silent on the master push, OR describe what fired so the bug can be diagnosed before continuing.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Positive-case verification — webpack.config.cjs whitespace edit (D-1a, CI-01, success criteria #2 + #5 + #6)</name>
  <files>webpack.config.cjs</files>
  <read_first>
    - webpack.config.cjs — confirm the file exists at repo root and is the file used by `npm run build`. (It is; package.json scripts.build = `webpack --mode production` resolves to `webpack.config.cjs` by default.) Inspect the last few lines to identify a safe whitespace-edit target.
    - .planning/phases/06-workflow-scaffold-and-gates/06-CONTEXT.md D-1a — explicitly mandates a code-side companion test for the positive case. Without it, P6 only proves the skip path; the run path stays untested until P7's first real publish.
    - .planning/phases/06-workflow-scaffold-and-gates/06-RESEARCH.md Gap 6 behavior matrix — confirms a code-touching commit (path NOT in paths-ignore) RUNS publish.yml.
    - .github/workflows/publish.yml on the master tip — confirm the file is what Plan 06-02 produced and is committed to master. If still only on milestone1.1, merge before this test.
    - package.json — version field MUST be `1.0.7` so the dry-run echo's expected output is `would publish v1.0.8`.
  </read_first>
  <what-built>
    Plan 06-02's publish.yml triggers on push-to-master + workflow_dispatch with paths-ignore + sequential gates + dry-run echo. This task verifies that a code-side commit (file NOT in paths-ignore) actually triggers the workflow on master, runs all gates green, and the dry-run echo emits the expected `would publish v1.0.8` line.
  </what-built>
  <how-to-verify>
    PRECONDITION: Negative-case test (Task 1) has completed; master is otherwise idle.

    Step 1 — Prepare a trivial webpack.config.cjs edit on a feature branch:
      a. From master tip: `git checkout master && git pull && git checkout -b chore/p6-verify-positive-case`.
      b. Make a single trivial whitespace edit to `webpack.config.cjs`. Recommended: add a trailing newline at EOF if absent, OR add a single trailing space at the end of an existing comment line. DO NOT modify any executable code (no rule changes, no plugin tweaks). DO NOT add a `// test commit` comment (per CLAUDE.md "no comments by default" rule). DO NOT touch any other file.
      c. `git status` MUST show only `modified: webpack.config.cjs`.
      d. `npm run build` locally MUST still succeed before pushing — proves the whitespace edit didn't break webpack.

    Step 2 — Push and PR:
      a. `git add webpack.config.cjs && git commit -m "chore(p6): positive-case verify — webpack config whitespace edit"`.
      b. `git push -u origin chore/p6-verify-positive-case`.
      c. Open a PR via `gh pr create` or the UI. ci.yml WILL fire on the PR (PR-only trigger from Plan 06-01). Wait for it to go green.

    Step 3 — Merge to master and observe publish.yml run:
      a. Merge the PR (squash or merge-commit; either works).
      b. Open the GitHub Actions tab. publish.yml SHOULD start a new run within ~10 seconds of the merge.
      c. Watch the run end-to-end. Expected total wall-clock: < 5 minutes (matches existing ci.yml runtime).

    EXPECTED OBSERVATIONS (each step in publish.yml run):
      1. Checkout — green.
      2. Setup Node.js — green; npm cache hit (look for "Cache restored successfully" in the step log; cache key is shared with ci.yml).
      3. Install dependencies — green (npm ci).
      4. Typecheck — green (npm run typecheck).
      5. Unit tests — green (398/398 passing; numbers may have evolved — match the current vitest count).
      6. Build (production) — green (webpack writes to dist/).
      7. Bundle size gate — green (< 250 KB gzipped; current is 147.9 KB).
      8. Verify all manifest assets exist on disk — green (dist exists post-build, images/ and overview.md are committed).
      9. Verify TFX_PAT secret resolves — log line `tfx-pat-present=true` (success criterion #6 confirmed; if `false` appears, TFX_PAT is misnamed or missing in repo secrets).
      10. Probe master branch protection — green (regardless of 200 or 404; the step is designed to handle both). Note the step's $GITHUB_STEP_SUMMARY content — that's where the protection state is captured for Task 3.
      11. Dry-run — compute next version (DOES NOT publish) — green; stdout shows `would publish v1.0.8` (because package.json version is 1.0.7); step output `next-version=v1.0.8`; $GITHUB_STEP_SUMMARY contains the markdown block "Would publish: `v1.0.8`".
      12. ci.yml should NOT fire on this master push (Plan 06-01 removed `push: master`). Confirm via Actions tab — only one workflow run for this commit (publish.yml).

    Step 4 — Capture the result for 06-03-SUMMARY.md:
      a. Note the publish.yml run URL.
      b. Copy the dry-run step's stdout containing `would publish v1.0.8` (verbatim).
      c. Copy the TFX_PAT probe step's stdout containing `tfx-pat-present=true` (verbatim).
      d. Save the link to the run's summary page (where the branch-protection markdown block was rendered) — Task 3 also pulls the body from there.

    FAILURE MODES (each maps to a specific P6 plumbing bug):
      - publish.yml does NOT fire on the master push → trigger plumbing broken. Diagnose: check that `branches: [master]` matches the actual default-branch name (`master`, not `main`); check that the merge produced a `push` event (squash-merge does, force-push does not always); check that `paths-ignore` doesn't accidentally include `webpack.config.cjs` somehow. Fix Plan 06-02 and re-run.
      - publish.yml fires but a gate fails → real regression unrelated to P6 plumbing (the same gates work today on PRs via ci.yml; if they break on master tip, that's a code/config issue not a workflow-scaffold issue). Document and address as a separate quick task; re-run after fix.
      - Dry-run echo prints `would publish v1.0.8` but step output `next-version` is missing → the `$GITHUB_OUTPUT` write is malformed in publish.yml. Fix and re-run.
      - TFX_PAT probe prints `tfx-pat-present=false` → the GitHub repo secret name does not match `TFX_PAT` (case-sensitive). Per D-2 the secret is supposed to exist; investigate Settings → Secrets and variables → Actions and rename if needed. Re-run via `workflow_dispatch` after fixing.
      - ci.yml ALSO fires on the master push → Plan 06-01's edit isn't actually on master. Re-merge Plan 06-01 and re-run this task.
  </how-to-verify>
  <acceptance_criteria>
    - A merge commit lands on master via PR with `git diff --name-only HEAD~1` (or equivalent for squash) showing exactly `webpack.config.cjs`.
    - GitHub Actions tab shows EXACTLY ONE new publish.yml run triggered by this master push (and zero new ci.yml runs for the push side).
    - The publish.yml run completes green on all 10 step names listed in EXPECTED OBSERVATIONS.
    - The "Verify TFX_PAT secret resolves" step's stdout contains the literal substring `tfx-pat-present=true` (success criterion #6 satisfied).
    - The "Dry-run — compute next version (DOES NOT publish)" step's stdout contains the literal substring `would publish v1.0.8` (success criterion #5 satisfied — confirmed against package.json version 1.0.7 + 1 = 1.0.8).
    - The dry-run step output `next-version` resolves to `v1.0.8` (visible if `gh run view <run-id> --json jobs` is queried; or visible in the run's $GITHUB_STEP_SUMMARY markdown block).
    - The "Probe master branch protection" step ran green (didn't itself fail) and wrote a markdown block to $GITHUB_STEP_SUMMARY — this output is the source for Task 3.
    - The Marketplace listing for `TsezariMshvenieradzeTfsAiReviewTask.story-point-calculator` is STILL at v1.0.7 (NOT v1.0.8) — confirms no actual publish occurred (success criterion #5: dry-run echo only).
    - 06-03-SUMMARY.md captures the run URL, the verbatim dry-run echo line, the verbatim TFX_PAT probe line, and the merge commit SHA.
  </acceptance_criteria>
  <resume-signal>Type "positive-case-verified" once publish.yml has finished green with the expected dry-run echo, the TFX_PAT probe printed `true`, and Marketplace is still at v1.0.7. If anything failed, paste the failing step's log so the bug can be diagnosed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Capture branch-protection-probe-result.md as a durable phase artifact (D-5, success criterion #6)</name>
  <files>.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md</files>
  <read_first>
    - .planning/phases/06-workflow-scaffold-and-gates/06-CONTEXT.md decision D-5 — mandates that the probe result is recorded for Phase 7's planner to consume.
    - .planning/phases/06-workflow-scaffold-and-gates/06-RESEARCH.md "Pitfall A2" + "Verified pattern: separate phase-artifact capture for branch-protection result" — explains why a step-output is insufficient and how to capture durably.
    - The publish.yml run from Task 2 — the "Probe master branch protection" step's stdout AND its $GITHUB_STEP_SUMMARY markdown block. Either is the source for the artifact body.
  </read_first>
  <action>
    Capture the master branch-protection state into a durable phase artifact.

    Preferred approach (uses local `gh` CLI; works because the user has admin on the repo):

    ```bash
    gh api repos/tsmshvenieradze/StoryPointExtension/branches/master/protection \
      > /tmp/branch-protection-raw.txt 2>&1 || true
    ```

    The `|| true` ensures a 404 (which exits non-zero) still writes the body for posterity. The redirect captures both stdout and stderr so a 404 body or a 200 JSON object both land in the file.

    Then construct `.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md` with this exact shape:

    ```markdown
    # Master branch-protection probe result

    **Probed:** YYYY-MM-DD (set to the actual date)
    **Repo:** tsmshvenieradze/StoryPointExtension
    **Endpoint:** GET /repos/tsmshvenieradze/StoryPointExtension/branches/master/protection
    **Source:** local `gh api` invocation (Task 3 of Plan 06-03), cross-referenced with publish.yml's "Probe master branch protection" step in run <RUN_URL>.

    ## Result

    **State:** {PROTECTED | NOT PROTECTED}

    ## Implication for Phase 7 (commit-back token)

    {If NOT PROTECTED:} master is unprotected, so Phase 7's `git-auto-commit-action@v6` step can use the default `GITHUB_TOKEN` with `permissions: contents: write` at the publish job level. No PAT or GitHub App needed. This matches the v1.1 milestone working assumption (per `.planning/STATE.md` "v1.1 decisions" and `.planning/research/SUMMARY.md`).

    {If PROTECTED:} master IS branch-protected. Phase 7's commit-back must escalate to a GitHub App or `RELEASE_PAT` with bypass. The default `GITHUB_TOKEN` will be rejected by the protection rule. Phase 7's planner MUST update the milestone plan to add a `RELEASE_PAT` repo secret AND/OR provision a GitHub App with `Contents: write` permission and add it to the bypass list. See `.planning/research/SUMMARY.md` "Branch-protection-aware push" Future Requirement.

    ## Raw API response

    ```json
    {literal contents of /tmp/branch-protection-raw.txt go here, indented under the json fence}
    ```
    ```

    Replace the `{...}` placeholders with actual values:
    - `YYYY-MM-DD` → today (e.g. `2026-05-05`).
    - `<RUN_URL>` → the publish.yml positive-case run URL from Task 2.
    - `{PROTECTED | NOT PROTECTED}` → choose ONE based on whether `gh api` exited 0 (PROTECTED) or non-zero (NOT PROTECTED).
    - The `## Implication for Phase 7` block: keep ONLY the relevant branch (PROTECTED or NOT PROTECTED), DELETE the other branch's text entirely so the artifact reads cleanly.
    - The `Raw API response` fenced block: paste the verbatim contents of `/tmp/branch-protection-raw.txt`. If the response was a 404 with body `{"message":"Branch not protected", ...}`, paste that JSON. If 200, paste the protection-rule JSON object.

    Do NOT redact anything (the protection endpoint returns no secrets — it's metadata about which branches require reviews / status checks / etc.). Do NOT modify the `gh api` output before pasting (preserve formatting, including any leading or trailing newlines from gh's output).

    Fallback if the local `gh api` call fails for any reason (auth issue, missing CLI, etc.): copy the publish.yml positive-case run's "Probe master branch protection" step LOG (visible in the Actions UI when you click the step) into the `Raw API response` block instead. The step is designed to print the same body to its log under either the `Master IS branch-protected:` or `Master is NOT branch-protected:` prefix.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md && grep -q "branch-protection probe result" .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md && grep -E -q "(NOT PROTECTED|^\*\*State:\*\* PROTECTED)" .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md && grep -q "Implication for Phase 7" .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md && grep -q "Raw API response" .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md` exists.
    - File contains the H1 `# Master branch-protection probe result`.
    - File contains a `**State:**` line whose value is either `PROTECTED` or `NOT PROTECTED` (literal one of those two strings, not a placeholder).
    - File contains a `## Implication for Phase 7 (commit-back token)` section with body relevant to the actual State (the irrelevant branch's placeholder is REMOVED, not left in).
    - File contains a `## Raw API response` section with a fenced code block containing actual response body (NOT empty, NOT a placeholder).
    - File contains a reference to the publish.yml positive-case run URL or commit SHA (cross-traceability for future researchers).
    - The State value matches the publish.yml positive-case run's $GITHUB_STEP_SUMMARY content from Task 2 (cross-check; if they disagree, the local probe and the workflow probe disagree, which is itself a finding worth recording).
    - File is committed to git (git status clean after commit).
  </acceptance_criteria>
  <done>
    - branch-protection-probe-result.md exists, is filled in, and is committed.
    - Phase 7 has a durable file to consume that answers the "is master protected?" question.
    - The Implication-for-Phase-7 paragraph guides P7's planner to either keep the default `GITHUB_TOKEN` plan or escalate to App/PAT.
  </done>
</task>

</tasks>

<verification>
Plan-level verification (run after Tasks 1-3 complete):

1. **Negative case observed:** Task 1's resume signal received with "negative-case-verified". The Actions tab confirmed publish.yml stayed silent on the README-only master push and ci.yml stayed silent on the master push side (it fired on the PR, which is correct).

2. **Positive case observed:** Task 2's resume signal received with "positive-case-verified". The publish.yml run completed green; dry-run echoed `would publish v1.0.8`; TFX_PAT probe emitted `true`; Marketplace remained at v1.0.7 (no actual publish).

3. **Branch-protection artifact captured:** Task 3 produced `branch-protection-probe-result.md` with a real State value, real raw response body, and a P7-actionable Implication paragraph.

4. **Cross-check the 6 ROADMAP success criteria:**
   - SC #1 (PR triggers ci.yml only): observed in Tasks 1 and 2 (each PR ran ci.yml; neither PR fired publish.yml).
   - SC #2 (master merge triggers publish.yml only): observed in Task 2's positive case.
   - SC #3 (docs-only commit skipped): observed in Task 1's negative case.
   - SC #4 (gate failure stops before dry-run): NOT directly verified in this plan — the Wave-1 plans' YAML-inspection acceptance criteria (no `continue-on-error: true`, sequential step order) prove this by construction. A live failure injection would consume an extra force-merge for marginal value; deferred to Phase 7 where the FAIL-04 commit-back path needs runtime exercise anyway.
   - SC #5 (dry-run echoes would-be next version, no Marketplace mutation): observed in Task 2.
   - SC #6 (TFX_PAT resolves + branch-protection probe captured): observed in Task 2 (TFX_PAT probe) + Task 3 (branch-protection artifact).
</verification>

<success_criteria>
- README.md whitespace edit merged to master triggered NEITHER ci.yml NOR publish.yml on the master push side (paths-ignore filter works for `**.md`).
- webpack.config.cjs whitespace edit merged to master triggered ONLY publish.yml; all 10 steps green; dry-run echoed `would publish v1.0.8`; TFX_PAT probe printed `true`; Marketplace unchanged at v1.0.7.
- `.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md` exists with a durable record of master's protection state and a P7-actionable Implication paragraph.
- All four tasks committed to git; both verification PRs merged to master.
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflow-scaffold-and-gates/06-03-SUMMARY.md` summarizing:
- Negative-case PR / merge commit SHA, Actions tab observation (no runs fired on master push side).
- Positive-case PR / merge commit SHA, publish.yml run URL, the verbatim dry-run echo line (`would publish v1.0.8`), the verbatim TFX_PAT probe line (`tfx-pat-present=true`), Marketplace post-state (still at v1.0.7).
- Branch-protection probe State value and link to `branch-protection-probe-result.md`.
- Cross-check table mapping each of the 6 ROADMAP success criteria to the observation that proves it (or notes deferral, e.g., SC #4 deferred to P7 by-construction).
</output>
