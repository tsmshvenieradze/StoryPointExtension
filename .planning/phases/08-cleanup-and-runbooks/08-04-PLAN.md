---
phase: 08-cleanup-and-runbooks
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-03"]
files_modified:
  - scripts/publish-cezari.cjs
  - scripts/.archive/publish-cezari.cjs
  - package.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]
must_haves:
  truths:
    - "scripts/publish-cezari.cjs no longer exists at its original path; scripts/.archive/publish-cezari.cjs exists (moved via git mv, history preserved, grep-discoverable) — CLEAN-01"
    - "scripts/.archive/publish-cezari.cjs has an ARCHIVED header comment at the top pointing to publish.yml and OPERATIONS.md"
    - "package.json no longer contains the publish:cezari or publish:public script entries; the package script (tfx extension create ...) is untouched — CLEAN-02"
    - "git grep -F 'publish:cezari' returns 0 hits outside scripts/.archive/ and .planning/ — CLEAN-03"
    - "no npm script was re-added as an 'emergency hatch' — the only manual publish path is the OPERATIONS.md runbook (D-5)"
  artifacts:
    - path: "scripts/.archive/publish-cezari.cjs"
      provides: "the legacy publish helper, frozen reference"
      contains: "ARCHIVED"
    - path: "package.json"
      provides: "scripts block with publish:cezari and publish:public removed"
  key_links:
    - from: "scripts/.archive/publish-cezari.cjs header comment"
      to: ".planning/OPERATIONS.md (the live emergency-publish runbook)"
      via: "ARCHIVED comment pointer"
      pattern: "OPERATIONS.md"
---

<objective>
Retire the legacy manual-publish path (CONTEXT D-5 / CLEAN-01..03): `git mv scripts/publish-cezari.cjs scripts/.archive/publish-cezari.cjs`, add an ARCHIVED header comment, remove the `publish:cezari` and `publish:public` npm scripts from `package.json`, and verify `git grep -F 'publish:cezari'` returns 0 hits outside `scripts/.archive/` and `.planning/`. The canonical `tfx` invocation was already captured into `.planning/OPERATIONS.md` in Plan 01 (DOC-02, capture-before-archive ordering) and Phase 7's v1.0.8 publish + Plan 03's v1.0.9 re-verification have already demonstrated the GH Action works — so deleting the manual escape hatch's npm-script form is safe; the OPERATIONS.md runbook is its documented replacement (NOT a re-added script, NOT a second live publish path).

Purpose: one canonical publish path (the release-branch-triggered GH Action); the emergency fallback is a runbook, not a runnable second script. `.archive/` keeps the working code (cross-platform `spawnSync` fix, `.env.local` parsing, `--public` handling) git-restorably for institutional memory.

Output: `scripts/publish-cezari.cjs` -> `scripts/.archive/publish-cezari.cjs` (with ARCHIVED header), `package.json` with two scripts removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cleanup-and-runbooks/08-CONTEXT.md
@.planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md
@.planning/OPERATIONS.md
@scripts/publish-cezari.cjs
@package.json
</context>

<discretion_decisions>
- **ARCHIVED header-comment wording** (CONTEXT D-5 leaves the exact wording to the planner — use this):
  ```js
  // ARCHIVED — superseded by .github/workflows/publish.yml (the release-branch auto-publish path).
  // Kept for reference only; do NOT re-wire this as an npm script. The canonical manual
  // emergency-publish tfx invocation is documented in .planning/OPERATIONS.md (section 2).
  ```
  Place it as the first lines of the moved file, ABOVE the existing `#!/usr/bin/env node` shebang? — No: a shebang must be line 1 if present. Put the ARCHIVED block immediately AFTER the shebang line (line 2+), before the existing `// scripts/publish-cezari.cjs — Phase 5 ...` comment. (If the executor prefers, it MAY drop the shebang entirely since the file is no longer meant to be executed — either is acceptable; if dropped, the ARCHIVED block becomes line 1.)
</discretion_decisions>

<tasks>

<task type="auto">
  <name>Task 1: git mv publish-cezari.cjs into scripts/.archive/ and add the ARCHIVED header</name>
  <files>scripts/publish-cezari.cjs, scripts/.archive/publish-cezari.cjs</files>
  <read_first>
    - scripts/publish-cezari.cjs (the file being moved — note its current line 1 is `#!/usr/bin/env node`)
    - .planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md (CLEAN-01 row; the "### The cleanup (CLEAN-01/02/03) — order matters" code block; Don't Hand-Roll "Moving a file while keeping git history" row — use `git mv`, not `cp`+`rm`)
    - .planning/OPERATIONS.md (confirm section 2 / the emergency-publish runbook exists — the ARCHIVED comment points to it; this is the capture-before-archive precondition)
  </read_first>
  <action>
1. Confirm `.planning/OPERATIONS.md` exists and contains `## 2. Manual emergency-publish runbook` (the DOC-02 capture from Plan 01). If it does not, STOP — the capture-before-archive ordering (CONTEXT D-1 §2 / DOC-02) is violated; do not proceed.
2. `git mv scripts/publish-cezari.cjs scripts/.archive/publish-cezari.cjs` — this creates `scripts/.archive/` automatically and preserves rename history (`git log --follow`).
3. Edit `scripts/.archive/publish-cezari.cjs`: insert the ARCHIVED header block immediately after the `#!/usr/bin/env node` shebang line (i.e. it becomes lines 2-4), keeping everything else byte-for-byte identical:
   ```js
   #!/usr/bin/env node
   // ARCHIVED — superseded by .github/workflows/publish.yml (the release-branch auto-publish path).
   // Kept for reference only; do NOT re-wire this as an npm script. The canonical manual
   // emergency-publish tfx invocation is documented in .planning/OPERATIONS.md (section 2).
   // scripts/publish-cezari.cjs — Phase 5 canonical publish-to-cezari helper.
   ... (rest of the original file unchanged) ...
   ```
4. Do NOT change any other line of the moved file. Do NOT create `scripts/.archive/.gitkeep` or any other file.
  </action>
  <verify>
    <automated>test -f scripts/.archive/publish-cezari.cjs && ! test -f scripts/publish-cezari.cjs && head -5 scripts/.archive/publish-cezari.cjs | grep -q 'ARCHIVED' && grep -q 'OPERATIONS.md' scripts/.archive/publish-cezari.cjs && git log --follow --oneline -- scripts/.archive/publish-cezari.cjs | head -1 && echo ARCHIVE_MOVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/.archive/publish-cezari.cjs` exists.
    - `scripts/publish-cezari.cjs` does NOT exist.
    - One of the first 5 lines of `scripts/.archive/publish-cezari.cjs` contains `ARCHIVED`.
    - `scripts/.archive/publish-cezari.cjs` contains `OPERATIONS.md` (the pointer in the header).
    - The move was done via `git mv` (rename history preserved — `git log --follow -- scripts/.archive/publish-cezari.cjs` shows the pre-move history).
    - The original file's body (the `spawnSync`, `.env.local` parsing, `--public` handling logic) is byte-for-byte unchanged except for the inserted ARCHIVED header block.
  </acceptance_criteria>
  <done>`publish-cezari.cjs` lives at `scripts/.archive/publish-cezari.cjs` with an ARCHIVED header pointing to `publish.yml` + `OPERATIONS.md`; git history preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove publish:cezari + publish:public from package.json; verify git grep is clean (CLEAN-02, CLEAN-03)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the `scripts` block — lines 12-25; the two keys to delete are `"publish:cezari": "node scripts/publish-cezari.cjs"` and `"publish:public": "node scripts/publish-cezari.cjs --public"`; the `"package": "tfx extension create --manifest-globs vss-extension.json --output-path dist/"` key STAYS — it's used independently)
    - .planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md (CLEAN-02 + CLEAN-03 rows; Pitfall 7 — enumerate all the places `publish:cezari` / `publish-cezari` / `publish:public` could live before declaring CLEAN-03 satisfied)
  </read_first>
  <action>
1. In `package.json`, delete exactly these two lines from the `"scripts"` object:
   - `"publish:cezari": "node scripts/publish-cezari.cjs",`
   - `"publish:public": "node scripts/publish-cezari.cjs --public"`
   Fix the trailing comma so the JSON stays valid (the line before `"publish:cezari"` — currently `"check:size": "node scripts/check-bundle-size.cjs",` — will need its trailing comma removed if `check:size` becomes the last entry in the `scripts` object; verify the resulting JSON parses). Do NOT touch any other key (`clean`, `typecheck`, `test`, `test:watch`, `test:coverage`, `build`, `build:dev`, `dev`, `package`, `check:size` all stay).
2. Confirm the file is valid JSON: `node -e "require('./package.json')"` exits 0.
3. Confirm `npm run` no longer lists `publish:cezari` / `publish:public`: `jq -r '.scripts | keys[]' package.json` includes neither.
4. Run the CLEAN-03 verification (and the broader sanity sweep from Pitfall 7):
   - `git grep -nF 'publish:cezari'` -> every hit must be in `scripts/.archive/` or `.planning/` (the moved file's `// Usage: npm run publish:cezari` header line is in `.archive/` -> allowed; `.planning/**` historical refs -> allowed). NO hit in `package.json`, `README.md`, `.github/`, `src/`.
   - `git grep -nF 'publish-cezari'` (the filename form) -> hits only in `scripts/.archive/` and `.planning/`.
   - `git grep -nF 'publish:public'` -> 0 hits in `package.json`; `.planning/` allowed.
   If any hit appears outside the allowed paths, fix it (it should not — `publish.yml`/`ci.yml` reference the workflow files, not these scripts).
  </action>
  <verify>
    <automated>node -e "require('./package.json')" && ! jq -e '.scripts["publish:cezari"]' package.json >/dev/null && ! jq -e '.scripts["publish:public"]' package.json >/dev/null && jq -e '.scripts["package"]' package.json >/dev/null && [ "$(git grep -lF 'publish:cezari' -- ':!scripts/.archive/' ':!.planning/' | wc -l)" -eq 0 ] && [ "$(git grep -lF 'publish:public' -- 'package.json' | wc -l)" -eq 0 ] && echo CLEANUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` parses as valid JSON (`node -e "require('./package.json')"` exits 0).
    - `jq -e '.scripts["publish:cezari"]' package.json` fails (key absent); `jq -e '.scripts["publish:public"]' package.json` fails (key absent).
    - `jq -e '.scripts["package"]' package.json` succeeds (the `tfx extension create` script is untouched); the other scripts (`clean`, `typecheck`, `test`, `test:watch`, `test:coverage`, `build`, `build:dev`, `dev`, `check:size`) are all still present.
    - `git grep -F 'publish:cezari' -- ':!scripts/.archive/' ':!.planning/'` returns no files (exit 1 / empty).
    - `git grep -F 'publish-cezari' -- ':!scripts/.archive/' ':!.planning/'` returns no files.
    - `git grep -F 'publish:public' -- 'package.json'` returns no files.
    - No new npm script was added (no `publish:*` key reintroduced — D-5).
  </acceptance_criteria>
  <done>`package.json` has `publish:cezari` + `publish:public` removed (and nothing else changed); CLEAN-03's `git grep` is clean; no replacement script added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archived file <-> repo | `scripts/.archive/publish-cezari.cjs` is preserved but inert — it must not become a live publish path again (no npm script wiring); it contains no secrets (it READS `.env.local` at runtime, but no credential is embedded in the file) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering / scope creep | re-introducing a runnable second publish path | mitigate | CLEAN-02 removes the npm scripts; the ARCHIVED header explicitly says "do NOT re-wire this as an npm script"; acceptance criteria forbid any new `publish:*` key. The only documented manual path is the OPERATIONS.md runbook (D-5). |
| T-08-16 | Information Disclosure | the archived `publish-cezari.cjs` | mitigate | The file contains no credentials — it parses `.env.local` (gitignored) at runtime and even refuses to run if `.env.local` is git-tracked. Moving it doesn't change that. The ARCHIVED header adds no secret. |
| T-08-17 | Repudiation | losing the working legacy code | mitigate | `git mv` preserves the file + its history under `scripts/.archive/`; `git log --follow` recovers the full provenance. Not deleted (CONTEXT D-5 rejected outright deletion). |

No `high`-severity unmitigated threats. Low-surface mechanical cleanup; no secrets, no application code.
</threat_model>

<verification>
- `scripts/publish-cezari.cjs` gone; `scripts/.archive/publish-cezari.cjs` present with ARCHIVED header; `git log --follow` shows the rename.
- `package.json` is valid JSON; `publish:cezari` + `publish:public` removed; `package` + the rest of `scripts` untouched.
- `git grep -F 'publish:cezari'` -> hits only in `scripts/.archive/` and `.planning/`.
- `npm ci && npm run typecheck && npm test -- --run && npm run build && npm run check:size` still all pass (the cleanup didn't break the build — `publish-cezari.cjs` was never part of the build graph). [Run this as a sanity check; it should be unaffected.]
</verification>

<success_criteria>
- CLEAN-01: `publish-cezari.cjs` archived to `scripts/.archive/` (preserved, grep-discoverable, ARCHIVED header).
- CLEAN-02: `publish:cezari` + `publish:public` removed from `package.json`.
- CLEAN-03: `git grep -F 'publish:cezari'` returns 0 hits outside `scripts/.archive/` and `.planning/`.
- D-5: no re-added npm script; the GH Action is the canonical publish path; OPERATIONS.md §2 is the documented manual fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-and-runbooks/08-04-SUMMARY.md`.
</output>
