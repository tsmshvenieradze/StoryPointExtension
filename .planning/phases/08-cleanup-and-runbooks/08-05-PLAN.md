---
phase: 08-cleanup-and-runbooks
plan: 05
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "PROJECT.md's 'Validated' section names v1.1 Auto-Publish CI/CD as shipped, with a one-line summary of the workflow + secret + commit-back loop, mirroring the v1.0 entries' shape — DOC-03"
    - "The PROJECT.md v1.1 'Validated' entry reflects the ACTUAL shipped model: 'every promotion (PR master -> release) ships a new patch automatically; master stays fully protected (PR + status-checks + signed-commits, release App on bypass list)' — NOT the original 'every PR merge to master ships' wording"
    - "PROJECT.md's 'Active' section no longer contains the v1.1 placeholder (it has been promoted to Validated); the 'Current Milestone' and 'Goal' wording is corrected to the release-branch model"
    - "REQUIREMENTS.md's stale '32' total is corrected to '38' (Cross-Phase Note #6)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "v1.1 promoted to Validated with the corrected release-branch-model wording"
      contains: "release"
    - path: ".planning/REQUIREMENTS.md"
      provides: "corrected requirement tally"
  key_links:
    - from: ".planning/PROJECT.md Validated section"
      to: "the v1.1 auto-publish surface (publish.yml, TFX_PAT/APP_* secrets, OPERATIONS.md)"
      via: "a one-line summary entry"
      pattern: "v1\\.1"
---

<objective>
Promote v1.1 Auto-Publish CI/CD from "Active" to "Validated" in `.planning/PROJECT.md` (CONTEXT DOC-03), using the CORRECTED milestone wording: the shipped model is "every promotion (PR `master -> release`) ships a new patch automatically; master stays fully protected" — NOT the original ROADMAP "every PR merge to master ships" wording. Also fix the stale "32 -> 38" requirement tally in `.planning/REQUIREMENTS.md` (Cross-Phase Note #6). This runs after Plan 03's re-verification + SC #5 exercise has actually demonstrated the model works in the wild — so the "Validated" claim is truthful. (DOC-03 is the documentation deliverable owned by THIS plan; Plan 03 also carries the DOC-03 tag but only as a milestone-validation dependency marker — see Plan 03's frontmatter note.)

Purpose: PROJECT.md is the single source of milestone status; future readers must see the actual shipped architecture, not the pre-Phase-8 plan.

Output: updated `.planning/PROJECT.md`, updated `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-cleanup-and-runbooks/08-CONTEXT.md
@.planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md
@.planning/OPERATIONS.md
@.planning/phases/08-cleanup-and-runbooks/08-SC5-EXERCISE.md
</context>

<discretion_decisions>
- The v1.1 "Validated" entry must mirror the SHAPE of the existing v1.0 `### Validated` bullets (a `✓ **Title** — one-paragraph summary — Phase N` line). Use phase ref "Phases 6-8".
- It must contain BOTH: (a) a one-line summary of the loop — "GitHub Actions `publish.yml` triggered by a push to the long-lived `release` branch; pre-flight gates (typecheck/tests/build/bundle-size) -> `bump-version.mjs` -> `tfx extension create` + upload-artifact -> `tfx extension publish` to `TsezariMshvenieradzeTfsAiReviewTask` using the `TFX_PAT` secret -> `story-point-release-bot` GitHub App (token via `actions/create-github-app-token`) commits `chore(release): vX.Y.Z [skip ci]` to `release` -> annotated tag -> a `release -> master` back-merge PR a maintainer merges via the Web UI"; and (b) the corrected model statement — "every promotion (PR `master -> release`) ships a new patch automatically; master stays fully protected (require-PR + 2 status checks + signed-commits, `story-point-release-bot` App on the bypass list)". Mention `.planning/OPERATIONS.md` as the ops doc. Mention the shipped versions through Phase 8 (v1.0.8 first auto-publish; v1.0.9 re-verification; v1.0.10 SC #5 recovery — or whatever Plan 03 actually shipped per `08-SC5-EXERCISE.md`).
- Also update PROJECT.md's `## Current Milestone: v1.1 Auto-Publish CI/CD` block's `**Goal:**` line and the "Every PR merge to master ships..." `**Current Milestone**` / target-features wording to the release-branch model (or, if `/gsd-complete-milestone` will rewrite "Current Milestone" entirely later, at minimum correct the `Goal` line and add the Validated entry — note in the SUMMARY which approach was taken).
</discretion_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Promote v1.1 to Validated in PROJECT.md with the corrected release-branch-model wording (DOC-03)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the `### Validated` section — mirror the v1.0 bullets' shape; the `### Active` section — the v1.1 placeholder to remove; the `## Current Milestone: v1.1` block and the `**Goal:**` line — the "Every PR merge to master ships" wording to correct)
    - .planning/phases/08-cleanup-and-runbooks/08-CONTEXT.md (the "Milestone-goal wording change (for DOC-03)" paragraph in `<domain>` — the canonical corrected phrasing: "Every promotion (PR master -> release) ships a new patch version automatically; master stays fully protected")
    - .planning/phases/08-cleanup-and-runbooks/08-SC5-EXERCISE.md (the actual shipped versions through Phase 8 — cite them accurately in the Validated entry)
    - .planning/OPERATIONS.md (referenced by the Validated entry as the ops doc)
    - .planning/phases/08-cleanup-and-runbooks/08-RESEARCH.md (DOC-03 row in `## Phase Requirements`)
  </read_first>
  <action>
1. In `.planning/PROJECT.md`'s `### Validated` section, ADD a new `✓` bullet (after the existing Phase 5 / Quick-task bullets) for the v1.1 milestone, mirroring the existing bullets' shape. It MUST include the one-line loop summary and the corrected model statement and the OPERATIONS.md reference and the shipped versions — see the `<discretion_decisions>` block above for the exact required content. Example skeleton (fill with the real shipped versions from `08-SC5-EXERCISE.md`):
   ```markdown
   - ✓ **v1.1 Auto-Publish CI/CD (CI/GATE/BUMP/PUBLISH/TAG/FAIL/CLEAN/DOC)** — GitHub Actions `publish.yml` triggered by a push to the long-lived `release` branch; pre-flight gates (typecheck -> `vitest run` -> webpack production build -> bundle ≤ 250 KB gzipped) -> `scripts/bump-version.mjs` (atomic two-file patch bump, in-memory) -> `tfx extension create` + `actions/upload-artifact` (`vsix-X.Y.Z`, 90-day) -> `tfx extension publish` to publisher `TsezariMshvenieradzeTfsAiReviewTask` using the `TFX_PAT` repo secret -> `story-point-release-bot` GitHub App (installation token via `actions/create-github-app-token@v2`) commits `chore(release): vX.Y.Z [skip ci]` to `release` -> annotated git tag `vX.Y.Z` (best-effort, idempotent) -> a `release -> master` back-merge PR a maintainer merges via the GitHub Web UI (verified-signature merge commit satisfies master's signed-commits rule). **Shipped model: every promotion (PR `master -> release`) ships a new patch automatically; master stays fully protected (require-PR + 2 status checks + signed-commits, with the `story-point-release-bot` App on the bypass list).** Legacy `publish:cezari` / `publish:public` npm scripts removed; `publish-cezari.cjs` archived to `scripts/.archive/`; operations runbook (PAT rotation, emergency-publish, release-branch model, ruleset config, partial-failure recovery, Option B reversibility) at `.planning/OPERATIONS.md`. First auto-publish v1.0.8 (2026-05-11); release-branch model re-verified shipping v1.0.9; SC #5 broken-PAT reversibility verified in the wild (recovery shipped v1.0.10). — Phases 6-8
   ```
   (Adjust the versions and any details to match `08-SC5-EXERCISE.md` exactly.)
2. In `.planning/PROJECT.md`'s `### Active` section, REMOVE the "v1.1 Auto-Publish CI/CD requirements will be enumerated in ..." placeholder block (it's now Validated). If the section would be empty, leave a brief "(none — v1.2+ themes are in 'Next Milestone Goals')" note or remove the empty heading per the file's style.
3. Update the `## Current Milestone: v1.1 Auto-Publish CI/CD` block's `**Goal:**` line from "Every PR merge to master ships a new patch version of the extension to Marketplace automatically, with no manual steps." to "Every promotion (PR `master -> release`) ships a new patch version of the extension to Marketplace automatically; master stays fully protected (no automatic publish on a plain master merge)." Also correct the "**Current State**"/header "Every PR merge to master ships..." phrasing wherever it appears in PROJECT.md to the release-branch model. (If a later `/gsd-complete-milestone` will rewrite the "Current Milestone" block entirely, you MAY leave that block mostly as-is but you MUST still correct the `Goal` line and add the Validated entry — note in the SUMMARY which you did.)
4. Update PROJECT.md's `*Last updated: ...*` footer line to today's date with a one-line note about the v1.1 promotion.
  </action>
  <verify>
    <automated>grep -q 'v1.1' .planning/PROJECT.md && grep -qi 'release' .planning/PROJECT.md && grep -qi 'master stays fully protected\|stays fully protected' .planning/PROJECT.md && grep -q 'OPERATIONS.md' .planning/PROJECT.md && ! grep -q 'Every PR merge to master ships a new patch version of the extension to Marketplace automatically, with no manual steps' .planning/PROJECT.md && echo PROJECT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md`'s `### Validated` section contains a `✓` bullet for v1.1 that: mentions `publish.yml`, the `release` branch trigger, the pre-flight gates, `bump-version.mjs`, `tfx extension publish`, the `TFX_PAT` secret, the `story-point-release-bot` GitHub App + `actions/create-github-app-token`, the `[skip ci]` commit-back, the annotated tag, the `release -> master` back-merge PR, and `.planning/OPERATIONS.md`.
    - That bullet contains the phrase "master stays fully protected" (the corrected model) and "every promotion" / "`master -> release`".
    - It cites the shipped versions consistent with `08-SC5-EXERCISE.md`.
    - The `### Active` section no longer contains the v1.1 enumeration placeholder.
    - The `## Current Milestone` block's `**Goal:**` line no longer says "Every PR merge to master ships ..." — it says the release-branch-model wording.
    - The footer `*Last updated: ...*` line is updated to today's date with a v1.1-promotion note.
  </acceptance_criteria>
  <done>PROJECT.md "Validated" names v1.1 as shipped with the one-line loop summary AND the corrected "master stays fully protected / promotion ships" model; "Active" placeholder removed; Goal line corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the stale 32 -> 38 requirement tally in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "**Total v1.1 requirements: 38**" line — already corrected once on 2026-05-05 per the line's own note; AND check the top-of-file or any other "32" reference; the ROADMAP Cross-Phase Note #6 flags this)
    - .planning/ROADMAP.md ("Cross-Phase Notes for plan-phase" #6 — the "Stale REQ tally" note)
  </read_first>
  <action>
Scan `.planning/REQUIREMENTS.md` for any remaining "32" that should be "38" (the enumeration is 8 CI + 7 GATE + 5 BUMP + 5 PUBLISH + 4 TAG + 3 FAIL + 3 CLEAN + 3 DOC = 38). The line `**Total v1.1 requirements: 38** (...) — corrected from earlier "32" tally on 2026-05-05 by gsd-roadmapper ...` is already correct; verify it. If any OTHER place in the file (an intro line, a "Total v1.1 requirements: 32" elsewhere) still says 32, correct it to 38 and add a brief inline note. If there is genuinely nothing left to fix (the only "32" is inside the historical correction note "corrected from earlier 32"), make NO change to this file and record that in the SUMMARY. Do NOT touch the per-requirement list or the traceability table — those are already 38/38.
  </action>
  <verify>
    <automated>grep -q '38' .planning/REQUIREMENTS.md && [ "$(grep -oE '\b32\b' .planning/REQUIREMENTS.md | wc -l)" -le 1 ] && echo REQ_TALLY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` does not present "32" as the live total anywhere (the only permissible "32" is inside the historical "corrected from earlier 32" note).
    - The live total reads `38`.
    - The per-requirement list and traceability table are unchanged.
  </acceptance_criteria>
  <done>REQUIREMENTS.md's tally is consistent at 38; no stray "live 32" remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> readers | PROJECT.md / REQUIREMENTS.md are committed to the public repo — they must not embed secret values; they reference secret *names* (`TFX_PAT`, `APP_ID`, `APP_PRIVATE_KEY`) only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Information Disclosure | PROJECT.md Validated entry | mitigate | The entry names secrets by name only (`TFX_PAT`, `APP_PRIVATE_KEY`); no token, no key material. Acceptance criteria don't require any credential. |
| T-08-19 | Repudiation / integrity | inaccurate "Validated" claim | mitigate | This plan runs AFTER Plan 03's re-verification + SC #5 exercise have actually demonstrated the model in the wild (`depends_on: ["08-03"]`); the Validated entry cites the actual shipped versions from `08-SC5-EXERCISE.md` — the claim is evidence-backed, not aspirational. |

No `high`-severity unmitigated threats. Docs-only plan; no application code, no secrets.
</threat_model>

<verification>
- `.planning/PROJECT.md` "Validated" has the v1.1 entry with the loop summary + the corrected "master stays fully protected / promotion ships" model + `.planning/OPERATIONS.md` reference + the shipped versions.
- `.planning/PROJECT.md` "Active" no longer has the v1.1 placeholder; the `Goal` line is corrected.
- `.planning/REQUIREMENTS.md` total is consistently `38`.
- Both files are still well-formed markdown (headings intact, no broken tables).
</verification>

<success_criteria>
- DOC-03: PROJECT.md "Validated" names v1.1 as shipped with a one-line workflow+secret+commit-back-loop summary, AND reflects the ACTUAL shipped model ("every promotion PR master -> release ships a patch; master stays fully protected") — not the original "every PR merge to master ships" wording.
- The stale REQUIREMENTS.md "32" tally is reconciled to 38 (Cross-Phase Note #6) — or confirmed already-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-and-runbooks/08-05-SUMMARY.md`.
</output>
</content>
</invoke>
