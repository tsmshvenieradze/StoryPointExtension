---
phase: 07-bump-publish-tag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bump-version.mjs
  - tests/scripts/bump-version.test.mjs
  - vitest.config.ts
  - .github/workflows/publish.yml
autonomous: true
requirements:
  - BUMP-01
  - BUMP-02
  - BUMP-03
  - BUMP-04
  - BUMP-05
  - PUBLISH-01
  - PUBLISH-02
  - PUBLISH-03
  - PUBLISH-04
  - PUBLISH-05
  - TAG-01
  - TAG-02
  - TAG-03
  - TAG-04
must_haves:
  truths:
    - "scripts/bump-version.mjs reads package.json + vss-extension.json, picks the higher version, writes both at max+1 (BUMP-01, BUMP-02, D-1)"
    - "scripts/bump-version.mjs is in-memory only — it mutates the two JSON files but does NOT git-commit (BUMP-04, Option B)"
    - "scripts/bump-version.mjs writes `next-version=v<X.Y.Z>` AND `next-version-bare=<X.Y.Z>` to $GITHUB_OUTPUT (D-4 + ROADMAP SC #6 artifact-name nuance)"
    - "scripts/bump-version.mjs emits `::warning::Drift reconciled: ...` to stderr AND a `## Drift reconciled` block to $GITHUB_STEP_SUMMARY when pkg.version != manifest.version (D-2)"
    - "scripts/bump-version.mjs writes a `## Bump` block to $GITHUB_STEP_SUMMARY in every successful run (D-8)"
    - "tests/scripts/bump-version.test.mjs has exactly two vitest tests: happy path (both at 1.0.7 → both at 1.0.8 + GITHUB_OUTPUT line + no drift annotation) AND drift case (pkg=1.0.7, manifest=1.0.6 → both at 1.0.8 + drift annotation in stderr + drift block in summary file) (BUMP-05, D-3)"
    - "vitest.config.ts include glob picks up tests/scripts/bump-version.test.mjs (so `npm test` runs it)"
    - "publish.yml's actions/checkout step has `with: { fetch-depth: 0 }` so the tag step can see prior tags (research/STACK.md)"
    - "publish.yml's publish job has `permissions: contents: write` at the JOB level (top-level stays at `contents: read`) — D-12, CI-05, branch-protection probe = NOT PROTECTED"
    - "publish.yml job name no longer contains `(dry-run in P6)`"
    - "publish.yml does NOT contain the `Dry-run — compute next version` step or any line matching `would publish v` (it is replaced)"
    - "publish.yml contains a Bump version step with `id: bump` running `node scripts/bump-version.mjs` after the branch-protection probe and before tfx create (BUMP-03)"
    - "publish.yml contains a Package vsix step running `npx tfx extension create --manifest-globs vss-extension.json --output-path dist/` and writing a `## Package` block to $GITHUB_STEP_SUMMARY (PUBLISH-01, D-8)"
    - "publish.yml contains an actions/upload-artifact@v4 step with `name: vsix-${{ steps.bump.outputs.next-version-bare }}`, `path: dist/*.vsix`, `retention-days: 90`, `if-no-files-found: error` (PUBLISH-02, ROADMAP SC #6)"
    - "publish.yml contains a Publish to Marketplace step invoking `npx tfx extension publish --vsix dist/*.vsix --auth-type pat --token \"$TFX_PAT\" --no-prompt --no-wait-validation` with TFX_PAT in env: block (NOT inline interpolation), and writing `## Publish` + listing URL to $GITHUB_STEP_SUMMARY (PUBLISH-03, PUBLISH-04, PUBLISH-05, D-8)"
    - "publish.yml contains a stefanzweifel/git-auto-commit-action@v6 step with `commit_message: chore(release): ${{ steps.bump.outputs.next-version }} [skip ci]`, bot identity (`github-actions[bot]` + `41898282+github-actions[bot]@users.noreply.github.com`), `file_pattern: package.json vss-extension.json`, NO built-in tagging (TAG-01, TAG-02)"
    - "publish.yml contains a Surface commit-back result step writing `## Commit-back` block with `${{ steps.commit.outputs.commit_hash }}` to $GITHUB_STEP_SUMMARY (D-8)"
    - "publish.yml contains a Tag release step with `continue-on-error: true` that creates an annotated tag `v${{ steps.bump.outputs.next-version-bare }}` (or equivalent), idempotently skips if the tag exists locally OR on origin, and writes either `🏷️ Tagged ...` or `Tag ... already exists — idempotent skip` to $GITHUB_STEP_SUMMARY (TAG-03, TAG-04, D-10 states a + b)"
    - "publish.yml contains a Surface tag failure follow-up step gated on `if: failure() && steps.<tag-step-id>.outcome == 'failure'` writing the `⚠️ Tag step failed ...` line + recovery hint to $GITHUB_STEP_SUMMARY (D-10 state c)"
    - "publish.yml has `continue-on-error: true` on EXACTLY ONE step (the tag step) — no other step has it (FAIL-01)"
    - "publish.yml's commit_message contains the literal token `[skip ci]` (anti-loop guard #2 of triple defense per D-12)"
  artifacts:
    - path: "scripts/bump-version.mjs"
      provides: "ESM bump script: max-version reconciliation + atomic two-file write + GITHUB_OUTPUT + drift surfacing"
      contains: "import { readFileSync, writeFileSync, appendFileSync } from \"node:fs\""
      contains_2: "import.meta.url"
      contains_3: "const LOG_PREFIX = \"[bump-version]\""
      contains_4: "process.env.GITHUB_OUTPUT"
      contains_5: "process.env.GITHUB_STEP_SUMMARY"
      contains_6: "next-version=v"
      contains_7: "next-version-bare="
      contains_8: "::warning::Drift reconciled"
      contains_9: "## Drift reconciled"
      contains_10: "## Bump"
      contains_11: "JSON.stringify(pkg, null, 2)"
      contains_12: "JSON.stringify(manifest, null, 2)"
      does_not_contain: "require("
      does_not_contain_note: "ESM only — no CommonJS require"
      does_not_contain_2: "child_process"
      does_not_contain_3: "git "
    - path: "tests/scripts/bump-version.test.mjs"
      provides: "Two vitest cases (happy + drift) per BUMP-05 + D-3"
      contains: "import { describe, it, expect, beforeEach, afterEach } from 'vitest'"
      contains_2: "spawnSync"
      contains_3: "mkdtempSync"
      contains_4: "GITHUB_OUTPUT"
      contains_5: "GITHUB_STEP_SUMMARY"
      contains_6: "happy"
      contains_7: "drift"
      contains_8: "::warning::Drift reconciled"
      contains_9: "next-version=v1.0.8"
    - path: "vitest.config.ts"
      provides: "Vitest config picks up tests/scripts/bump-version.test.mjs"
      contains: "tests/**/*.test.mjs"
    - path: ".github/workflows/publish.yml"
      provides: "Real publish workflow — first run on merge ships v1.0.8"
      contains: "fetch-depth: 0"
      contains_2: "permissions:"
      contains_3: "contents: write"
      contains_4: "id: bump"
      contains_5: "node scripts/bump-version.mjs"
      contains_6: "npx tfx extension create --manifest-globs vss-extension.json --output-path dist/"
      contains_7: "actions/upload-artifact@v4"
      contains_8: "vsix-${{ steps.bump.outputs.next-version-bare }}"
      contains_9: "retention-days: 90"
      contains_10: "if-no-files-found: error"
      contains_11: "npx tfx extension publish --vsix dist/*.vsix --auth-type pat --token \"$TFX_PAT\" --no-prompt --no-wait-validation"
      contains_12: "stefanzweifel/git-auto-commit-action@v6"
      contains_13: "[skip ci]"
      contains_14: "github-actions[bot]"
      contains_15: "41898282+github-actions[bot]@users.noreply.github.com"
      contains_16: "file_pattern: package.json vss-extension.json"
      contains_17: "continue-on-error: true"
      contains_18: "git tag -a"
      contains_19: "git push origin"
      contains_20: "git ls-remote --tags origin"
      contains_21: "## Bump"
      contains_22: "## Package"
      contains_23: "## Publish"
      contains_24: "## Commit-back"
      contains_25: "🏷️"
      does_not_contain: "Dry-run — compute next version"
      does_not_contain_2: "would publish v"
      does_not_contain_3: "(dry-run in P6)"
  key_links:
    - from: "scripts/bump-version.mjs"
      to: "publish.yml Bump version step"
      via: "process.env.GITHUB_OUTPUT (next-version + next-version-bare)"
      pattern: "publish.yml step `id: bump` runs `node scripts/bump-version.mjs`; downstream steps reference `${{ steps.bump.outputs.next-version }}` (with v) and `${{ steps.bump.outputs.next-version-bare }}` (no v)"
    - from: "publish.yml Package vsix step"
      to: "publish.yml Publish step + Upload artifact step"
      via: "dist/*.vsix glob"
      pattern: "tfx extension create writes to dist/; upload-artifact and tfx publish both reference `dist/*.vsix`"
    - from: "publish.yml Commit version bump step"
      to: "master HEAD"
      via: "stefanzweifel/git-auto-commit-action@v6 with default GITHUB_TOKEN"
      pattern: "action's commit_message contains `[skip ci]`; permissions block gives contents: write at job level; master is NOT PROTECTED per 06-03 probe artifact"
    - from: "publish.yml Tag release step"
      to: "origin tags"
      via: "git tag -a + git push origin"
      pattern: "step is continue-on-error: true; checks `git rev-parse $V` AND `git ls-remote --tags origin $V` before creating; follow-up step gated on `if: failure() && steps.<id>.outcome == 'failure'` writes failure summary"
    - from: "publish.yml every load-bearing step"
      to: "$GITHUB_STEP_SUMMARY"
      via: "appendFileSync (script) or `{ ... } >> $GITHUB_STEP_SUMMARY` (shell)"
      pattern: "five sections: ## Bump, ## Package, ## Publish, ## Commit-back, ## Tag (+ conditional ## Drift reconciled). Upload-artifact uses default action output, no manual write needed (D-8)."
---

<objective>
Atomic Phase 7 implementation. ONE PR delivers the bump script, its two vitest tests, the vitest-config glob extension, and the publish.yml swap from dry-run to real publish/commit/tag tail. The merge of THIS PR IS the first organic auto-publish — v1.0.7 → v1.0.8 ships when the PR lands on master (D-5, first-run-organic).

Purpose: Land all 14 Phase 7 requirements (BUMP-01..05, PUBLISH-01..05, TAG-01..04) in a single coherent diff. No staged feature flag, no `workflow_dispatch`-only gate, no separate verification merge — Option B reversibility (D-9) is the safety net.

Output: 3 new files (`scripts/bump-version.mjs`, `tests/scripts/bump-version.test.mjs`, no new file for the verification artifact — that's 07-02), 2 modified files (`vitest.config.ts`, `.github/workflows/publish.yml`).

Verification of the live publish chain happens in 07-02 (post-merge observation; no code there).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-bump-publish-tag/07-CONTEXT.md
@.planning/phases/07-bump-publish-tag/07-PATTERNS.md
@.planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.github/workflows/publish.yml
@scripts/publish-cezari.cjs
@scripts/check-bundle-size.cjs
@package.json
@vss-extension.json
@vitest.config.ts
@tests/audit/parse.test.ts

<interfaces>
<!-- Contract surfaces the executor must honor verbatim. Pulled from CONTEXT D-1..D-13 + 07-PATTERNS.md. -->

scripts/bump-version.mjs PUBLIC CONTRACT:
- stdin: none
- argv: none (NOT a CLI tool — single-purpose CI script)
- env in:  process.env.GITHUB_OUTPUT (REQUIRED — abort if unset, exit 4)
            process.env.GITHUB_STEP_SUMMARY (OPTIONAL — silently skip summary writes if unset)
- env out: $GITHUB_OUTPUT gets two appended lines: `next-version=v<X.Y.Z>\n` AND `next-version-bare=<X.Y.Z>\n`
- files in: <repo>/package.json, <repo>/vss-extension.json (both read with utf8)
- files out: same two files rewritten with JSON.stringify(obj, null, 2) + "\n" (preserve 2-space indent + trailing newline)
- stdout: `[bump-version] Bumped to v<X.Y.Z> (from <current>)`
- stderr: `::warning::Drift reconciled: pkg=<orig-pkg>, manifest=<orig-manifest> → bumped to <X.Y.Z>` (only when drifted)
- step-summary append on success: `## Bump\nBumped to \`v<X.Y.Z>\` (from \`<current>\`)\n`
- step-summary append on drift: additional `## Drift reconciled\n- \`package.json\`: <orig-pkg>\n- \`vss-extension.json\`: <orig-manifest>\n- Bumped both to: \`<X.Y.Z>\`\n`
- exit codes: 0 success, 2 missing input file, 3 malformed JSON, 4 GITHUB_OUTPUT unset

publish.yml DOWNSTREAM REFERENCES (after the swap):
- ${{ steps.bump.outputs.next-version }} → `v1.0.8`  (used by: tag step, commit message)
- ${{ steps.bump.outputs.next-version-bare }} → `1.0.8`  (used by: upload-artifact `name`, publish summary line)
- ${{ steps.commit.outputs.commit_hash }} → bump-commit SHA (used by: commit-back summary line)

publish.yml STEP ORDER (after the swap; lines 1-108 carry over byte-identical from Phase 6 except checkout fetch-depth and job name/permissions):
  1. Checkout (existing — ADD `with: { fetch-depth: 0 }`)
  2. Setup Node.js (existing — verbatim)
  3. Install dependencies (existing — verbatim)
  4. Typecheck (existing — verbatim)
  5. Unit tests (existing — verbatim)
  6. Build (production) (existing — verbatim)
  7. Bundle size gate (existing — verbatim)
  8. Verify all manifest assets exist on disk (existing — verbatim)
  9. Verify TFX_PAT secret resolves (existing — verbatim)
  10. Probe master branch protection (existing — verbatim per D-9)
  --- DELETE: Dry-run — compute next version (lines 110-123) ---
  11. Bump version (in-memory only) — NEW (id: bump, runs scripts/bump-version.mjs)
  12. Package vsix — NEW (tfx extension create + ## Package summary)
  13. Upload .vsix artifact — NEW (actions/upload-artifact@v4)
  14. Publish to Marketplace — NEW (tfx extension publish + ## Publish summary)
  15. Commit version bump — NEW (id: commit, git-auto-commit-action@v6)
  16. Surface commit-back result — NEW (## Commit-back summary)
  17. Tag release (best-effort, idempotent) — NEW (id: tag, continue-on-error: true)
  18. Surface tag failure — NEW (if: failure() && steps.tag.outcome == 'failure')

vitest.config.ts CHANGE:
  include: ['tests/**/*.test.ts', 'tests/**/*.test.mjs']
  (extend the array; do NOT replace it)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scripts/bump-version.mjs + 2 vitest tests + extend vitest config</name>
  <files>scripts/bump-version.mjs, tests/scripts/bump-version.test.mjs, vitest.config.ts</files>
  <read_first>
    - .planning/phases/07-bump-publish-tag/07-CONTEXT.md (D-1, D-2, D-3, D-4 — bump script semantics)
    - .planning/phases/07-bump-publish-tag/07-PATTERNS.md (sections "scripts/bump-version.mjs (NEW — utility, file-I/O)" through "scripts/bump-version.test.mjs (NEW — vitest, Node-only)" — full canonical templates with verbatim code)
    - scripts/check-bundle-size.cjs (header pattern, repo-root resolution, exit-code convention, LOG_PREFIX)
    - scripts/publish-cezari.cjs (process.env.<NAME> abort pattern at lines 49-53, spawnSync `shell: process.platform === "win32"` at lines 27-31 for cross-platform test invocation)
    - tests/audit/parse.test.ts (vitest describe/it shape; the project's existing convention)
    - package.json (`engines.node: ">=20.10.0"`, current version `1.0.7`)
    - vss-extension.json (current version `1.0.7`, 2-space indent + trailing `\n`)
    - vitest.config.ts (current `include: ['tests/**/*.test.ts']` — must be extended, not replaced)
  </read_first>
  <behavior>
    - scripts/bump-version.mjs:
      - Header comment block matches scripts/check-bundle-size.cjs shape (one-line description, paragraph behavior summary, "Usage" + "Usage in CI" sections, cite BUMP-01..05 + D-1/D-2/D-4).
      - Shebang `#!/usr/bin/env node` (line 1).
      - ESM imports: `import { readFileSync, writeFileSync, appendFileSync } from "node:fs"; import { fileURLToPath } from "node:url"; import path from "node:path";` — NO `require`, NO external deps.
      - `const __dirname = path.dirname(fileURLToPath(import.meta.url));` then `const REPO_ROOT = path.resolve(__dirname, "..");`.
      - `const LOG_PREFIX = "[bump-version]";`.
      - Read PKG_PATH = `<repo>/package.json` and MANIFEST_PATH = `<repo>/vss-extension.json` via `JSON.parse(readFileSync(..., "utf8"))`.
      - Capture pkg_orig + manifest_orig BEFORE mutation (used for drift line).
      - `drifted = pkg.version !== manifest.version`.
      - `current = drifted ? semverMax(pkg.version, manifest.version) : pkg.version;`.
      - Inline 5-line `semverMax(a, b)` helper that splits on `.` and compares numeric tuples (3 parts). Do NOT add the `semver` npm package.
      - Compute `next` by parsing `current` as `[maj, min, pat] = current.split(".").map(Number)` and writing `${maj}.${min}.${pat + 1}`.
      - Mutate `pkg.version = next` and `manifest.version = next`.
      - Write both files via `writeFileSync(PATH, JSON.stringify(obj, null, 2) + "\n")` — 2-space indent + trailing newline preserved.
      - Read `process.env.GITHUB_OUTPUT`. If unset: `console.error(`${LOG_PREFIX} ABORT: GITHUB_OUTPUT env var unset.`); process.exit(4);`.
      - Append BOTH lines to GITHUB_OUTPUT: `appendFileSync(ghOutput, \`next-version=v${next}\n\`);` AND `appendFileSync(ghOutput, \`next-version-bare=${next}\n\`);`.
      - If `drifted`:
        - `console.error(\`::warning::Drift reconciled: pkg=${pkg_orig}, manifest=${manifest_orig} → bumped to ${next}\`);` (NO `[bump-version]` prefix on this line — would corrupt the GitHub annotation parser).
        - If `process.env.GITHUB_STEP_SUMMARY` is set, `appendFileSync` a `## Drift reconciled\n- \`package.json\`: ${pkg_orig}\n- \`vss-extension.json\`: ${manifest_orig}\n- Bumped both to: \`${next}\`\n` block.
      - If `process.env.GITHUB_STEP_SUMMARY` is set, append `## Bump\nBumped to \`v${next}\` (from \`${current}\`)\n` to the summary file. (Append the `## Bump` block AFTER the drift block when drifted, OR alone when not.)
      - Final stdout: `console.log(\`${LOG_PREFIX} Bumped to v${next} (from ${current})\`);`.
      - Exit 0 implicitly.
      - Belt-and-suspenders aborts (NOT exercised by tests, exist for safety): exit 2 if either input file is missing, exit 3 if either input file is malformed JSON. Implement these as try/catch around `readFileSync` and `JSON.parse` with the bracketed `[bump-version] ABORT: ...` log prefix.

    - tests/scripts/bump-version.test.mjs:
      - Imports: `import { describe, it, expect, beforeEach, afterEach } from 'vitest';`, plus `spawnSync` from `node:child_process`, `mkdtempSync` + `writeFileSync` + `readFileSync` + `cpSync` + `rmSync` from `node:fs`, `tmpdir` from `node:os`, `path` from `node:path`, `fileURLToPath` from `node:url`.
      - `describe('bump-version.mjs (BUMP-05, D-3)', () => { ... })` block with two `it(...)` cases (NOT `it.each` — assertions diverge).
      - Helper inside the describe block:
        - `beforeEach`: mkdtempSync to a unique temp dir; `cpSync` from the real repo's `scripts/bump-version.mjs` into `<tempdir>/scripts/bump-version.mjs`; allocate `outputPath` = `<tempdir>/gh-output.txt` and `summaryPath` = `<tempdir>/gh-summary.md`; touch both as empty files. Store paths on a closure-scoped `ctx` object.
        - `afterEach`: `rmSync(tempDir, { recursive: true, force: true })`.
        - `setupRepo({ pkg, manifest })` writes the two JSON files into `<tempdir>` at root with `{ "name": "fixture", "version": pkg }` (pkg.json — keep minimal but include `"name"`) and `{ "version": manifest }` (manifest — minimal). Use `JSON.stringify(obj, null, 2) + "\n"` for byte-parity with the real files.
        - `runBump()` returns `spawnSync("node", ["scripts/bump-version.mjs"], { cwd: tempDir, env: { ...process.env, GITHUB_OUTPUT: outputPath, GITHUB_STEP_SUMMARY: summaryPath }, encoding: "utf8", shell: process.platform === "win32" });`.
        - `readPkg()` / `readManifest()` parse the temp dir's files.
      - Test 1 — `it('happy path: both at 1.0.7 → both at 1.0.8 + GITHUB_OUTPUT lines + no drift annotation', ...)`:
        - `setupRepo({ pkg: "1.0.7", manifest: "1.0.7" });`
        - `const r = runBump();`
        - `expect(r.status).toBe(0);`
        - `expect(readPkg().version).toBe("1.0.8");`
        - `expect(readManifest().version).toBe("1.0.8");`
        - `const out = readFileSync(outputPath, "utf8");`
        - `expect(out).toContain("next-version=v1.0.8");`
        - `expect(out).toContain("next-version-bare=1.0.8");`
        - `expect(r.stderr).not.toContain("::warning::");` (no drift annotation)
        - `expect(readFileSync(summaryPath, "utf8")).not.toContain("## Drift reconciled");`
        - `expect(readFileSync(summaryPath, "utf8")).toContain("## Bump");`
      - Test 2 — `it('drift: pkg=1.0.7, manifest=1.0.6 → both at 1.0.8 (max+1) + drift annotation', ...)`:
        - `setupRepo({ pkg: "1.0.7", manifest: "1.0.6" });`
        - `const r = runBump();`
        - `expect(r.status).toBe(0);`
        - `expect(readPkg().version).toBe("1.0.8");`
        - `expect(readManifest().version).toBe("1.0.8");`
        - `const out = readFileSync(outputPath, "utf8");`
        - `expect(out).toContain("next-version=v1.0.8");`
        - `expect(r.stderr).toContain("::warning::Drift reconciled");`
        - `const summary = readFileSync(summaryPath, "utf8");`
        - `expect(summary).toContain("## Drift reconciled");`
        - `expect(summary).toContain("## Bump");`

    - vitest.config.ts:
      - Change line 8 from `include: ['tests/**/*.test.ts'],` to `include: ['tests/**/*.test.ts', 'tests/**/*.test.mjs'],`.
      - Do NOT change anything else (coverage thresholds, environment, globals stay verbatim).
      - Coverage threshold scoping `src/calc/**` + `src/audit/**` is unaffected — `tests/scripts/bump-version.test.mjs` is NOT subject to the 100% threshold (per CONTEXT line 160: "scripts/** test, NOT subject to the 100%-coverage threshold").
  </behavior>
  <action>
    Create `scripts/bump-version.mjs` per the behavior block above using the canonical code template in 07-PATTERNS.md "Canonical bump-version.mjs core" (lines 117-157) — translate that prose into the actual ESM file, including the `semverMax` helper at lines 162-168 and the 2-space JSON output convention at lines 170-174.

    Create `tests/scripts/bump-version.test.mjs` per the behavior block above using the templates at 07-PATTERNS.md lines 200-246 (subprocess pattern + temp-dir scaffolding + the two `it(...)` cases). Cross-platform shell flag MUST be set (`shell: process.platform === "win32"`) per publish-cezari.cjs lines 27-31.

    Modify `vitest.config.ts` line 8 to extend the include array. Use the Edit tool. Final form: `include: ['tests/**/*.test.ts', 'tests/**/*.test.mjs'],`.

    Run `npx vitest run tests/scripts/bump-version.test.mjs` from the repo root and confirm both tests pass before declaring done. If either test fails, fix the script (NOT the test) — the tests encode the contract.

    Run `npm test` once at the end to confirm the full suite (398+2 = 400 tests minimum) still passes — adding the new file must NOT break existing tests.

    Do NOT touch package.json, do NOT add the bump script to npm scripts (it's CI-only — invoked via `node scripts/bump-version.mjs` directly from publish.yml; per CONTEXT line 195 the bump runs at run time on master, NOT in the Phase 7 PR diff).
  </action>
  <verify>
    <automated>npx vitest run tests/scripts/bump-version.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/bump-version.mjs` exists.
    - `grep -c '^#!/usr/bin/env node$' scripts/bump-version.mjs` returns 1.
    - `grep -c 'import { readFileSync, writeFileSync, appendFileSync } from "node:fs"' scripts/bump-version.mjs` returns 1 (or equivalent split-import; the three named imports must all be present).
    - `grep -c 'fileURLToPath(import.meta.url)' scripts/bump-version.mjs` returns 1 (ESM, no __dirname/require).
    - `grep -c 'const LOG_PREFIX = "\[bump-version\]"' scripts/bump-version.mjs` returns 1.
    - `grep -c 'process.env.GITHUB_OUTPUT' scripts/bump-version.mjs` >= 1.
    - `grep -c 'process.env.GITHUB_STEP_SUMMARY' scripts/bump-version.mjs` >= 1.
    - `grep -c 'next-version=v' scripts/bump-version.mjs` >= 1.
    - `grep -c 'next-version-bare=' scripts/bump-version.mjs` >= 1.
    - `grep -c '::warning::Drift reconciled' scripts/bump-version.mjs` >= 1.
    - `grep -c '## Drift reconciled' scripts/bump-version.mjs` >= 1.
    - `grep -c '## Bump' scripts/bump-version.mjs` >= 1.
    - `grep -c "JSON.stringify(.*null, 2)" scripts/bump-version.mjs` >= 2 (one for pkg, one for manifest — preserves 2-space indent).
    - `grep -E "require\(" scripts/bump-version.mjs` returns no matches (ESM only).
    - `grep -E "child_process|spawn|exec" scripts/bump-version.mjs` returns no matches (in-memory only — no git, no shell).

    - `tests/scripts/bump-version.test.mjs` exists.
    - `grep -c "import { describe, it, expect" tests/scripts/bump-version.test.mjs` returns 1.
    - `grep -c "spawnSync" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "mkdtempSync" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "happy path" tests/scripts/bump-version.test.mjs` returns 1.
    - `grep -c "drift" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "GITHUB_OUTPUT" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "GITHUB_STEP_SUMMARY" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "next-version=v1.0.8" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "next-version-bare=1.0.8" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -c "::warning::Drift reconciled" tests/scripts/bump-version.test.mjs` >= 1.
    - `grep -cE "^  it\(" tests/scripts/bump-version.test.mjs` returns 2 (exactly two test cases).

    - `vitest.config.ts` `include:` array contains both `'tests/**/*.test.ts'` AND `'tests/**/*.test.mjs'`.
    - `grep -c "tests/\*\*/\*.test.mjs" vitest.config.ts` returns 1.

    - `npx vitest run tests/scripts/bump-version.test.mjs` exits 0 with `2 passed`.
    - `npm test` exits 0 (full suite stays green; coverage thresholds for src/calc/** and src/audit/** still satisfied since neither was touched).
  </acceptance_criteria>
  <done>Bump script + two tests + vitest-config glob extension all in place; `npx vitest run tests/scripts/bump-version.test.mjs` reports 2/2 passing; `npm test` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Swap publish.yml dry-run echo for the real bump → package → publish → commit-back → tag tail</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .planning/phases/07-bump-publish-tag/07-CONTEXT.md (D-5 first-run-organic, D-7 verification posture, D-8 step-summary verbosity, D-9 probe stays unchanged, D-10 tag three states, D-12 carried-forward action pins + actor guard, Claude's-Discretion lines 113-134)
    - .planning/phases/07-bump-publish-tag/07-PATTERNS.md (sections ".github/workflows/publish.yml (MODIFIED)" through end — every NEW step has its canonical YAML inline; treat as verbatim source)
    - .planning/phases/06-workflow-scaffold-and-gates/branch-protection-probe-result.md (AUTHORITATIVE: master is NOT PROTECTED → use default GITHUB_TOKEN + permissions: contents: write at job level; no App, no RELEASE_PAT)
    - .planning/research/STACK.md (action versions, fetch-depth: 0 requirement for tag step, bot identity 41898282)
    - .planning/research/PITFALLS.md (Pitfall 5 = required tfx flags `--auth-type pat --no-prompt --no-wait-validation`; Pitfall 11 = Option B; Pitfall 14 = idempotent tag step; "Always pass via env: not via inline ${{ }} interpolation in run: shell strings. Never echo the secret.")
    - .github/workflows/publish.yml (current Phase 6 dry-run shape — lines 1-108 carry over, lines 110-123 get deleted)
    - scripts/publish-cezari.cjs (lines 84-91 — canonical reviewed `tfx extension publish` invocation flag set, with the `--share-with cezari` flag stripped for public publish)
    - package.json (line 21 `"package": "tfx extension create --manifest-globs vss-extension.json --output-path dist/"` — reuse the exact same flag set in the workflow's Package step for parity)
  </read_first>
  <action>
    Use the Edit tool on `.github/workflows/publish.yml`. Make these surgical changes; do NOT rewrite the file from scratch (lines 1-108 are byte-stable carry-overs from Phase 6).

    1) Edit job name (line 22):
       - Find: `    name: Publish to Marketplace (dry-run in P6)`
       - Replace with: `    name: Publish to Marketplace`

    2) Edit Checkout step (lines 27-28) to add `fetch-depth: 0`:
       - Find:
         ```
               - name: Checkout
                 uses: actions/checkout@v5
         ```
       - Replace with:
         ```
               - name: Checkout
                 uses: actions/checkout@v5
                 with:
                   fetch-depth: 0
         ```
       - Rationale: `git ls-remote --tags origin` and `git rev-parse <tag>` in the tag step require full history (research/STACK.md).

    3) Add a job-level `permissions:` block. Insert AFTER line 25 (`if: github.actor != 'github-actions[bot]'`) and BEFORE `steps:` line 26:
       ```
           permissions:
             contents: write
       ```
       - Rationale: bump-back commit + tag push need write access. Top-level (lines 17-18) stays at `contents: read` — principle of least privilege; only the publish job upgrades. Branch-protection-probe-result.md confirms master is NOT PROTECTED → default GITHUB_TOKEN suffices.

    4) Delete the dry-run echo step (lines 110-123 in the current file):
       ```
             - name: Dry-run — compute next version (DOES NOT publish)
               id: dryrun
               run: |
                 set -euo pipefail
                 CURRENT=$(node -p "require('./package.json').version")
                 IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
                 NEXT="${MAJOR}.${MINOR}.$((PATCH + 1))"
                 echo "would publish v${NEXT}"
                 echo "next-version=v${NEXT}" >> "$GITHUB_OUTPUT"
                 {
                   echo "## Dry-run result"
                   echo "Current: \`${CURRENT}\`"
                   echo "Would publish: \`v${NEXT}\`"
                 } >> "$GITHUB_STEP_SUMMARY"
       ```
       Replace it (in place — same position in the steps list, AFTER the `Probe master branch protection` step) with the eight new steps below in this exact order:

       ```yaml
             - name: Bump version (in-memory only)
               id: bump
               run: node scripts/bump-version.mjs

             - name: Package vsix
               run: |
                 set -euo pipefail
                 npx tfx extension create --manifest-globs vss-extension.json --output-path dist/
                 VSIX=$(ls dist/*.vsix | head -1)
                 SIZE_KB=$(du -k "$VSIX" | cut -f1)
                 {
                   echo "## Package"
                   echo "Created \`$(basename "$VSIX")\` (${SIZE_KB} KB)"
                 } >> "$GITHUB_STEP_SUMMARY"

             - name: Upload .vsix artifact
               uses: actions/upload-artifact@v4
               with:
                 name: vsix-${{ steps.bump.outputs.next-version-bare }}
                 path: dist/*.vsix
                 retention-days: 90
                 if-no-files-found: error

             - name: Publish to Marketplace
               env:
                 TFX_PAT: ${{ secrets.TFX_PAT }}
               run: |
                 set -euo pipefail
                 npx tfx extension publish --vsix dist/*.vsix --auth-type pat --token "$TFX_PAT" --no-prompt --no-wait-validation
                 {
                   echo "## Publish"
                   echo "Published \`v${{ steps.bump.outputs.next-version-bare }}\` to Marketplace"
                   echo "https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeTfsAiReviewTask.story-point-calculator"
                 } >> "$GITHUB_STEP_SUMMARY"

             - name: Commit version bump
               id: commit
               uses: stefanzweifel/git-auto-commit-action@v6
               with:
                 commit_message: "chore(release): ${{ steps.bump.outputs.next-version }} [skip ci]"
                 commit_user_name: "github-actions[bot]"
                 commit_user_email: "41898282+github-actions[bot]@users.noreply.github.com"
                 commit_author: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
                 file_pattern: "package.json vss-extension.json"

             - name: Surface commit-back result
               run: |
                 {
                   echo "## Commit-back"
                   echo "Committed bump as \`${{ steps.commit.outputs.commit_hash }}\` ([skip ci])"
                 } >> "$GITHUB_STEP_SUMMARY"

             - name: Tag release (best-effort, idempotent)
               id: tag
               continue-on-error: true
               run: |
                 set -euo pipefail
                 V="${{ steps.bump.outputs.next-version }}"
                 if git rev-parse "$V" >/dev/null 2>&1; then
                   echo "Tag $V already exists locally — skipping"
                   {
                     echo "## Tag"
                     echo "Tag \`$V\` already exists — idempotent skip per TAG-04"
                   } >> "$GITHUB_STEP_SUMMARY"
                   exit 0
                 fi
                 if git ls-remote --tags origin "$V" | grep -q "$V"; then
                   echo "Tag $V already exists on origin — skipping"
                   {
                     echo "## Tag"
                     echo "Tag \`$V\` already exists — idempotent skip per TAG-04"
                   } >> "$GITHUB_STEP_SUMMARY"
                   exit 0
                 fi
                 git tag -a "$V" -m "Release $V"
                 git push origin "$V"
                 {
                   echo "## Tag"
                   echo "🏷️ Tagged \`$V\` (annotated, pushed to origin)"
                 } >> "$GITHUB_STEP_SUMMARY"

             - name: Surface tag failure (workflow stays green per TAG-04)
               if: failure() && steps.tag.outcome == 'failure'
               run: |
                 V_BARE="${{ steps.bump.outputs.next-version-bare }}"
                 {
                   echo "## Tag"
                   echo "⚠️ Tag step failed (workflow stays green per TAG-04 best-effort)."
                   echo "Recovery: \`git tag -a v${V_BARE} -m 'Release v${V_BARE}' && git push origin v${V_BARE}\`"
                 } >> "$GITHUB_STEP_SUMMARY"
       ```

    5) After writing, verify by reading the file back. Expected final structure: lines 1-25 (header + on + concurrency + top-level permissions), line 26 (jobs.publish.name — UPDATED), line 27 (runs-on), line 28 (timeout-minutes), line 29 (if actor-guard), lines 30-31 (NEW permissions block), line 32 (steps:), then Checkout (with fetch-depth: 0), Setup Node, Install, Typecheck, Tests, Build, Bundle size, Asset audit, TFX_PAT verify, Branch-protection probe (all unchanged), then the eight NEW steps. NO `Dry-run — compute next version` step anywhere. NO `would publish v` literal anywhere. EXACTLY ONE `continue-on-error: true` (on the Tag release step).

    Pre-merge checks that the developer should run locally before merging this PR (per CONTEXT D-6 — record completion in 07-02, NOT here):
      a. `npx tfx extension publish --help` — reconcile flag spelling against the workflow's `--auth-type pat --token "$TFX_PAT" --no-prompt --no-wait-validation`. Fix YAML if any flag has been renamed in tfx-cli@0.23.1.
      b. Local `node scripts/publish-cezari.cjs` smoke (or just the auth handshake portion) to confirm TFX_PAT works against Marketplace. Side-effect note: if it actually publishes locally, that becomes the de-facto first publish and the merge becomes a no-op until the next PR — acceptable per CONTEXT D-6.

    Do NOT add a `workflow_dispatch`-only gate. Do NOT add a feature flag. Do NOT add a side-by-side rehearsal workflow. The merge of this PR IS the first publish (D-5).
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const y=fs.readFileSync('.github/workflows/publish.yml','utf8');const must=['fetch-depth: 0','permissions:','contents: write','id: bump','node scripts/bump-version.mjs','npx tfx extension create --manifest-globs vss-extension.json --output-path dist/','actions/upload-artifact@v4','vsix-${{ steps.bump.outputs.next-version-bare }}','retention-days: 90','if-no-files-found: error','npx tfx extension publish --vsix dist/*.vsix --auth-type pat --token \"$TFX_PAT\" --no-prompt --no-wait-validation','stefanzweifel/git-auto-commit-action@v6','[skip ci]','github-actions[bot]','41898282+github-actions[bot]@users.noreply.github.com','file_pattern: \"package.json vss-extension.json\"','continue-on-error: true','git tag -a','git push origin','git ls-remote --tags origin','## Bump','## Package','## Publish','## Commit-back','if: failure() && steps.tag.outcome == \\'failure\\''];const forbidden=['Dry-run — compute next version','would publish v','(dry-run in P6)'];let ok=true;for(const m of must){if(!y.includes(m)){console.error('MISSING: '+m);ok=false;}}for(const f of forbidden){if(y.includes(f)){console.error('FORBIDDEN PRESENT: '+f);ok=false;}}const cnt=(y.match(/continue-on-error: true/g)||[]).length;if(cnt!==1){console.error('continue-on-error count = '+cnt+' (want 1)');ok=false;}if(!ok)process.exit(1);console.log('publish.yml structure OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.github/workflows/publish.yml`.
    - `grep -c 'fetch-depth: 0' .github/workflows/publish.yml` returns 1.
    - `grep -c 'name: Publish to Marketplace$' .github/workflows/publish.yml` returns 1 (no `(dry-run in P6)` suffix).
    - `grep -cE '^    permissions:' .github/workflows/publish.yml` returns 1 (job-level permissions block — top-level `permissions:` at line 17 has no leading spaces in `permissions:`).
    - `grep -c 'contents: write' .github/workflows/publish.yml` returns 1.
    - `grep -c 'contents: read' .github/workflows/publish.yml` returns 1 (top-level baseline preserved).
    - `grep -c 'id: bump' .github/workflows/publish.yml` returns 1.
    - `grep -c 'node scripts/bump-version.mjs' .github/workflows/publish.yml` returns 1.
    - `grep -c 'npx tfx extension create --manifest-globs vss-extension.json --output-path dist/' .github/workflows/publish.yml` returns 1.
    - `grep -c 'actions/upload-artifact@v4' .github/workflows/publish.yml` returns 1.
    - `grep -c 'name: vsix-\${{ steps.bump.outputs.next-version-bare }}' .github/workflows/publish.yml` returns 1.
    - `grep -c 'retention-days: 90' .github/workflows/publish.yml` returns 1.
    - `grep -c 'if-no-files-found: error' .github/workflows/publish.yml` returns 1.
    - `grep -c 'npx tfx extension publish --vsix dist/\*.vsix --auth-type pat --token "\$TFX_PAT" --no-prompt --no-wait-validation' .github/workflows/publish.yml` returns 1.
    - `grep -cE '^        env:' .github/workflows/publish.yml` returns >= 1 (publish step has env: block; probe step also has env: block — total >= 2 acceptable; the key requirement is that TFX_PAT comes via env:, NOT inline `${{ secrets.TFX_PAT }}` in the run: line).
    - `grep -c 'TFX_PAT: \${{ secrets.TFX_PAT }}' .github/workflows/publish.yml` returns 1.
    - `grep -c 'stefanzweifel/git-auto-commit-action@v6' .github/workflows/publish.yml` returns 1.
    - `grep -F 'commit_message: "chore(release): ${{ steps.bump.outputs.next-version }} [skip ci]"' .github/workflows/publish.yml` returns >= 1 line (literal commit_message input present with both `${{ steps.bump.outputs.next-version }}` AND `[skip ci]`).
    - `grep -c 'commit_user_name: "github-actions\[bot\]"' .github/workflows/publish.yml` returns 1.
    - `grep -c '41898282+github-actions\[bot\]@users.noreply.github.com' .github/workflows/publish.yml` >= 1.
    - `grep -c 'file_pattern: "package.json vss-extension.json"' .github/workflows/publish.yml` returns 1.
    - `grep -c 'id: commit' .github/workflows/publish.yml` returns 1.
    - `grep -c 'steps.commit.outputs.commit_hash' .github/workflows/publish.yml` returns 1.
    - `grep -c 'id: tag' .github/workflows/publish.yml` returns 1.
    - `grep -c 'continue-on-error: true' .github/workflows/publish.yml` returns EXACTLY 1 (only the tag step has it; no other step does).
    - `grep -c 'git tag -a' .github/workflows/publish.yml` returns 1.
    - `grep -c 'git push origin' .github/workflows/publish.yml` returns 1.
    - `grep -c 'git ls-remote --tags origin' .github/workflows/publish.yml` returns 1.
    - `grep -c 'git rev-parse' .github/workflows/publish.yml` returns 1.
    - `grep -cF "if: failure() && steps.tag.outcome == 'failure'" .github/workflows/publish.yml` returns 1.
    - `grep -c '## Bump' .github/workflows/publish.yml` returns 0 (the `## Bump` block is written by bump-version.mjs, NOT inline YAML — but the workflow file may not contain the literal string).
      - Adjusted criterion: the workflow file MAY or MAY NOT contain the literal `## Bump` (the script writes it). Required: `grep -c '## Package' .github/workflows/publish.yml` returns 1, `grep -c '## Publish' .github/workflows/publish.yml` returns 1, `grep -c '## Commit-back' .github/workflows/publish.yml` returns 1, `grep -c '## Tag' .github/workflows/publish.yml` >= 2 (Created/Skipped state in tag step + Failed state in follow-up).
    - `grep -c 'Dry-run — compute next version' .github/workflows/publish.yml` returns 0 (deleted).
    - `grep -c 'would publish v' .github/workflows/publish.yml` returns 0 (deleted).
    - `grep -c '(dry-run in P6)' .github/workflows/publish.yml` returns 0 (deleted).
    - `grep -c 'workflow_dispatch:' .github/workflows/publish.yml` returns 1 (preserved from Phase 6 — FAIL-03).
    - `grep -c 'paths-ignore:' .github/workflows/publish.yml` returns 1 (preserved from Phase 6).
    - `grep -c 'concurrency:' .github/workflows/publish.yml` returns 1 (preserved from Phase 6).
    - `grep -cF "if: github.actor != 'github-actions[bot]'" .github/workflows/publish.yml` returns 1 (CI-06 actor-guard preserved).
    - `grep -c 'Probe master branch protection' .github/workflows/publish.yml` returns 1 (D-9 — probe stays unchanged).
    - The single-quoted node verifier in `<verify>` exits 0.
    - YAML parses (optional sanity: `python -c "import yaml; yaml.safe_load(open('.github/workflows/publish.yml'))"` if python available; otherwise `node -e "require('js-yaml')..."` if available; otherwise rely on the GitHub Actions parser at run time).
  </acceptance_criteria>
  <done>publish.yml swap complete: dry-run echo gone; bump → package → upload → publish → commit → surface → tag → surface-failure tail wired; fetch-depth: 0 added; job-level `permissions: contents: write` added; job name no longer says `(dry-run in P6)`; verifier script exits 0.</done>
</task>

</tasks>

<verification>
End-of-plan checks (run before opening the PR):

1. `npx vitest run tests/scripts/bump-version.test.mjs` — 2 tests pass.
2. `npm test` — full suite passes (398+ existing + 2 new = 400+ green).
3. `npm run typecheck` — clean (no TS errors; the new `.mjs` script is not type-checked, the new `.test.mjs` is not type-checked, but `vitest.config.ts` typechecks against its own type definitions).
4. The verifier in Task 2's `<verify>` block exits 0.
5. Manual sanity: open `.github/workflows/publish.yml` in an editor, confirm step order matches the 18-step plan in the `<interfaces>` block (10 carry-over from P6 + 8 NEW; dry-run echo gone).
6. Pre-merge developer checks per CONTEXT D-6 (DO NOT block on these — they are dev's eyeball + pen-and-paper checks; record the outcome in 07-02):
   a. `npx tfx extension publish --help` → reconcile flag spelling. If any flag has changed in tfx-cli@0.23.1 (e.g., `--no-prompt` renamed), update publish.yml AND re-run Task 2 verifier.
   b. Local PAT smoke via `node scripts/publish-cezari.cjs` (or auth-handshake-only portion). Confirms TFX_PAT works.
</verification>

<success_criteria>
- All 14 phase requirements (BUMP-01..05, PUBLISH-01..05, TAG-01..04) addressed by the diff.
- Two vitest tests pass; full suite stays green.
- publish.yml's dry-run step is gone; eight new steps are wired; one `continue-on-error: true` (tag step only).
- Job name no longer carries `(dry-run in P6)`; permissions: contents: write at job level only; top-level remains contents: read.
- Bump script writes both `next-version=v1.0.8` and `next-version-bare=1.0.8` to `$GITHUB_OUTPUT`; downstream steps reference the appropriate flavor (tag uses prefixed, artifact + publish summary use bare).
- All five load-bearing step-summary sections present (`## Bump` from script, `## Package`, `## Publish`, `## Commit-back`, `## Tag`); upload-artifact omits its own summary write per D-8.
- Tag step has three explicit summary states (Created / Skipped / Failed via follow-up step gated on `if: failure() && steps.tag.outcome == 'failure'`).
- Branch-protection probe step (Phase 6 carry-over) UNCHANGED per D-9.
- Triple anti-loop defense intact: actor-guard at job level (carry-over) + `[skip ci]` in commit message (NEW) + default GITHUB_TOKEN by GitHub design.
- The PR diff modifies these files ONLY: `scripts/bump-version.mjs` (new), `tests/scripts/bump-version.test.mjs` (new), `vitest.config.ts` (1-line glob extension), `.github/workflows/publish.yml` (job name + checkout fetch-depth + permissions block + dry-run-step replaced with 8 new steps). NO change to package.json, NO change to vss-extension.json, NO change to ci.yml, NO change to scripts/publish-cezari.cjs (its archival is Phase 8).
- The merge of THIS PR fires `publish.yml` on master and ships v1.0.8 organically. No staged feature flag, no manual dispatch gate (D-5).
</success_criteria>

<output>
After completion, create `.planning/phases/07-bump-publish-tag/07-01-SUMMARY.md` documenting:
- The two artifacts created (`bump-version.mjs` + test).
- The vitest config glob extension.
- The publish.yml diff summary (lines deleted vs lines added; 8 new steps named).
- The pre-merge developer checks completed (D-6: tfx --help reconcile + local PAT smoke result).
- Confirmation that no other files were touched (PR diff scope).
- Note: the actual first-run-organic publish (and its triple-check verification) is captured by 07-02-SUMMARY + 07-VERIFICATION.md, NOT here.
</output>
