---
phase: 05-polish-marketplace-publish
plan: 03
type: execute
wave: 1
depends_on: ["05-02"]
files_modified:
  - vss-extension.json
  - marketplace/overview.md
  - README.md
  - images/screenshots/screenshot-calculator-light.png
  - images/screenshots/screenshot-calculator-dark.png
autonomous: false
requirements: [PKG-05]
must_haves:
  truths:
    - "`vss-extension.json` carries the Marketplace listing fields (`content.details.path`, `links.{getstarted,support,license}`, `repository`, `screenshots[]`, expanded `tags`, the new `marketplace/` files entry) per RESEARCH §Pattern 3 manifest delta — but `version` stays 0.2.x and `public` stays false (the public flip is Wave 3 / Plan 05-05's job)"
    - "`marketplace/overview.md` exists with the GFM listing content per RESEARCH §Pattern 4 — covering D-9 (terse + technical tone), D-10 (3 known-limitations bullets), D-11 (user-facing formula only — no W / Raw SP math), D-1 (custom SP fields documented as v2 deferred), and the privacy/data-handling bullet block (PKG-05 SC #2 'no telemetry; data stays in user's ADO org')"
    - "`README.md` is expanded to the v1 polish per RESEARCH §README v1 Outline — engineer-audience content (install via Marketplace, Development, Publishing instructions referencing `npm run publish:cezari` and `publish:public`) plus the formula math (W = 0.4·C + 0.4·U + 0.2·E) that is intentionally NOT in overview.md per D-11 audience separation (RESEARCH §Pitfall 6)"
    - "Two screenshot PNGs (`images/screenshots/screenshot-calculator-{light,dark}.png`) exist on disk, captured per RESEARCH §Pattern 5 procedure on the cezari Phase-4 build (0.2.5 — already shared with cezari from Phase 4) — at the resolved-from-current-trio state (C=Hard, U=Medium, E=Easy → W=2.40, Raw SP=1.95, Final=2) per D-8 visual-richness recommendation"
    - "After the manifest edit + listing assets are committed, a private re-publish to cezari via `npm run publish:cezari` (with the manifest's version walked +1 patch — e.g., 0.2.5 → 0.2.6) succeeds, validates the listing-page rendering on Marketplace, and the user has visually inspected the listing page to confirm the description, screenshots, and links render correctly BEFORE Wave 3 commits the public flip"
  artifacts:
    - path: "vss-extension.json"
      provides: "Marketplace listing manifest fields per RESEARCH §Pattern 3 (content/links/repository/screenshots/expanded tags/marketplace files entry); version walked one patch for the listing-asset re-publish; public still false"
      contains: "\"content\""
    - path: "marketplace/overview.md"
      provides: "Marketplace listing detail page (GFM) per RESEARCH §Pattern 4 — covers D-1, D-9, D-10, D-11, PKG-05 SC #2 privacy"
      contains: "Privacy and data handling"
      min_lines: 60
    - path: "README.md"
      provides: "v1 polished engineer-audience README per RESEARCH §README v1 Outline — install/usage/calculation-math/development/publishing/contributing/license"
      contains: "How the calculation works"
      min_lines: 80
    - path: "images/screenshots/screenshot-calculator-light.png"
      provides: "Light-theme calculator screenshot at resolved-trio state per D-8"
      contains: "(binary PNG file; presence verified by `test -f`)"
    - path: "images/screenshots/screenshot-calculator-dark.png"
      provides: "Dark-theme calculator screenshot at resolved-trio state per D-8"
      contains: "(binary PNG file; presence verified by `test -f`)"
  key_links:
    - from: "vss-extension.json content.details.path"
      to: "marketplace/overview.md"
      via: "tfx-cli reads the manifest's content.details.path at package time and includes the markdown in the VSIX"
      pattern: "marketplace/overview\\.md"
    - from: "vss-extension.json files[]"
      to: "marketplace/ + images/screenshots/"
      via: "Both directories must be allowlisted via `files[]` for tfx to include them in the VSIX (Pitfall 4)"
      pattern: "\"path\":\\s*\"marketplace\""
    - from: "vss-extension.json screenshots[]"
      to: "images/screenshots/screenshot-calculator-{light,dark}.png"
      via: "Marketplace renders these in the listing carousel above the description"
      pattern: "screenshot-calculator-(light|dark)\\.png"
---

<objective>
Land the Marketplace listing-asset bundle in one cohesive plan: (1) the `vss-extension.json` manifest delta per RESEARCH §Pattern 3 (adds `content`, `links`, `repository`, `screenshots`, expanded `tags`, the `marketplace/` files entry — but does NOT bump version or flip public, both deferred to Plan 05-05), (2) `marketplace/overview.md` per RESEARCH §Pattern 4 (the GFM listing detail page covering D-9 / D-10 / D-11 / D-1 / PKG-05 SC #2 privacy), (3) the v1 polished `README.md` per RESEARCH §README v1 Outline (engineer-audience content with the formula math D-11 keeps OUT of overview.md), and (4) the two listing screenshots per RESEARCH §Pattern 5 (manual capture on cezari at the resolved-trio state per D-8). After commits, a private re-publish to cezari validates the listing-page rendering on Marketplace BEFORE Wave 3 commits the public flip.

Purpose: PKG-05 (description, screenshots light+dark, formula explanation, privacy/data-handling, 128×128 icon) becomes provable. The listing-asset re-publish to cezari proves the Marketplace renderer handles the GFM correctly, the screenshots resolve, the manifest links are correct, and `tfx extension create` includes the new directories — all auditable before the irreversible-ish public flip in Wave 3.

Output: Manifest with listing fields populated; the overview/README markdown pair; two PNG screenshots committed; one private re-publish to cezari with user visual confirmation of the rendered listing page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish-marketplace-publish/05-CONTEXT.md
@.planning/phases/05-polish-marketplace-publish/05-RESEARCH.md
@vss-extension.json
@README.md

<interfaces>
<!-- All four artifacts have verbatim source in RESEARCH.md. -->

**Manifest delta (vss-extension.json):** RESEARCH §Pattern 3 contains the verbatim full-file replacement. CRITICAL DEVIATION FROM RESEARCH: in Plan 05-03 we keep `"version": "0.2.5"` (or `0.2.6` after the version walk for re-publish — see Task 5) AND `"public": false`. RESEARCH §Pattern 3 shows the final-state manifest with `"version": "1.0.0"` and `"public": true`; that's the Plan 05-05 endpoint. Plan 05-03 adds the listing fields ONLY. The "Sequencing note on the public flip" in RESEARCH §Pattern 3 explicitly mandates this two-commit split:
> *"Commit A (Wave 1): Add `content`, `links`, `repository`, `screenshots`, the new `files` entry, and the expanded `tags`. Keep `version: 0.2.x`, keep `public: false`. Publish privately to cezari... Commit B (Wave 3): Bump `version: 0.2.x → 1.0.0`, flip `public: false → true`. Publish."*

**marketplace/overview.md:** RESEARCH §Pattern 4 contains the verbatim source (~476–547). Copy as-is. CRITICAL: this file does NOT include the W = 0.4·C + 0.4·U + 0.2·E formula or the 26^((W-1)/4) Raw SP formula, per D-11 ("user-facing formula explanation only" — five level labels per axis, plain English, no math symbols). The math lives in README.md.

**README.md:** RESEARCH §README v1 Outline (~1112–1223) contains the verbatim outline. The current `README.md` is unknown size; this plan REWRITES it to the polished v1 shape. The "How the calculation works" section is the engineering-curious surface that overview.md does NOT have (D-11 audience separation). RESEARCH §Pitfall 6 documents the audience-separation rationale ("README is for engineers; overview.md is for installers").

**Screenshots:** RESEARCH §Pattern 5 (~558–629) describes the manual capture procedure verbatim. The trio C=Hard, U=Medium, E=Easy (W=2.40, Raw SP=1.95, Final=2) is the recommended state for visual richness — non-trivial values across all three Calc panel rows. Marketplace recommends 1366×768 dimensions. Pitfall 3 details the dimension-padding approach if using DevTools "Capture node screenshot" produces sub-1366 captures.

**Build infrastructure prerequisite:** Plan 05-02 already landed (Wave 1, no `depends_on` between 05-02 and 05-03 — they share Wave 1 with NO file overlap). The `npm run publish:cezari` script that Task 6 invokes was created by Plan 05-02. **In execution order, Plan 05-02 must complete BEFORE Task 6 of this plan runs**; the wave structure assumes parallel completion of Wave 1 plans before Wave 2 starts, but within Wave 1 the publish-script-using task (Task 6 of 05-03) sequentially depends on Plan 05-02's deliverables. The orchestrator must enforce this — either by completing 05-02 first within Wave 1, or by reordering so 05-02 completes before 05-03 Task 6.

**Icon refinement (Open Q1 from RESEARCH):** the current `images/icon.png` is a 143-byte transparent placeholder. RESEARCH recommends regenerating it using `scripts/generate-toolbar-icon.cjs`-style logic (calculator glyph at 128×128). This plan does NOT include icon refinement as a required task — it adds it as Task 4 (icon refinement, optional/best-effort), per CONTEXT "Claude's Discretion: Icon refinement". If the user reports the listing-page render in Task 7 shows the icon as fine, the optional task can be skipped. If the icon visibly fails (Marketplace displays a blank tile), Task 4 fires.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the vss-extension.json listing-fields manifest delta (NO version bump, NO public flip)</name>
  <files>
    vss-extension.json
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pattern 3: Marketplace Listing Manifest Fields" — verbatim manifest delta (~398–456) + the "Sequencing note on the public flip" (~458–463)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 4: VSIX missing the marketplace/ directory after manifest edit" (the `files[]` entry is critical)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 7: VSIX missing the LICENSE file when content.license.path is declared" (do NOT declare `content.license`; use `links.license` URL instead)
    - vss-extension.json (current state — version 0.2.5, public false, no listing fields, files=[dist, images], tags=[story points, estimation, scrum, agile])
    - Confirm git remote URL: `git remote -v` should print `origin https://github.com/tsmshvenieradze/StoryPointExtension.git`. RESEARCH §Pattern 3's links/repository fields use this URL; if the remote is different, adjust the URLs accordingly. (As of plan creation, the remote was confirmed as `https://github.com/tsmshvenieradze/StoryPointExtension.git`.)
  </read_first>
  <action>
    1. Read `vss-extension.json` to confirm the current state matches what Plan 05-03 expects (version 0.2.5; public false; no `content`/`links`/`repository`/`screenshots` keys; `files` has 2 entries `dist` and `images`; `tags` is 4 elements).

    2. Edit `vss-extension.json` to add the listing-asset fields. Use the Edit tool (preferred) or Write tool (full rewrite acceptable for a single small JSON file). The TARGET state — copy the structure verbatim from RESEARCH §Pattern 3 manifest delta but WITH TWO ALTERATIONS:
       - Keep `"version": "0.2.5"` (do NOT bump to 1.0.0 — that's Plan 05-05).
       - Keep `"public": false` (do NOT flip to true — that's Plan 05-05).

       The full target file:

       ```json
       {
         "manifestVersion": 1,
         "id": "story-point-calculator",
         "version": "0.2.5",
         "name": "Story Point Calculator",
         "publisher": "TsezariMshvenieradzeExtensions",
         "public": false,
         "targets": [
           { "id": "Microsoft.VisualStudio.Services" }
         ],
         "categories": ["Azure Boards"],
         "tags": ["story points", "estimation", "scrum", "agile", "cmmi", "fibonacci"],
         "description": "Structured Story Point estimation using Complexity, Uncertainty, Effort.",
         "icons": {
           "default": "images/icon.png"
         },
         "scopes": [
           "vso.work_write"
         ],
         "files": [
           { "path": "dist", "addressable": true },
           { "path": "images", "addressable": true },
           { "path": "marketplace" }
         ],
         "content": {
           "details": {
             "path": "marketplace/overview.md"
           }
         },
         "links": {
           "getstarted": {
             "uri": "https://github.com/tsmshvenieradze/StoryPointExtension#usage"
           },
           "support": {
             "uri": "https://github.com/tsmshvenieradze/StoryPointExtension/issues"
           },
           "license": {
             "uri": "https://github.com/tsmshvenieradze/StoryPointExtension/blob/master/LICENSE"
           }
         },
         "repository": {
           "type": "git",
           "uri": "https://github.com/tsmshvenieradze/StoryPointExtension"
         },
         "screenshots": [
           { "path": "images/screenshots/screenshot-calculator-light.png" },
           { "path": "images/screenshots/screenshot-calculator-dark.png" }
         ],
         "contributions": [
           {
             "id": "calc-sp-action",
             "type": "ms.vss-web.action",
             "description": "Toolbar action that opens the Story Point calculator modal.",
             "targets": ["ms.vss-work-web.work-item-toolbar-menu"],
             "properties": {
               "text": "Calculate Story Points",
               "title": "Open the Story Point calculator",
               "toolbarText": "Calculate SP",
               "icon": "images/toolbar-icon.png",
               "uri": "dist/toolbar.html",
               "registeredObjectId": "calc-sp-action"
             }
           },
           {
             "id": "calc-sp-modal",
             "type": "ms.vss-web.external-content",
             "description": "Modal contents loaded into a host-managed dialog.",
             "properties": {
               "uri": "dist/modal.html"
             }
           }
         ]
       }
       ```

       **CRITICAL OMISSIONS** vs RESEARCH §Pattern 3:
       - NO `content.license` block — RESEARCH §Pitfall 7 says use `links.license` only (already present here). This avoids the "VSIX missing LICENSE" trap.
       - NO `branding` block — RESEARCH §Pattern 3 marks this OPTIONAL ("Recommended skip for v1").

    3. Verify: file is valid JSON (parses). Run `node -e "JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'))"` — exits 0 if parse succeeds.

    4. Verify the build still works (manifest changes shouldn't affect webpack, but confirm): `npm run build && npm run check:size` — exits 0.

    Note: This task does NOT trigger any publish. Tasks 2/3/5 add the referenced files (overview.md, README.md, screenshots); Task 6 publishes after all assets exist.
  </action>
  <acceptance_criteria>
    - `vss-extension.json` parses as valid JSON (greppable: `node -e "JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'))"` exits 0).
    - Version is still `"0.2.5"` (greppable: `grep -F '"version": "0.2.5"' vss-extension.json`) — NOT bumped to 1.0.0.
    - `public` is still `false` (greppable: `grep -F '"public": false' vss-extension.json`).
    - `content.details.path` exists pointing at `marketplace/overview.md` (greppable: `grep -F "marketplace/overview.md" vss-extension.json`).
    - `links.getstarted`, `links.support`, `links.license` all exist (greppable: `grep -F '"getstarted"' vss-extension.json && grep -F '"support"' vss-extension.json && grep -F '"license"' vss-extension.json`).
    - `repository.uri` matches the GitHub remote (greppable: `grep -F "https://github.com/tsmshvenieradze/StoryPointExtension" vss-extension.json`).
    - `screenshots[]` references both light and dark PNGs (greppable: `grep -F "screenshot-calculator-light.png" vss-extension.json && grep -F "screenshot-calculator-dark.png" vss-extension.json`).
    - `files[]` includes the new `marketplace` entry (greppable: `grep -F '"path": "marketplace"' vss-extension.json`).
    - `tags[]` is expanded to include `cmmi` and `fibonacci` (greppable: `grep -F '"cmmi"' vss-extension.json && grep -F '"fibonacci"' vss-extension.json`).
    - **Plan-Check H-1 fix:** No `content.license` block in the parsed manifest (RESEARCH §Pitfall 7). Verified via Node JSON.parse — exit non-zero if `m.content && m.content.license` is truthy. The previous `! grep -F "content.license"` check was vacuously satisfied (the dotted form never appears in valid JSON); this structured assertion is the actual regression guard. Verify command: `node -e "const m=JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'));if(m.content&&m.content.license){console.error('content.license set — Pitfall 7');process.exit(1)}"`.
    - **Plan-Check M-2 fix:** GitHub remote URL matches the value baked into the manifest (RESEARCH §Assumptions Log A8 — was ASSUMED). Precondition gate: `git remote get-url origin | grep -F "github.com/tsmshvenieradze/StoryPointExtension"` exits 0. If the remote diverges (private fork / rename), edit the manifest URLs to match the actual remote BEFORE running the rest of Task 1.
    - `npm run build && npm run check:size` exits 0 (manifest doesn't break the build).
  </acceptance_criteria>
  <verify>
    <automated>git remote get-url origin | grep -F "github.com/tsmshvenieradze/StoryPointExtension" && node -e "const m=JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'));if(m.content&&m.content.license){console.error('content.license set — Pitfall 7 violation');process.exit(1)}" && node -e "JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'))" && grep -F '"version": "0.2.5"' vss-extension.json && grep -F '"public": false' vss-extension.json && grep -F "marketplace/overview.md" vss-extension.json && grep -F '"getstarted"' vss-extension.json && grep -F "https://github.com/tsmshvenieradze/StoryPointExtension" vss-extension.json && grep -F "screenshot-calculator-light.png" vss-extension.json && grep -F "screenshot-calculator-dark.png" vss-extension.json && grep -F '"path": "marketplace"' vss-extension.json && grep -F '"cmmi"' vss-extension.json && grep -F '"fibonacci"' vss-extension.json && npm run build && npm run check:size</automated>
  </verify>
  <done>The manifest has the listing-asset fields populated. Version stays 0.2.5; public stays false. The build still works.</done>
</task>

<task type="auto">
  <name>Task 2: Create marketplace/overview.md (GFM listing detail page)</name>
  <files>
    marketplace/overview.md
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pattern 4: Marketplace Overview Markdown Subset" — verbatim source (~475–548)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Notes on tone (per D-9)" (~550–556)
    - .planning/phases/05-polish-marketplace-publish/05-CONTEXT.md D-1, D-9, D-10, D-11 (the listing-content decisions this file implements)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 6: README and overview.md drift apart" — audience separation rationale
  </read_first>
  <action>
    1. The `marketplace/` directory does not exist. Use the Write tool to create `marketplace/overview.md` directly — Write creates intermediate directories.

    2. Copy the verbatim GFM source from RESEARCH §"Pattern 4 — Verbatim recommended structure for `marketplace/overview.md`". The source covers all of:
       - **D-9 (terse + technical tone):** plain English, no marketing fluff, GFM tables for dense facts.
       - **D-10 (3 known limitations):** Esc dismissal, reactive read-only, custom SP fields not yet supported.
       - **D-11 (user-facing formula only):** describes the three axes in plain English with five level labels each; does NOT include the W = 0.4·C + 0.4·U + 0.2·E weighted-sum formula or the 26^((W-1)/4) Raw SP formula. The Fibonacci rounding values (0.5, 1, 2, 3, 5, 8, 13) ARE listed as "rounded to the nearest Fibonacci number" — that's a result description, not a math formula.
       - **D-1 (custom SP fields v2 deferred):** the "Custom Story Points field names not yet supported" bullet under Known limitations names the v2 roadmap explicitly.
       - **PKG-05 SC #2 (privacy/data-handling):** the bulleted "Privacy and data handling" section explicitly states "no telemetry; data stays in the user's ADO org" and lists the `vso.work_write` scope claim.

    3. Verify the file:
       - `test -f marketplace/overview.md` exits 0.
       - File contains the literal headings `# Story Point Calculator`, `## What it does`, `## Screenshots`, `## How to use`, `## The three axes`, `## Supported processes`, `## Privacy and data handling`, `## Known limitations (v1)`, `## Roadmap (v2)`, `## Support`, `## License`.
       - File does NOT contain the W = 0.4·C + 0.4·U + 0.2·E literal formula (D-11 forbids math in overview.md). Greppable: `! grep -F "0.4·C + 0.4·U + 0.2·E" marketplace/overview.md` AND `! grep -F "0.5 × 26" marketplace/overview.md`.
       - File contains the 3 known limitations bullets (greppable: `grep -F "Esc key does not dismiss" marketplace/overview.md && grep -F "Read-only state surfaces reactively" marketplace/overview.md && grep -F "Custom Story Points field names not yet supported" marketplace/overview.md`).
       - File contains the `## Privacy and data handling` heading (greppable: `grep -F "## Privacy and data handling" marketplace/overview.md`).
       - File contains the inline screenshot references that match the manifest's `screenshots[]` paths (greppable: `grep -F "images/screenshots/screenshot-calculator-light.png" marketplace/overview.md && grep -F "images/screenshots/screenshot-calculator-dark.png" marketplace/overview.md`).
  </action>
  <acceptance_criteria>
    - `marketplace/overview.md` exists.
    - File contains all 11 expected headings (greppable per the heading list above; specifically `grep -c "^## " marketplace/overview.md` returns >= 9 — at least 9 H2 sections).
    - File covers D-1 (greppable: `grep -F "Custom Story Points field names not yet supported" marketplace/overview.md`).
    - File covers D-10 (greppable: `grep -F "Esc key does not dismiss" marketplace/overview.md && grep -F "Read-only state surfaces reactively" marketplace/overview.md`).
    - File covers PKG-05 SC #2 (greppable: `grep -F "no telemetry" marketplace/overview.md` OR `grep -iF "no telemetry" marketplace/overview.md` — case-insensitive fallback).
    - File does NOT contain the W = 0.4·C... formula or the 0.5 × 26 Raw SP formula (D-11 audience separation).
    - File references both screenshot paths (greppable: per the screenshot-name greps above).
  </acceptance_criteria>
  <verify>
    <automated>test -f marketplace/overview.md && grep -F "# Story Point Calculator" marketplace/overview.md && grep -F "## What it does" marketplace/overview.md && grep -F "## Privacy and data handling" marketplace/overview.md && grep -F "## Known limitations (v1)" marketplace/overview.md && grep -iF "no telemetry" marketplace/overview.md && grep -F "Custom Story Points field names not yet supported" marketplace/overview.md && grep -F "Esc key does not dismiss" marketplace/overview.md && ! grep -F "0.4·C + 0.4·U + 0.2·E" marketplace/overview.md && ! grep -F "0.5 × 26" marketplace/overview.md && grep -F "images/screenshots/screenshot-calculator-light.png" marketplace/overview.md && grep -F "images/screenshots/screenshot-calculator-dark.png" marketplace/overview.md</automated>
  </verify>
  <done>The Marketplace listing detail page exists at `marketplace/overview.md` with all D-1/D-9/D-10/D-11/PKG-05 SC #2 content. The file does NOT include the W formula (D-11 — that lives in README.md only).</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite README.md to the v1 polished engineer-audience structure</name>
  <files>
    README.md
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"README v1 Outline (Claude's Discretion)" — verbatim outline (~1109–1233)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 6: README and overview.md drift apart" — recommended pattern (a) audience-separated files
    - marketplace/overview.md (just created in Task 2 — the README's Usage / Privacy / Limitations / Roadmap sections mirror this content)
    - The existing README.md (read it once to see what's currently there; the rewrite is a full replacement)
    - package.json scripts entries created by Plan 05-02 (`check:size`, `publish:cezari`, `publish:public`) — README's Development + Publishing sections reference these
  </read_first>
  <action>
    1. Read the current `README.md` to understand what's being replaced. The Phase 0 / Phase 1 README is minimal (a few lines describing the extension). The Phase 5 polish brings it to the v1 shape.

    2. Use the Write tool to rewrite `README.md` per RESEARCH §"README v1 Outline" verbatim. The structure (each section is a `## ` heading):

       - **Title + tagline:** `# Story Point Calculator` + 1-line description.
       - **Install:** Marketplace install link/instructions. The link will be live once Plan 05-05 publishes; for now use `https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator` (the predictable URL).
       - **What it does:** 1 paragraph, same opening as overview.md (allowed overlap).
       - **Screenshots:** Inline image references — `![Light theme](images/screenshots/screenshot-calculator-light.png)` and `![Dark theme](...)`. (Files don't exist yet — Task 5 captures them; the references are forward-looking.)
       - **Usage:** Steps 1-4 from overview.md "How to use" (verbatim per Pitfall 6 pattern (a) — "How to use" can be near-identical between the two files).
       - **How the calculation works:** ENGINEER-AUDIENCE FORMULA TABLE per RESEARCH §README outline (~1136–1152). Includes the literal `W = 0.4·C + 0.4·U + 0.2·E` formula, the `0.5 × 26^((W−1)/4)` Raw SP formula, and the Fibonacci threshold table (≤0.75→0.5, ≤1.5→1, ≤2.5→2, ≤4→3, ≤6.5→5, ≤10.5→8, else→13). This is the content D-11 deliberately keeps OUT of overview.md.
       - **Supported processes:** Same table as overview.md.
       - **Privacy:** Same bullets as overview.md.
       - **Known limitations (v1):** Same bullets as overview.md.
       - **Roadmap (v2):** Same as overview.md.
       - **Development:** The 5-line npm script chain — `npm ci`, `npm run typecheck`, `npm test`, `npm run build`, `npm run check:size`. Mention CI runs the same chain via `.github/workflows/ci.yml`.
       - **Publishing:** Two sub-sections per RESEARCH §"README v1 Outline → Publishing" (~1183–1214):
         - **One-time setup:** create `.env.local` with `TFX_PAT=<PAT>`; gitignored.
         - **Publish privately to cezari (dev iteration):** `npm run build && npm run publish:cezari` (the script created by Plan 05-02).
         - **Publish publicly to Marketplace:** confirm publisher verified at the portal URL; edit vss-extension.json (bump version, set public:true); run `npm run build && npm run publish:cezari` (same script — tfx reads `public:true` from the manifest); verify at the listing URL.
       - **Contributing:** Brief — "Issues welcome at <link>" or 1 paragraph.
       - **License:** "MIT. See LICENSE."

    3. Verify:
       - File contains the literal `W = 0.4·C + 0.4·U + 0.2·E` formula (D-11 audience separation — the math IS in README, NOT in overview.md). Greppable: `grep -F "W = 0.4·C + 0.4·U + 0.2·E" README.md`.
       - File contains the engineer-audience sections (Development, Publishing, Contributing) that overview.md does NOT have.
       - File contains references to the new npm scripts (`check:size`, `publish:cezari`).
       - File contains the Fibonacci threshold table (greppable: `grep -F "0.5, 1, 2, 3, 5, 8, 13" README.md` — the 7 Fibonacci values inline somewhere).
  </action>
  <acceptance_criteria>
    - `README.md` exists.
    - File contains the W formula (greppable: `grep -F "W = 0.4·C + 0.4·U + 0.2·E" README.md`) — the engineer-audience math D-11 keeps out of overview.md.
    - File contains the Fibonacci values (greppable: `grep -F "0.5, 1, 2, 3, 5, 8, 13" README.md` OR equivalently a regex match — the 7 Fibonacci numbers should appear in the calculation explanation).
    - File contains the new npm script names (greppable: `grep -F "npm run check:size" README.md && grep -F "npm run publish:cezari" README.md`).
    - File contains a Development section AND a Publishing section (greppable: `grep -F "## Development" README.md && grep -F "## Publishing" README.md`).
    - File contains the Marketplace install URL (greppable: `grep -F "marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator" README.md`).
    - File at least 80 lines (`wc -l README.md` >= 80; the v1 polished README is ~120 lines per the outline).
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && grep -F "W = 0.4·C + 0.4·U + 0.2·E" README.md && grep -F "npm run check:size" README.md && grep -F "npm run publish:cezari" README.md && grep -F "## Development" README.md && grep -F "## Publishing" README.md && grep -F "marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator" README.md && [ "$(wc -l < README.md)" -ge 80 ]</automated>
  </verify>
  <done>README.md is rewritten to the v1 polished engineer-audience structure. The audience separation from overview.md is preserved (math is in README, not in overview).</done>
</task>

<task type="auto">
  <name>Task 4: Icon refinement (optional/best-effort per RESEARCH Open Q1)</name>
  <files>
    images/icon.png
    scripts/generate-icon.cjs
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Open Questions → Q1: Should the icon be refined for v1.0.0 or shipped as the transparent placeholder?" — recommendation is "Regenerate the icon"
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Assumptions Log → A5" — the current icon.png is a 143-byte transparent placeholder per the placeholder-generator script
    - scripts/generate-toolbar-icon.cjs (existing 16×16 calculator-glyph generator — pattern to scale up)
    - scripts/generate-placeholder-icon.cjs (existing transparent-pixel generator — what we're replacing)
  </read_first>
  <action>
    Per RESEARCH Open Q1 ("Recommendation: Regenerate the icon — 30 minutes to extend `generate-placeholder-icon.cjs` to draw a 128×128 calculator glyph"), do a best-effort icon refinement.

    Approach decision (Claude's discretion within RESEARCH guidance — choose one):

    **Option I (recommended for low cost):** Pure-Node PNG regeneration mirroring `scripts/generate-toolbar-icon.cjs`'s pattern at 128×128. Create `scripts/generate-icon.cjs` that emits a 128×128 PNG with a recognizable calculator glyph (rectangular body + 3×3 button grid + display strip on a colored background — say a subtle blue or gray). Run the script to overwrite `images/icon.png`. The output PNG should be at least 1 KB (vs. the current 143-byte transparent placeholder).

    **Option II (escape hatch if pure-Node PNG generation is hard):** Skip refinement. Document in the SUMMARY that the placeholder ships in 1.0.0 and that Plan 05-05's Task 7 visual confirmation will surface whether it's acceptable. RESEARCH §Pattern 5 / Pitfall 3 / Open Q1 acknowledge that Marketplace doesn't strictly reject blank icons — the worry is "looks unprofessional", which a real visual review can disposition.

    Decision rule:
    - If a working `scripts/generate-icon.cjs` can be produced in <20 minutes of Claude time, use Option I.
    - If pure-Node 128×128 PNG generation requires non-trivial dependency or library lookup, fall back to Option II and note in the SUMMARY.

    Verification per option:
    - **Option I:** `images/icon.png` is at least 1 KB (greppable via `[ "$(stat -c%s images/icon.png 2>/dev/null || stat -f%z images/icon.png)" -ge 1024 ]` — the cross-platform `stat` invocation is messy; alternative: `wc -c < images/icon.png` returns >= 1024). The new file is a valid PNG (the first 8 bytes are the PNG magic — `xxd images/icon.png | head -1` shows `89 50 4e 47`).
    - **Option II:** `images/icon.png` unchanged from current state (143 bytes — `wc -c < images/icon.png` returns ~143). This task short-circuits with a SUMMARY note.

    This task is NON-BLOCKING for downstream tasks (5, 6, 7). Tasks 5/6/7 proceed regardless of which option ran.

    Do NOT attempt to use external image-editing tools or check in non-Node assets. The repo's existing pattern is pure-Node generators (toolbar-icon.cjs, placeholder-icon.cjs); follow that pattern or skip cleanly.
  </action>
  <acceptance_criteria>
    - One of two outcomes is true:
      - **Option I taken:** `images/icon.png` is >= 1024 bytes; `scripts/generate-icon.cjs` exists; `npm run build && npm run check:size` still exits 0 (the icon size doesn't push us over 250 KB — the icon is a few KB at most).
      - **Option II taken:** `images/icon.png` unchanged (~143 bytes); SUMMARY documents the decision to defer; Plan 05-05 Task 7 visual-confirmation will surface whether the placeholder is acceptable.
    - In either case, `vss-extension.json` is unchanged (icon path stays `images/icon.png`).
    - Build still works (`npm run build && npm run check:size` exits 0).
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run check:size && test -f images/icon.png</automated>
  </verify>
  <done>Icon is either refined (Option I — `scripts/generate-icon.cjs` regenerates a recognizable 128×128 calculator glyph) or deferred (Option II — placeholder ships, Plan 05-05 visually inspects). Either way, the build is unaffected.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: User captures the two screenshots on cezari per RESEARCH §Pattern 5</name>
  <files>
    images/screenshots/screenshot-calculator-light.png
    images/screenshots/screenshot-calculator-dark.png
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pattern 5: Screenshot Capture on Windows" — verbatim procedure (~558–629)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 3: Marketplace screenshot dimensions don't match recommendation" — the 1366×768 padding step
    - .planning/phases/05-polish-marketplace-publish/05-CONTEXT.md D-8 (2 screenshots: light + dark; resolved-from-current-trio state)
  </read_first>
  <action>
    This is a human-action checkpoint. Claude cannot drive the cezari browser session, the host theme toggle, or the Chrome DevTools "Capture node screenshot" affordance. The user runs the procedure manually and saves two PNG files into `images/screenshots/`.

    The user is also free to defer this task to AFTER Plan 05-04's cross-process work if they prefer to capture screenshots from the freshly-set-up Agile or CMMI projects (visual variety) — but the recommended path is "capture on the existing cezari Scrum project, where the modal is already known to render correctly per Phase 4 verification."
  </action>
  <checkpoint>
    <type>human-action</type>
    <details>
      Steps for the user (per RESEARCH §Pattern 5):

      The cezari org already has the 0.2.5 VSIX installed (Phase 4 share). No new publish needed for the screenshot capture — the existing build is what's being marketed. Open work item PBI #4 or any PBI in the Cezari project.

      **For each theme (light, then dark):**

      1. Set the ADO host theme:
         - cezari.visualstudio.com → top-right user avatar → User settings → Theme → choose Light (then on the second pass, Dark).
         - The work item form should reflect the new theme immediately.

      2. Click the "Calculate Story Points" toolbar button on the open PBI. The modal opens.

      3. In the modal, set:
         - Complexity = **Hard**
         - Uncertainty = **Medium**
         - Effort = **Easy**

         This trio resolves to W=2.40, Raw SP=1.95, Final=2 SP. Visually the Calc panel will show all three rows (W, Raw SP, Final SP) plus the formula line — the most representative state per D-8.

      4. Open Chrome DevTools (F12 / Ctrl+Shift+I).

      5. In the Elements panel, locate the iframe element containing the modal (search for `<iframe>` near the host's dialog element). Right-click the iframe's `<body>` element in the DOM tree → "Capture node screenshot". Chrome saves a PNG to your Downloads folder.

         **Alternative (if "Capture node screenshot" is unavailable in your Chrome version):** use Windows Snipping Tool (Win+Shift+S) to capture just the modal's bounding rectangle. Crop later if needed.

      6. (Optional but recommended per RESEARCH §Pitfall 3) Pad/scale to 1366×768:
         - Open the captured PNG in Paint or any image editor.
         - Create a new 1366×768 canvas with a neutral background:
           - Light theme: light gray (#f5f5f5) or white background.
           - Dark theme: dark gray (#1f1f1f) or black background.
         - Paste the modal capture centered.
         - Save as PNG.
         - This step is OPTIONAL — Marketplace renders smaller PNGs too; the padding only matters if you want pixel-perfect listing-carousel sizing.

      7. Save the PNG files to the EXACT paths the manifest expects:
         - Light theme: `images/screenshots/screenshot-calculator-light.png`
         - Dark theme: `images/screenshots/screenshot-calculator-dark.png`

         The directory `images/screenshots/` doesn't exist yet — create it (`mkdir images/screenshots`).

      8. Verify file format:
         - Both files should be PNG (NOT JPEG — JPEG compression makes UI text fuzzy).
         - Both files should be at least 5 KB (typical UI PNG at modal size).

      9. Paste back into chat: just confirm "screenshots captured" + the file sizes (`wc -c images/screenshots/*.png` output) so Task 6 can sanity-check before re-publish.

      Resume keyword: `screenshots-captured`.
    </details>
    <awaiting>
      User confirms in chat that both PNG files exist at the exact expected paths with non-trivial size. Resume keyword: `screenshots-captured`.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>test -f images/screenshots/screenshot-calculator-light.png && test -f images/screenshots/screenshot-calculator-dark.png && [ "$(wc -c < images/screenshots/screenshot-calculator-light.png)" -ge 5120 ] && [ "$(wc -c < images/screenshots/screenshot-calculator-dark.png)" -ge 5120 ]</automated>
  </verify>
  <done>Both PNG screenshots exist on disk at the exact paths the manifest references. File sizes are non-trivial (>= 5 KB each).</done>
</task>

<task type="auto">
  <name>Task 6: Walk manifest version 0.2.5 → 0.2.6 + private re-publish to cezari for listing-asset validation</name>
  <files>
    vss-extension.json
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pattern 6 → publish-cezari.cjs" (the script Plan 05-02 created)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 4: VSIX missing the marketplace/ directory after manifest edit" (post-publish verification: `unzip -l ...vsix | grep -E 'overview|screenshots'`)
    - vss-extension.json (current version 0.2.5 from Task 1)
    - scripts/publish-cezari.cjs (the publish script Plan 05-02 created)
  </read_first>
  <action>
    1. Edit `vss-extension.json` to walk the version one patch: `"version": "0.2.5"` → `"version": "0.2.6"`. This is a one-line Edit:
       - old_string: `"version": "0.2.5"`
       - new_string: `"version": "0.2.6"`

    2. Run the full local gate chain to confirm everything is green before publishing:
       - `npm run typecheck && npm test && npm run build && npm run check:size` — exits 0.

    3. Run `npm run package` to produce a local VSIX (`tfx extension create`). The output filename is something like `TsezariMshvenieradzeExtensions.story-point-calculator-0.2.6.vsix` in the current directory (or `dist/` per the `--output-path dist/` flag in `package.json` scripts.package).

    4. Run Pitfall 4's verification command on the local VSIX before pushing it to Marketplace. The VSIX is a zip file:
       ```bash
       unzip -l TsezariMshvenieradzeExtensions.story-point-calculator-0.2.6.vsix | grep -E 'overview|screenshots'
       ```
       (On Windows, `unzip` may not be available — use `tar tf` (Windows 10+ has tar) or `Expand-Archive` in PowerShell, or open the .vsix in 7-Zip / WinRAR. The expected output lists:
       - `marketplace/overview.md`
       - `images/screenshots/screenshot-calculator-light.png`
       - `images/screenshots/screenshot-calculator-dark.png`

       If any of these are missing from the VSIX, STOP — the manifest's `files[]` array is wrong. Re-read RESEARCH §Pitfall 4 and fix the manifest.

    5. Publish privately to cezari: `npm run publish:cezari`.

       The script (created by Plan 05-02) loads `.env.local`, invokes tfx, and re-shares with cezari (`--share-with cezari`). Expected output: tfx prints "Published" + the version `0.2.6`. If the publish errors out with "Version number must increase":
       - The most likely cause is a previous Phase 4 cezari publish at 0.2.6 (the planner CONTEXT mentions Phase 4 ended at 0.2.5; if the developer manually republished at 0.2.6 in any context, the collision is real).
       - Fix: bump to 0.2.7 (or higher) in vss-extension.json, commit the bump, retry publish.

    6. Note the published version in the SUMMARY. The cezari org's installed extension auto-updates to the new version.

    7. Commit the manifest edits (Task 1's listing-fields delta + Task 6's version bump) along with the new files (overview.md, README.md, screenshots) as a single git commit. (See plan-level commit notes — Task 1's listing-fields plus the version walk are typically separate commits per RESEARCH §"Sequencing note on the public flip", but for Plan 05-03 the version walk is just the patch needed to publish; group with the listing-fields commit.) Suggested commit message:
       ```
       feat(05-03): add Marketplace listing assets — manifest delta, overview.md, README v1, screenshots, version 0.2.6
       ```
  </action>
  <acceptance_criteria>
    - `vss-extension.json` version is `"0.2.6"` (or higher if publish-loop required additional bumps) (greppable: `grep -E '"version":\s*"0\.2\.[6-9]"' vss-extension.json` returns 1 match — version walked from 0.2.5).
    - `npm run package` produces a `.vsix` file (greppable: `ls *.vsix dist/*.vsix 2>/dev/null | head -1` returns at least one path).
    - The VSIX includes `marketplace/overview.md` and both screenshot PNGs (greppable via the unzip/tar listing command per RESEARCH §Pitfall 4 — must show all 3 files).
    - `npm run publish:cezari` exits 0 with stdout indicating publish success (the user pastes the script's output into chat OR the executor parses tfx's output for "Published").
    - The user is informed of the published version so Task 7 can re-check the listing page.
  </acceptance_criteria>
  <verify>
    <automated>grep -E '"version":\s*"0\.2\.[6-9]"' vss-extension.json && npm run typecheck && npm test && npm run build && npm run check:size && npm run package</automated>
  </verify>
  <done>Manifest 0.2.6 (or higher) is published privately to cezari with the listing-asset bundle. The VSIX has been verified to include overview.md and both screenshots. The cezari org's install auto-updates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: User visually confirms the listing-page rendering on Marketplace BEFORE Wave 3 public flip</name>
  <files>(none — human-verify checkpoint; user inspects the Marketplace listing page on the publisher portal and reports visual findings)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 3: Marketplace screenshot dimensions don't match recommendation" + §"Pitfall 4: VSIX missing the marketplace/ directory" (the symptoms a Marketplace render reveals)
  </read_first>
  <action>
    This is a human-verify checkpoint. The user navigates to the publisher portal (or the private listing URL) and visually inspects how the listing renders with the Plan 05-03 assets. Plan 05-05 (Wave 3) cannot flip `public:true` until this confirmation is captured — a private-listing render is the only opportunity to catch screenshot-dimension issues, manifest-link 404s, or markdown rendering surprises before they become public.
  </action>
  <what-built>
    The user inspects the cezari-shared private extension's listing-page view at:
    - **Publisher manage page**: https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions → click the `story-point-calculator` extension → "View extension" or equivalent affordance.

    OR (alternate path):
    - **Direct listing URL** (private — accessible only to cezari org members and the publisher): something like `https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator` — this URL works for shared private extensions if the user is logged in to a Microsoft account that has access to the cezari org.

    Visual checks the user runs:
    1. **Description / overview tab**: the content of `marketplace/overview.md` renders as GFM (headings, tables, lists, links). The "Privacy and data handling" section is visible. The "Known limitations (v1)" bullets render. The screenshot inline references (`![Light theme](...)`) display the actual PNG (NOT a broken-image icon).

    2. **Screenshots carousel** (above or beside the description): both `screenshot-calculator-light.png` and `screenshot-calculator-dark.png` render. Image quality is acceptable (text is readable; not blurry from over-scaling per Pitfall 3).

    3. **Icon tile** (top-left of the listing card): the 128×128 icon renders. If Task 4 ran Option I, the icon shows the calculator glyph; if Task 4 deferred (Option II), the icon is blank/transparent — note in chat whether this is acceptable.

    4. **Metadata strip**: the `version` shows `0.2.6` (or whatever Task 6 published). The `tags` chips include "story points", "estimation", "scrum", "agile", "cmmi", "fibonacci".

    5. **Links** (in the listing's right-side metadata panel or "Get started" button):
       - "Get started" → clicks through to the GitHub README's `#usage` anchor.
       - "Support" → clicks through to `https://github.com/tsmshvenieradze/StoryPointExtension/issues`.
       - "License" → clicks through to the LICENSE in the GitHub repo.
       - "Repository" → clicks through to `https://github.com/tsmshvenieradze/StoryPointExtension`.

    6. **Categories**: "Azure Boards" is the category chip.

    For each visible issue (e.g., a 404 link, a broken image, a markdown rendering oddity, a too-small screenshot), the user reports it back so a follow-up fix can land before Wave 3.
  </what-built>
  <how-to-verify>
    1. Open the publisher portal: https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions.
    2. Find the `story-point-calculator` row, click "View" (or equivalent affordance) to open the listing page in preview mode.
    3. Walk through the 6 visual checks above. Note PASS/FAIL per check.
    4. Capture a screenshot of the listing page (any quality — for the SUMMARY's record).
    5. Paste back into chat:
       - Per-check PASS / FAIL with one-line note.
       - Listing-page screenshot URL or description.
       - Any remediation needed (e.g., "screenshots are blurry — re-pad to 1366×768"; "support link 404s — check repo URL").

    Resume keyword: `listing-page-confirmed` (if all PASS) OR `listing-page-issues-found` (if remediation needed — Claude will respond with a follow-up patch task).
  </how-to-verify>
  <resume-signal>Type `listing-page-confirmed` (all 6 checks pass) or `listing-page-issues-found` (paste per-check verdicts; Claude will land a follow-up patch task within Plan 05-03's scope before Wave 2 starts)</resume-signal>
  <verify>
    <automated>MISSING — human-verify gate; user-supplied screenshot + per-check verdicts are the evidence; no automated check.</automated>
  </verify>
  <done>The Marketplace listing renders with the Plan 05-03 assets without surprises. The user has explicitly confirmed (or surfaced remediation items for) each of the 6 visual checks. Wave 3 (Plan 05-05) can confidently flip `public:true` knowing the listing will render correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `marketplace/overview.md` (developer-authored GFM) → Marketplace renderer | The renderer outputs HTML on a public listing page. The content is treated as trusted (publisher-authored); GFM's standard sanitization applies. |
| Screenshots PNG → Marketplace listing carousel | The PNGs are user-controlled binary content rendered as `<img>` tags on the listing page. Marketplace strips EXIF/metadata; no script content is possible in PNG. |
| Manifest `links.*.uri` → user click | Each link URL is rendered as an outbound link on the listing. Wrong URLs lead users to 404s or unrelated repos — surfaced by Task 7's link-walk check. |
| Private cezari publish → public flip prep | The private listing render is the SAME code path as a public listing render; what cezari sees is what the public will see. Task 7 is the validation gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | I (Information disclosure) | Repository URL `https://github.com/tsmshvenieradze/StoryPointExtension` is intentionally public; bakes the GitHub username into the listing | accept | The publisher slug already contains the user's name (`TsezariMshvenieradzeExtensions`); the GitHub username is by design public. |
| T-05-03-02 | T (Tampering) | A wrong manifest path (e.g., `marketplace/overview.md` typo) results in an empty listing description | mitigate | Pitfall 4 verification command (`unzip -l ... \| grep overview`) is in Task 6. Task 7 visually confirms the description renders. |
| T-05-03-03 | T (Tampering) | The wrong tags / categories make the extension undiscoverable on Marketplace | mitigate | Tags + categories are JSON-edited (not free-text) and verified via greppable acceptance criteria in Task 1. |
| T-05-03-04 | I (Information disclosure) | Screenshots inadvertently expose cezari work item content (titles, IDs, comments) that reveals private project info | mitigate | The capture procedure (Task 5) explicitly says use a "PBI with no current Story Points value" — generic test work item. The user is in control of what work item is open at capture time; the planner cannot enforce title-blanking but can flag the concern. The cezari project is the developer's own playground; risk is low. |
| T-05-03-05 | E (Elevation of privilege) | The README's "publish publicly" instructions could be followed by a future contributor with write access who isn't aware of the publisher-verification gate | mitigate | The README's Publishing section step 1 is "Confirm publisher verified at <portal URL>" — explicit verification step before flip. The README documents the dependency. |
</threat_model>

<verification>
- `vss-extension.json` has the listing-asset fields (content, links, repository, screenshots, expanded tags, marketplace files entry); version walked one patch (0.2.5 → 0.2.6); public still false.
- `marketplace/overview.md` exists with the GFM listing detail page covering D-1, D-9, D-10, D-11, PKG-05 SC #2 privacy.
- `README.md` is rewritten to the v1 polished engineer-audience structure with the formula math (audience separation per Pitfall 6).
- Two screenshot PNGs exist on disk at the exact paths the manifest references.
- Icon either refined (Task 4 Option I) or deferred (Option II) with SUMMARY note.
- A private re-publish to cezari at 0.2.6 succeeded with the listing-asset bundle.
- The user has visually confirmed the listing page renders correctly on Marketplace BEFORE Wave 3 commits the public flip.
</verification>

<success_criteria>
- PKG-05 (description, screenshots light+dark, formula explanation, privacy/data-handling, 128×128 icon) is met:
  - Description: `marketplace/overview.md` (GFM, terse + technical per D-9).
  - Screenshots light + dark: `images/screenshots/screenshot-calculator-{light,dark}.png` per D-8.
  - Formula explanation: in overview.md (user-facing per D-11) AND README.md (engineer-facing math).
  - Privacy/data-handling statement: bullet block in overview.md per PKG-05 SC #2.
  - 128×128 icon: `images/icon.png` (refined per Task 4 Option I or deferred per Option II).
- The Marketplace listing renders correctly with the Plan 05-03 assets, validated empirically via private re-publish + user visual inspection.
- Plan 05-05 (Wave 3) can land the public flip knowing the listing UX is already verified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-marketplace-publish/05-03-SUMMARY.md` per the standard template. Capture:
- Files created (overview.md, README.md, 2 screenshots, optional generate-icon.cjs).
- The icon refinement choice (Option I or II) with rationale.
- The private re-publish version (0.2.6 or higher if collision-bumped).
- The user's per-check verdict from Task 7 (6 visual checks).
- Any remediation done in response to Task 7 findings (e.g., "support link 404 → fixed by updating links.support to <correct URL>").
- A confirmation that Wave 3 (Plan 05-05) is unblocked from the listing-asset side.
</output>
