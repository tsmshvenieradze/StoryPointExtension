---
phase: 05-polish-marketplace-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
autonomous: false
requirements: [PKG-06]
must_haves:
  truths:
    - "The verification status of the TsezariMshvenieradzeExtensions publisher is empirically known (verified | request-filed | unverified-no-action) before any public-publish work begins"
    - "If the publisher is unverified, a verification request has been filed via the Marketplace publisher portal and the request timestamp is recorded so Wave 3 can confirm the days-long external dependency cleared"
    - "The 05-VERIFICATION.md file exists with a populated `## Publisher Verification Gate` section that Wave 3 (Plan 05-05) reads to confirm before flipping public:true"
    - "The user, not Claude, has visited the publisher portal and reported back the literal verification-status string visible on the page (no inference from publish history)"
  artifacts:
    - path: ".planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md"
      provides: "Phase 5 verification record skeleton with the Publisher Verification Gate section filled in (status, evidence, request-filed timestamp if applicable)"
      contains: "## Publisher Verification Gate"
      min_lines: 30
  key_links:
    - from: ".planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md ## Publisher Verification Gate"
      to: ".planning/phases/05-polish-marketplace-publish/05-05-PLAN.md (Wave 3 public publish task — gate condition)"
      via: "Plan 05-05 reads the verification status string from this section before authorizing the public:true flip commit"
      pattern: "Verification status:\\s*(verified|request-filed|unverified)"
---

<objective>
Establish the Phase 5 verification record skeleton (`05-VERIFICATION.md`) and resolve the publisher-verification gate that blocks Wave 3's public publish. Per RESEARCH §Pitfall 1 / Finding 3, going `public: true` requires the publisher account `TsezariMshvenieradzeExtensions` be verified by Microsoft — verification is a publisher-account property, not an extension property, and is the only step in Phase 5 with a days-long external waiting dependency. This plan files the verification request (or confirms verification already exists) as the first commit of Phase 5 so the long pole starts running in parallel with Waves 1+2.

Purpose: PKG-06 is "Publisher account registered, verified, and confirmed before the first public publish." This plan delivers the "verified, and confirmed" half — registration is already done (publisher exists; Phases 0–4 published 5+ private VSIXes). Without this gate cleared, Plan 05-05 (public publish) cannot land.

Output: A populated `## Publisher Verification Gate` section in `05-VERIFICATION.md` that Plan 05-05 references as a precondition. If the publisher is already verified, the gate clears immediately; if not, a verification request is filed and the request timestamp is recorded so Wave 3 can confirm clearance before the public flip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-polish-marketplace-publish/05-CONTEXT.md
@.planning/phases/05-polish-marketplace-publish/05-RESEARCH.md
@vss-extension.json

<interfaces>
<!-- Plan 05-01 reads from CONTEXT.md/RESEARCH.md; produces the verification skeleton. -->

Per RESEARCH §Pitfall 1 + Finding 3, publisher verification gates `public:true`:
- Verbatim from Microsoft Learn 2026-04 (manifest reference): *"If your publisher is verified, you can make your extension public by setting the Public flag in your extension manifest"*.
- The publisher `TsezariMshvenieradzeExtensions` exists (proven by 5+ private publishes in Phases 2–4). Verification is a SEPARATE state property visible on the publisher's management page.
- Verification can take days. File the request as one of the first Phase 5 commits.

Publisher portal URL (verbatim from RESEARCH):
  https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions

The `05-VERIFICATION.md` file does NOT yet exist (Phase 5 is just starting). This plan creates it in skeleton form (one populated section + headers for sections later plans fill: `## Cross-Process Smoke` from Plan 05-04, `## Public Publish Record` from Plan 05-05).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User confirms publisher verification status at the portal (and files request if unverified)</name>
  <files>(none — human-action checkpoint; user inspects the publisher portal page and reports back the literal status string)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §Pitfall 1 (verification request flow)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §Finding 3 (verbatim Microsoft Learn quote on public:true gating)
    - vss-extension.json (publisher field — confirm it reads `TsezariMshvenieradzeExtensions`)
  </read_first>
  <action>
    This is a human-action checkpoint. Claude cannot inspect the Marketplace management portal — it requires authenticated browser access to a Microsoft account. The user navigates to the portal, reads the publisher's verification status, and (if unverified) clicks the "Request verification" affordance. The user pastes the verbatim status string back into chat; Task 2 transcribes it into 05-VERIFICATION.md.
  </action>
  <checkpoint>
    <type>human-action</type>
    <details>
      Steps for the user:

      1. Open https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions in a logged-in browser (the same Microsoft account that owns the publisher and that has published the 0.1.x and 0.2.x VSIXes).

      2. Find the publisher's verification status. Common locations: the publisher details panel at the top of the page, OR a "Profile" / "Settings" tab showing a "Verified ✓" badge or a "Request verification" button. The exact label depends on the current Marketplace UI version (the portal evolves; status MAY appear as "Verified", "Unverified", "Verification pending", or similar).

      3. Capture the verbatim string visible on the page describing the verification state. Examples of what you might see:
         - "Verified" or "Verified publisher" (badge / label) → proceed to Task 2 with status = `verified`.
         - "Request verification" button visible (no Verified badge) → publisher is currently unverified. CLICK the button and follow the verification request flow (Microsoft typically asks for publisher identity confirmation — domain ownership, organization name, contact info). Submit the request; capture the date/time of submission. Return to chat with status = `request-filed` and the submission timestamp.
         - "Verification pending" / "Verification requested {date}" → a request is already in flight from a previous attempt. Capture the date and return with status = `request-filed-prior` and the existing pending-since date.

      4. If the portal UI does not surface verification status anywhere visible: try the publisher's Settings tab, or open the "Profile" affordance. If still unclear, capture a screenshot of what IS visible and paste it into chat — Claude can help locate the relevant control.

      5. Paste back into chat:
         - The verbatim status string seen on the portal.
         - If a request was filed: the submission timestamp (your local time + zone is fine).
         - One sentence describing where on the page the status was found (so this plan documents the empirical UI location for future revisits).

      The portal access is the entire blocker — Claude cannot proceed to Task 2 without your report-back.
    </details>
    <awaiting>
      User pastes back: (a) verbatim verification status string, (b) submission timestamp if a new request was filed, (c) one-sentence note on where on the page the status was found. Resume keyword: `verification-status-captured`.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>MISSING — human-action gate; user-supplied evidence is consumed by Task 2.</automated>
  </verify>
  <done>The publisher's verification status is empirically known (literal string captured from the portal). If unverified, a request has been filed and the submission timestamp is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Create 05-VERIFICATION.md skeleton with the Publisher Verification Gate section populated</name>
  <files>.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md §"Spike Results" + §"Phase 4 Verdict" (formatting precedent — section headers, evidence-line formatting, verdict-line formatting)
    - The user-pasted Task 1 verification status report (status string, submission timestamp if applicable, page-location note)
    - .planning/phases/05-polish-marketplace-publish/05-CONTEXT.md (D-2 / D-5 / D-6 / D-8 — wave-2 and wave-3 sections referenced as `(populated by Plan 05-XX)` placeholders)
  </read_first>
  <action>
    Create `.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` using the Write tool. The file is the canonical record for Phase 5 empirical evidence; later plans (05-04, 05-05) append their sections.

    Use this exact structure:

    ```markdown
    # Phase 5 Verification — Polish & Marketplace Publish

    **Started:** <today's date YYYY-MM-DD>
    **Org:** cezari.visualstudio.com (Scrum, Agile, CMMI projects per D-5)
    **Run by:** Tsezari (cezo777@gmail.com)
    **Status:** in-progress

    ## Publisher Verification Gate (Plan 05-01 — PKG-06)

    **Publisher:** TsezariMshvenieradzeExtensions
    **Portal URL:** https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions
    **Checked:** <today's date YYYY-MM-DD>
    **Checked by:** Tsezari (manual portal inspection; Claude cannot access authenticated portal pages)

    ### Verification status

    **Verification status:** <verified | request-filed | request-filed-prior | unverified>

    **Verbatim portal evidence:**

    > <paste user-supplied verbatim status string from Task 1>

    **Page location:** <user-supplied one-sentence note on where on the portal page the status was found>

    **Request submission timestamp** (if `request-filed` or `request-filed-prior`): <ISO-8601 timestamp of submission, OR "n/a" if status is verified>

    ### Implication for Wave 3 (Plan 05-05)

    <if status = verified>
    Plan 05-05 may flip `public: true` and run `tfx extension publish` immediately upon Wave 1+2 completion. No external waiting dependency.
    </if>
    <if status = request-filed | request-filed-prior>
    Plan 05-05 is BLOCKED on Microsoft's verification approval. The user must re-check the portal at the start of Wave 3 and confirm `Verified` before the public flip commit lands. If the request is denied or stalled past Phase 5's window, the public publish moves to a later milestone — Wave 1+2 work (CI gate, listing assets, cross-process smoke) is still independently valuable.
    </if>
    <if status = unverified>
    The user did NOT file a verification request in Task 1 (active decision to ship as a private extension only). Plan 05-05 is permanently blocked on `public: true`; only `--share-with` private re-publishes are available. ROADMAP Phase 5 success criteria #4 (public listing on at least 2 different ADO orgs) cannot be cleared. Discuss with developer whether Phase 5 ships as private-only or whether to re-open the verification decision.
    </if>

    ## Cross-Process Smoke (Plan 05-04 — PKG-04 / PKG-07)

    *(Populated by Plan 05-04 — Agile + CMMI smoke verdicts on cezari per D-5/D-6.)*

    ## Public Publish Record (Plan 05-05 — PKG-07 / Phase 5 close)

    *(Populated by Plan 05-05 — version walk 0.2.5 → 1.0.0, public:true flip commit reference, post-publish 1.0.0 re-verification on cezari.)*

    ## Phase 5 Verdict

    *(Populated by Plan 05-05 close.)*
    ```

    Substitute the four `<if status = ...>` branches with ONLY the one matching the user's reported status. Do not include the un-matched branches in the final file (this is an executable record, not a template).

    Write the file via the Write tool — never via heredoc.

    Commit with: `docs(05-01): record publisher verification status — <status>`.
  </action>
  <acceptance_criteria>
    - File exists: `.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` (greppable: `test -f .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md`).
    - File contains the literal heading `## Publisher Verification Gate` (greppable: `grep -F "## Publisher Verification Gate" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` returns 1 match).
    - File contains a literal `Verification status:` line followed by one of `verified`, `request-filed`, `request-filed-prior`, `unverified` (greppable: `grep -E "Verification status:\s+(verified|request-filed|request-filed-prior|unverified)" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` returns at least 1 match).
    - File contains placeholder headings for Plan 05-04 and Plan 05-05's later additions (greppable: `grep -F "## Cross-Process Smoke" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` AND `grep -F "## Public Publish Record" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` each return 1 match).
    - File is committed to git with a `docs(05-01):` commit message (greppable: `git log --oneline -1` includes `docs(05-01)`).
    - The file embeds the user-supplied verbatim status string as a blockquote (`>` prefix); no fabrication.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "## Publisher Verification Gate" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -E "Verification status:\s+(verified|request-filed|request-filed-prior|unverified)" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "## Cross-Process Smoke" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "## Public Publish Record" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md</automated>
  </verify>
  <done>05-VERIFICATION.md exists with the Publisher Verification Gate section populated from the user's portal inspection. Wave 3 (Plan 05-05) has a single, greppable record of the gate's state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Marketplace publisher portal | Authenticated browser session; the portal returns publisher identity + verification state. Claude does not (and cannot) inspect this surface. |
| User report → 05-VERIFICATION.md | The user pastes the verbatim status string into chat; Claude transcribes into the verification record. The fidelity of the transcription is the integrity boundary. |
| 05-VERIFICATION.md → Plan 05-05 | Plan 05-05 reads the literal `Verification status:` line and gates the public-flip commit on it. A wrong status here = a wrong public-flip decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I (Information disclosure) | Publisher portal URL embedded in 05-VERIFICATION.md exposes the publisher slug `tsezarimshvenieradzeextensions` | accept | The publisher is intentionally public-Marketplace-facing; the slug is part of the public listing URL once the extension goes public. No PII beyond the publisher's chosen identity. |
| T-05-01-02 | T (Tampering) | A wrong `Verification status:` value (e.g., transcribed `verified` when actual is `unverified`) leads Plan 05-05 to flip `public:true` against an unverified publisher | mitigate | Acceptance criterion requires the verbatim portal-evidence blockquote — Plan 05-05's gate task re-reads BOTH the status line AND the blockquote and asks the user to re-confirm at portal-check time before the public-flip commit. The verification status string is also re-checked in Plan 05-05's first task; this is a defense-in-depth check, not a single-point-of-truth claim. |
| T-05-01-03 | R (Repudiation) | Without an audit record, a denied verification request might be invisible until Wave 3 attempts the public flip and fails | mitigate | The `Request submission timestamp` field, when populated, gives Wave 3 a clear "expected response by" anchor. Microsoft's verification turnaround is days; if the timestamp is >7 days old at Wave 3 check time without status flip, escalate via the portal's contact form. |
</threat_model>

<verification>
- 05-VERIFICATION.md exists with the Publisher Verification Gate section populated from empirical portal evidence (user-supplied verbatim status string).
- The verification status is one of the four enum values: `verified`, `request-filed`, `request-filed-prior`, `unverified`.
- The file contains placeholder section headers for Plan 05-04 and Plan 05-05 to fill.
- The Wave 3 implication is explicitly written so Plan 05-05 has a single record to read.
</verification>

<success_criteria>
- PKG-06 has empirical evidence of the publisher's current verification state (not inferred from Phase 4 publish history).
- If verification is needed, the request is filed; Wave 3 has a deterministic precondition to re-check before flipping public:true.
- 05-VERIFICATION.md is committed and ready for Plans 05-04 and 05-05 to append their sections.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-marketplace-publish/05-01-SUMMARY.md` per the standard template. Capture: the captured verification status string, the request submission timestamp (if applicable), the wave-3 implication branch chosen, and any portal-UI observations the next-time inspector should know about.
</output>
