---
phase: 05-polish-marketplace-publish
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - vss-extension.json
  - package.json
  - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [PKG-02, PKG-03, PKG-04, PKG-05, PKG-06, PKG-07]
must_haves:
  truths:
    - "Wave 3 cannot start until ALL of: Plan 05-01 publisher verification status is `verified` (re-confirmed at the start of this plan, not just trusted from the Plan 05-01 timestamp), Plan 05-02 CI workflow + bundle gate are green on master, Plan 05-03 listing assets are committed and Task 7 visual confirmation captured, Plan 05-04 cross-process smoke evidence is recorded with PKG-04/PKG-07 PARTIAL verdicts. Each precondition is checked at the start of Task 1 below."
    - "Manifest version walks 0.2.x → 1.0.0 in a single deliberate commit per RESEARCH §Versioning Recommendation; package.json version syncs from 0.1.0 → 1.0.0 in the same commit (no more drift)"
    - "`vss-extension.json` `public` flips from false → true in the SAME commit as the version bump (the commit IS the public flip — easy to revert if Marketplace rejects)"
    - "Public publish via `npm run publish:public` (Plan 05-02 script — passes through `--public` flag; tfx reads `public:true` from manifest regardless) succeeds; Marketplace returns the new public listing URL `https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator`; the listing is visible to anonymous users (verified by opening in an incognito browser or by a non-cezari user)"
    - "Post-publish 1.0.0 re-verification on cezari (or any verified org) executes the smoke (open modal → trio → Apply) on the published 1.0.0 build to confirm the public version behaves identically to the 0.2.x cezari-verified build per RESEARCH Open Q4 recommendation"
    - "REQUIREMENTS.md PKG-02..07 checkboxes flip from `[ ]` to `[x]` (PKG-04, PKG-07 marked with PARTIAL footnote referencing 05-VERIFICATION.md PKG-04/07 verdicts); ROADMAP.md Phase 5 row is `[x]` with completion date; STATE.md reflects milestone v1.0 close (5/5 phases complete; 100% progress)"
  artifacts:
    - path: "vss-extension.json"
      provides: "Public Marketplace listing manifest at version 1.0.0 with public:true (the closing-state manifest)"
      contains: "\"version\": \"1.0.0\""
    - path: "package.json"
      provides: "Internal package version synced to 1.0.0 (drift from manifest closed)"
      contains: "\"version\": \"1.0.0\""
    - path: ".planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md"
      provides: "## Public Publish Record + ## Phase 5 Verdict sections populated; the canonical record for the public publish moment"
      contains: "## Public Publish Record"
  key_links:
    - from: "vss-extension.json public:true commit"
      to: "Marketplace public listing"
      via: "tfx extension publish reads public:true from the manifest and registers the extension publicly"
      pattern: "\"public\":\\s*true"
    - from: ".planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md ## Phase 5 Verdict"
      to: ".planning/REQUIREMENTS.md PKG-02..07 + .planning/ROADMAP.md Phase 5 row"
      via: "Phase 5 close commits flip the checkboxes only after the verification record sign-off"
      pattern: "PKG-0[2-7].*Phase 5.*Complete"
---

<objective>
Close Phase 5 by performing the public Marketplace publish: bump manifest version 0.2.x → 1.0.0, sync package.json from 0.1.0 → 1.0.0, flip `public: false → true`, run `npm run publish:public`, verify the Marketplace listing is publicly accessible, run the post-publish 1.0.0 re-verification smoke on cezari, and update all cross-cutting documents (REQUIREMENTS.md PKG-02..07 checkboxes, ROADMAP.md Phase 5 row, STATE.md milestone close, 05-VERIFICATION.md ## Public Publish Record + ## Phase 5 Verdict).

Purpose: Phase 5 ROADMAP success criterion #4 — "The extension is published `public:true` on Visual Studio Marketplace under the verified GPIH/TsezariMshvenieradzeExtensions publisher with exactly the `vso.work_write` scope (no scope changes from Phase 0), and an end-to-end install + Apply succeeds on at least two different ADO organizations — one running the Agile process, one running CMMI." With Wave 1+2 having delivered the listing assets, the build infrastructure, the publisher verification gate clearance, and the cross-process empirical evidence, Wave 3 is the irreversible-ish "ship it" moment. The plan front-loads precondition re-checks (publisher verification re-confirmed live, not from a stale Plan 05-01 timestamp) and back-loads the doc updates so the chain `code → publish → verify → close` is cleanly auditable.

Output: Public 1.0.0 listing live on Marketplace; cross-cutting docs all reflect Phase 5 closed; the v1.0 milestone is complete; STATE.md reflects 5/5 phases (100%).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-polish-marketplace-publish/05-CONTEXT.md
@.planning/phases/05-polish-marketplace-publish/05-RESEARCH.md
@.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
@vss-extension.json
@package.json

<interfaces>
<!-- Plan 05-05 reads from all prior Phase 5 plans' outputs and produces the closing artifacts. -->

**Wave 3 preconditions (from prior plans):**
- Plan 05-01 → `05-VERIFICATION.md ## Publisher Verification Gate` exists; verification status is one of `verified | request-filed | request-filed-prior | unverified`. **Wave 3 BLOCKED unless status is `verified`.** If still `request-filed`, Task 1 of this plan re-checks the portal; if Microsoft has approved since Plan 05-01, status flips to `verified` and Wave 3 proceeds. Otherwise Wave 3 holds.
- Plan 05-02 → `.github/workflows/ci.yml`, `scripts/check-bundle-size.cjs`, `scripts/publish-cezari.cjs` exist; `package.json` exposes `check:size`, `publish:cezari`, `publish:public` scripts.
- Plan 05-03 → `vss-extension.json` has listing fields populated (content, links, repository, screenshots, expanded tags); `marketplace/overview.md` exists; `README.md` is the v1 polished version; `images/screenshots/screenshot-calculator-{light,dark}.png` exist; the Plan 05-03 Task 7 visual confirmation cleared. The current version is 0.2.6 (or higher if Plan 05-04 fix-backs walked it further); `public` is still `false`.
- Plan 05-04 → `05-VERIFICATION.md ## Cross-Process Smoke` is populated with Agile + CMMI evidence; PKG-04 / PKG-07 PARTIAL verdicts are explicit.

**Versioning discipline (from RESEARCH §Versioning Recommendation):**
- Manifest version walks 0.2.x → 1.0.0 in a single deliberate commit at the public-publish moment.
- `package.json` syncs from 0.1.0 → 1.0.0 in the SAME commit (closing the long-standing drift documented in CONTEXT D-3 Claude's Discretion).
- `--rev-version` auto-bump is NOT used; the new `publish:public` script (Plan 05-02) reads version from the committed manifest.

**Sequence (per RESEARCH §Pattern 3 "Sequencing note on the public flip"):**
- Commit A (PRECONDITION CHECKS): re-verify publisher verification status; abort if not verified.
- Commit B (THE FLIP): version 0.2.x → 1.0.0 + package.json 0.1.0 → 1.0.0 + public false → true. ONE commit. The smallest possible diff for the most consequential change in Phase 5.
- Commit C (PUBLISH): no commit — the `npm run publish:public` invocation is logged in 05-VERIFICATION.md after success.
- Commit D (RE-VERIFICATION): the post-publish 1.0.0 cezari smoke evidence is recorded in 05-VERIFICATION.md `## Public Publish Record`; commit message `docs(05-05): record public publish + 1.0.0 re-verification on cezari`.
- Commit E (CLOSE): REQUIREMENTS.md + ROADMAP.md + STATE.md updates in one atomic commit; `docs(05-05): close Phase 5 and v1.0 milestone — PKG-02..07 [x]; <verdict>`.

**Phase 4 close-pattern precedent (Plan 04-06 Task 5):** REQUIREMENTS.md flip rules, ROADMAP.md Plans block format, STATE.md frontmatter+body update list. Phase 5 follows the same structural template; only the requirement IDs and verdict text change.

**1.0.0 re-verification justification (RESEARCH Open Q4 recommendation):** "Re-verify on 1.0.0 anyway. The cost is ~5 minutes... The upside is 'we know the public listing's version actually works on a real org.'" The smoke is the same C=Hard, U=Medium, E=Easy → SP=2 trio used in Phase 4 + Plan 05-03 + Plan 05-04 — keeps cross-version comparisons clean.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User re-confirms publisher verification status at the portal (Wave 3 gate)</name>
  <files>(none — human-action checkpoint; user re-checks the publisher portal page)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md ## Publisher Verification Gate (Plan 05-01's recorded status + submission timestamp if applicable)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pitfall 2: Public flip lands on master without the publisher being verified" (rationale for re-checking immediately before the flip, not relying on Plan 05-01's stale timestamp)
  </read_first>
  <action>
    This is a human-action checkpoint. The publisher verification status from Plan 05-01 may be days old. Microsoft's verification turnaround is typically days; this task re-checks the portal at the literal moment of Wave 3 to confirm verification is still in effect (or has flipped from `request-filed` → `verified`).
  </action>
  <checkpoint>
    <type>human-action</type>
    <details>
      Steps for the user:

      1. Re-open https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions in the logged-in browser.

      2. Find the publisher's verification status (same location as Plan 05-01).

      3. Compare the literal status string to Plan 05-01's recorded status (in `.planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md ## Publisher Verification Gate`):

         **Outcome paths:**

         - **Plan 05-01 was `verified` AND it's still `verified`:** Wave 3 is GO. Proceed to Task 2. Paste back: `verification-still-verified`.

         - **Plan 05-01 was `request-filed` AND it's now `verified`:** Microsoft approved the request during Wave 1+2 work. Wave 3 is GO. Paste back: `verification-now-verified` + the approval timestamp if visible.

         - **Plan 05-01 was `request-filed` AND it's still `request-filed`:** Microsoft has not approved yet. Wave 3 is BLOCKED. Options:
           - (a) Wait. Phase 5 holds until verification clears. Plan 05-05 resumes when status flips. Paste back: `verification-still-pending` and Claude pauses Wave 3.
           - (b) Re-submit the verification request if the portal indicates the prior request has expired or been denied. Paste back: `verification-resubmitted` + new submission timestamp; Claude pauses Wave 3.

         - **Plan 05-01 was `unverified` (active decision to ship private only):** Wave 3 IS NOT EXECUTING per the user's earlier decision. This task should NOT have started. If reached, the orchestrator has a bug; abort and discuss with developer.

         - **Plan 05-01 was `verified` AND it's now `unverified` (de-verification):** Microsoft has revoked verification. Rare but possible. Paste back: `verification-revoked`; investigate via the portal's contact form before any further action. Wave 3 is BLOCKED.

      4. Update `05-VERIFICATION.md ## Publisher Verification Gate` if the status changed since Plan 05-01:
         - Add a line under the existing `**Verification status:**` line: `**Verification status (re-checked at Wave 3 / Plan 05-05 start, <today>):** verified` (or whichever value).
         - This is a small Edit — don't rewrite the whole section, just append the re-check line.

      5. The user pastes back the resume keyword based on the outcome above.

      Resume keyword (one of): `verification-still-verified`, `verification-now-verified`, `verification-still-pending`, `verification-resubmitted`, `verification-revoked`.
    </details>
    <awaiting>
      User reports current verification status. If verified (still or now), Wave 3 proceeds to Task 2. If still pending or revoked, Wave 3 holds and the developer decides whether to defer Phase 5 close.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>MISSING — human-action gate; user-supplied portal-status report is the evidence.</automated>
  </verify>
  <done>The publisher verification status is `verified` at Wave 3 start (re-confirmed live, not relying on a possibly stale Plan 05-01 timestamp). 05-VERIFICATION.md is updated with the re-check line. The path to public publish is unblocked from the verification side.</done>
</task>

<task type="auto">
  <name>Task 2: The flip — version 0.2.x → 1.0.0 + package.json sync + public:true (one atomic commit)</name>
  <files>
    vss-extension.json
    package.json
  </files>
  <read_first>
    - vss-extension.json (current state — version 0.2.x from Plan 05-03/05-04; public:false; listing fields populated)
    - package.json (current state — version 0.1.0; per Plan 05-02 also has check:size, publish:cezari, publish:public scripts)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Versioning Recommendation (Claude's Discretion)" — bump 0.2.5 → 1.0.0; sync package.json; drop --rev-version
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pattern 3 → Sequencing note on the public flip" — the two-commit split (listing fields earlier; flip + bump as one commit now)
  </read_first>
  <action>
    The public flip is the single highest-stakes change in Phase 5. All three edits land in ONE commit so the diff is small, reviewable, and revertable.

    1. Run the full local gate chain to confirm the codebase is green BEFORE the flip:
       ```
       npm run typecheck && npm test && npm run build && npm run check:size
       ```
       Must exit 0. If anything fails, STOP — do not commit the flip on a broken build.

    2. Edit `vss-extension.json` to land TWO changes simultaneously (use the Edit tool with two separate exact-match calls, OR a single Write call rewriting the file):
       - **Change A — version bump:** `"version": "0.2.X"` → `"version": "1.0.0"` (where 0.2.X is whatever Plan 05-03/05-04 left it at — likely 0.2.6 or 0.2.7 if any fix-back walks happened).
         - First, read the current version from the file. Use `grep -E '"version":\s*"\d+\.\d+\.\d+"' vss-extension.json` or read the file and extract the literal string.
         - Then Edit with the exact-match old_string: `"version": "<current 0.2.X value>"` → new_string: `"version": "1.0.0"`.
       - **Change B — public flip:** `"public": false` → `"public": true`. Edit with exact-match: `"public": false` → `"public": true`.

    3. Edit `package.json` for the version sync:
       - old_string: `"version": "0.1.0"`
       - new_string: `"version": "1.0.0"`

    4. Verify the post-edit state:
       - `vss-extension.json` parses as JSON (`node -e "JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'))"` exits 0).
       - `vss-extension.json` `version` is `"1.0.0"` (greppable: `grep -F '"version": "1.0.0"' vss-extension.json`).
       - `vss-extension.json` `public` is `true` (greppable: `grep -F '"public": true' vss-extension.json`).
       - `package.json` `version` is `"1.0.0"` (greppable: `grep -F '"version": "1.0.0"' package.json`).
       - All earlier listing fields from Plan 05-03 are still present (greppable: `grep -F "marketplace/overview.md" vss-extension.json && grep -F "screenshot-calculator-light.png" vss-extension.json` — sanity check that no Plan 05-03 fields were lost in the edit).

    5. Run the full gate chain AGAIN to confirm the version + public bumps don't break anything:
       ```
       npm run typecheck && npm test && npm run build && npm run check:size
       ```
       Must exit 0.

    6. Commit (do NOT publish yet — Task 3 publishes):
       ```
       feat(05-05): bump to 1.0.0 and flip public:true for Marketplace launch
       ```
       The commit body should reference Plan 05-01's verified-publisher confirmation, Plan 05-03's listing-asset Marketplace render confirmation, Plan 05-04's cross-process smoke verdicts. Format suggestion (per project commit style):
       ```
       feat(05-05): bump to 1.0.0 and flip public:true for Marketplace launch

       Closes Phase 5 ship sequence preconditions:
       - Plan 05-01: publisher verified (per 05-VERIFICATION.md ## Publisher Verification Gate)
       - Plan 05-02: CI workflow + 250KB bundle gate green on master
       - Plan 05-03: listing assets rendered correctly on Marketplace (private cezari preview)
       - Plan 05-04: cross-process smoke PASS on Agile + CMMI (PKG-04/07 PARTIAL per D-5/D-7)

       Manifest 0.2.x -> 1.0.0; package.json 0.1.0 -> 1.0.0 (drift closed); public false -> true.
       ```

    Do NOT publish until Task 3.
  </action>
  <acceptance_criteria>
    - `vss-extension.json` `version` is `"1.0.0"` (greppable).
    - `vss-extension.json` `public` is `true` (greppable: `grep -F '"public": true' vss-extension.json`).
    - `vss-extension.json` parses as JSON.
    - `package.json` `version` is `"1.0.0"` (greppable).
    - All Plan 05-03 listing fields are still present (sanity check no edit accidentally clobbered them).
    - `npm run typecheck && npm test && npm run build && npm run check:size` exits 0 after the edits.
    - One commit landed with the `feat(05-05): bump to 1.0.0 and flip public:true` label (greppable: `git log --oneline -1 | grep -F "feat(05-05): bump to 1.0.0"`).
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('vss-extension.json','utf8'))" && grep -F '"version": "1.0.0"' vss-extension.json && grep -F '"public": true' vss-extension.json && grep -F '"version": "1.0.0"' package.json && grep -F "marketplace/overview.md" vss-extension.json && grep -F "screenshot-calculator-light.png" vss-extension.json && npm run typecheck && npm test && npm run build && npm run check:size</automated>
  </verify>
  <done>The flip commit is in place. Version is 1.0.0 in both manifest and package.json. public is true. The codebase is green. The next step is the actual publish.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User runs the public publish via npm run publish:public</name>
  <files>(none — human-action; user runs the publish command and reports the outcome)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"tfx extension publish — verified canonical command" — the public-publish form (no --share-with)
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Pattern 6 Option A → publish:public" — the script invocation
    - scripts/publish-cezari.cjs (Plan 05-02 created — invoked by both publish:cezari and publish:public)
  </read_first>
  <action>
    This is a human-action checkpoint. The publish command writes to a permanent Marketplace artifact (Marketplace registers every publish version, even on validation failure). Per CONTEXT D-2 (publish stays manual; no PAT in CI secrets), this is a deliberate human-driven action. Claude does not run `npm run publish:public` from the autonomous tool environment.
  </action>
  <checkpoint>
    <type>human-action</type>
    <details>
      Steps for the user:

      1. Confirm `.env.local` exists at repo root with `TFX_PAT=<your Marketplace publish PAT>`. Verify the PAT is current (PATs expire in 6 months or 1 year depending on creation settings; if it expired since Phase 4, regenerate at https://marketplace.visualstudio.com/manage/publishers/tsezarimshvenieradzeextensions with the **Marketplace (publish)** scope and update `.env.local`).

      2. Run the public publish command:
         ```bash
         npm run build && npm run publish:public
         ```

         Note: Per RESEARCH §Pattern 6, `tfx extension publish` reads `public:true` from the manifest directly — the `--public` flag passed by `publish:public` is harmlessly redundant. The manifest is the source of truth.

      3. Watch the output. Expected:
         - `[publish:cezari] npx tfx extension publish ...` (the script logs the command).
         - `tfx-cli` output: package the VSIX, upload, register the version. Typical lines: "Packaged ...vsix", "Publishing ...", "Published".
         - On success: stdout shows the published version (`1.0.0`) and a Marketplace URL.

      4. Confirm the publish landed publicly:
         - Open https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator in a NEW browser window (preferably incognito or a different account — proves the listing is publicly visible, not just to logged-in publisher accounts).
         - The listing should display:
           - The 1.0.0 version label.
           - The marketplace/overview.md content rendered.
           - The 2 screenshots in the carousel.
           - The "Get it free" or "Install" button (vs. a "Share" button on private listings).

      5. Capture for chat:
         - The verbatim stdout from the publish command (especially the published version + any URL printed).
         - Confirmation that the public URL works in incognito.
         - Any errors or warnings (e.g., "Validation pending" — Marketplace validation can take 5-30 minutes; if `--no-wait-validation` was used, the listing might be in "validating" state initially).

      6. If the publish ERRORED:
         - "Publisher must be verified" — the verification gate from Task 1 was wrong. STOP. Recheck Task 1.
         - "Version number must increase" — someone manually published 1.0.0 between Task 2's commit and now. Bump to 1.0.1 in vss-extension.json AND package.json (one more atomic commit), retry.
         - 401 / auth error — PAT expired. Regenerate per step 1, retry.
         - Other errors — paste verbatim into chat for diagnosis.

      Resume keyword (one of): `public-publish-success`, `public-publish-validating` (Marketplace returned but listing not yet visible — wait 5-30 min), `public-publish-error` (paste error message + Claude diagnoses).
    </details>
    <awaiting>
      User confirms public listing is live (or in validation; either is acceptable for proceeding to Task 4 — the listing typically goes live within 30 minutes). Resume keyword: `public-publish-success` or `public-publish-validating`.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>MISSING — human-action gate; user-supplied publish stdout + incognito-URL confirmation is the evidence.</automated>
  </verify>
  <done>The 1.0.0 public listing is live (or in Marketplace validation, expected to clear within 30 minutes). The user has captured the publish stdout and confirmed the public URL works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Post-publish 1.0.0 re-verification smoke on cezari (RESEARCH Open Q4)</name>
  <files>(none — human-verify; user runs the smoke on cezari with the auto-updated 1.0.0 install)</files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-RESEARCH.md §"Open Questions → Q4: Should the public publish (Wave 3) be preceded by a final cezari smoke test on the soon-to-be-public 1.0.0 build, or can the Wave 2 cezari verification (on 0.2.x) be trusted?" — recommendation is "Re-verify on 1.0.0 anyway"
    - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md §"Manual Verification Checklist — 1. Happy path, no current SP" — the same console-transcript shape used as evidence in Phase 4 / Plan 05-04 / this plan
  </read_first>
  <action>
    This is a human-verify checkpoint. The 1.0.0 public publish is logically the same code as the Plan 05-04-verified 0.2.x build (only the version + public flag changed in Task 2's commit). But the empirical confirmation that the public 1.0.0 build runs identically to the 0.2.x cezari-verified build is the final safety net before declaring Phase 5 closed.
  </action>
  <checkpoint>
    <type>human-verify</type>
    <details>
      Steps for the user:

      1. Wait for the cezari org's installed extension to auto-update to 1.0.0. cezari has the extension installed since Phase 4 (with the `--share-with cezari` flag); Marketplace's installed-version pointer follows the latest published version for installs with auto-update enabled (the default). Auto-update typically completes within ~15 minutes after publish.

         To verify the auto-update happened, refresh a cezari work item form. Open DevTools → Sources tab → look at the loaded `modal.js` bundle. The bundle should be the 1.0.0 build (not the 0.2.x build that was loaded before publish).

         Alternatively: visit https://cezari.visualstudio.com/_settings/extensions → Story Point Calculator → confirm the displayed version is 1.0.0 (or higher).

      2. Run the SAME smoke as Plan 05-04 Task 2's Scrum baseline (this is just re-running on the 1.0.0 build to confirm no regression):
         - Open the existing Cezari Scrum project → any PBI (PBI #4 from Phase 4 verification or any new PBI without an existing SP value).
         - Open DevTools Console (F12).
         - Click "Calculate Story Points" toolbar button.
         - Pick: C=Hard, U=Medium, E=Easy → SP=2 (W=2.40, Raw SP=1.95).
         - Click Apply.
         - Verify console transcript:
           ```
           [sp-calc/apply] postComment start fieldRefName=Microsoft.VSTS.Scheduling.StoryPoints payloadSp=2
           [sp-calc/apply] postComment ok commentId=<N>
           [sp-calc/apply] setFieldValue start refName=Microsoft.VSTS.Scheduling.StoryPoints value=2
           [sp-calc/apply] both writes succeeded
           ```
         - Verify form: SP field shows 2; Discussion has new comment.

      3. (Optional but encouraging) Run the same smoke on Agile-Test or CMMI-Test (Plan 05-04's projects) to confirm 1.0.0 also handles the FieldResolver fallback paths cleanly. NOT strictly required (Plan 05-04 already proved this on 0.2.x; the 1.0.0 codebase is byte-identical except version + public flag); skip if you're satisfied with the Scrum-only re-verification.

      4. Report back:
         - PASS / FAIL on the Scrum smoke (with verbatim console transcript if PASS, OR diagnostic info if FAIL).
         - The version string of the cezari-installed extension (visible in the org settings extensions page) — should be 1.0.0.
         - Any anomalies (e.g., "the modal opened but the dropdowns took longer than usual to render" — anything qualitatively different vs. the 0.2.x experience).

      If FAIL on 1.0.0: this is severe. The public listing has a bug that the 0.2.x verification didn't catch. STOP — do not proceed to Task 5 (cross-cutting doc updates). Diagnose: was the 1.0.0 build different from 0.2.x in any way? Likely cause is webpack cache pollution or a manifest field that affected runtime behavior. Roll back via git revert of the Task 2 commit, re-publish 0.2.x to cezari, and discuss with the developer.

      Resume keyword (one of): `1-0-0-smoke-pass`, `1-0-0-smoke-fail` (Claude pauses for diagnosis).
    </details>
    <awaiting>
      User confirms 1.0.0 smoke PASS on cezari Scrum (and optionally Agile/CMMI). Resume keyword: `1-0-0-smoke-pass`.
    </awaiting>
  </checkpoint>
  <verify>
    <automated>MISSING — human-verify gate; user-supplied console transcript + PASS verdict is the evidence.</automated>
  </verify>
  <done>The 1.0.0 public build runs identically to the 0.2.x cezari-verified build on the canonical Scrum smoke. The public listing is functionally validated; no behavioral regression slipped through.</done>
</task>

<task type="auto">
  <name>Task 5: Append ## Public Publish Record + ## Phase 5 Verdict to 05-VERIFICATION.md</name>
  <files>
    .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md (current state — Plan 05-01 created the file with `## Public Publish Record (Plan 05-05 ...)` and `## Phase 5 Verdict` placeholders; Plan 05-04 filled `## Cross-Process Smoke`)
    - The user-pasted Task 3 publish stdout
    - The user-pasted Task 4 1.0.0 smoke transcript
    - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md §"Phase 4 Verdict" — formatting precedent for the per-criterion verdict mapping
    - .planning/ROADMAP.md Phase 5 Success Criteria (the 4 criteria this plan's verdict maps to)
  </read_first>
  <action>
    Use the Edit tool to replace the two placeholder sections with populated content.

    **Step A — Replace `## Public Publish Record` placeholder:**
    - old_string: `## Public Publish Record (Plan 05-05 — PKG-07 / Phase 5 close)\n\n*(Populated by Plan 05-05 — version walk 0.2.5 → 1.0.0, public:true flip commit reference, post-publish 1.0.0 re-verification on cezari.)*`
    - new_string:
      ```markdown
      ## Public Publish Record (Plan 05-05 — PKG-07 / Phase 5 close)

      **Published:** <today's date YYYY-MM-DD>
      **Version walk:** 0.2.<X> (Plan 05-04 endpoint) → 1.0.0 (this plan)
      **package.json sync:** 0.1.0 → 1.0.0 (drift closed)
      **Public flag:** false → true
      **Flip commit:** <git rev-parse --short HEAD-of-Task-2-commit ; capture in chat>
      **Public listing URL:** https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator
      **Run by:** Tsezari (cezo777@gmail.com)

      ### Publish stdout (verbatim from `npm run publish:public`):

      ```
      <paste verbatim user-supplied stdout from Task 3 — the [publish:cezari] line + the tfx-cli output through "Published 1.0.0" or equivalent>
      ```

      ### Public-listing visibility check (incognito browser):

      <user-supplied confirmation that the listing renders for an anonymous user — typically a one-line "yes, visible at <URL> with Install button">

      ### Post-publish 1.0.0 re-verification on cezari (Scrum baseline)

      Per RESEARCH Open Q4: re-run the canonical happy-path smoke on the public 1.0.0 build to confirm behavior identical to the 0.2.x cezari-verified build.

      **Verbatim console transcript (1.0.0 on cezari Cezari/PBI):**

      ```
      <paste verbatim user-supplied [sp-calc/apply] log lines from Task 4>
      ```

      **Result:** <PASS | FAIL — typically PASS since 1.0.0 is byte-identical to 0.2.x except version + public>

      **(Optional) Agile + CMMI re-verification on 1.0.0:** <if user re-ran the cross-process smoke on 1.0.0, capture; otherwise note "skipped — 0.2.x cross-process evidence carries forward per Plan 05-04 ## Cross-Process Smoke">
      ```

    **Step B — Replace `## Phase 5 Verdict` placeholder:**
    - old_string: `## Phase 5 Verdict\n\n*(Populated by Plan 05-05 close.)*`
    - new_string:
      ```markdown
      ## Phase 5 Verdict

      **Verdict:** <PASS | PARTIAL PASS | FAIL — typically PARTIAL PASS due to declared D-5/D-7 deviations on PKG-04/07>

      **Per ROADMAP Phase 5 success criteria:**

      1. **Build pipeline produces a `.vsix` via `tfx-cli` with two webpack entries; CI fails the build when total bundle size exceeds 250 KB gzipped.** → **PASS** (Plan 05-02 evidence — `.github/workflows/ci.yml` runs `npm run check:size` after `npm run build`; current build is ~146.8 KB / 250 KB; deliberate-failure smoke confirmed exit 1 above 250 KB).
      2. **Marketplace listing has description, privacy/data-handling statement, formula explanation, 128×128 icon, screenshots in light + dark themes.** → **PASS** (Plan 05-03 evidence — `marketplace/overview.md` covers all 5 elements; `images/screenshots/screenshot-calculator-{light,dark}.png` committed; `images/icon.png` either refined or shipped per Task 4 of Plan 05-03 with explicit decision recorded).
      3. **Private install on a fresh ADO trial org lets a Contributor (non-admin) user complete the full flow.** → **PARTIAL** per CONTEXT D-7 (Plan 05-04 evidence — install + Apply verified at the publishing user's level on cezari; Contributor explicit test SKIPPED per D-7; cezari serves as the multi-process substitute per D-5; not a "fresh trial org" but a deliberately substituted single-org multi-process setup).
      4. **Extension published `public:true` on Visual Studio Marketplace with `vso.work_write` scope only; install + Apply succeeds on at least two different ADO organizations — one Agile, one CMMI.** → **PARTIAL** — `public:true` ✓ (Task 2/3 above); `vso.work_write` scope unchanged from Phase 0 ✓ (manifest grep proves only this scope is declared); Agile + CMMI processes verified ✓ (Plan 05-04 Cross-Process Smoke); BUT single-org coverage per D-5 (cezari runs all 3 processes; no second org). Declared deviation accepted by verifier per CONTEXT D-5.

      ### Known limitations (carried from Phase 4)

      Per `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md ## Phase 4 Verdict ### Known limitations`:
      1. Esc key does not dismiss the modal (workaround: click outside or X button).
      2. Reopen-pre-fill from sentinel comment permanently deferred (per spike A1 STRIPPED-FALLBACK).
      3. No eager read-only probe (per spike A3 LAZY-FALLBACK-ONLY).
      4. Custom SP field name override (D-1) deferred to v2 backlog.
      5. D-17 Scenarios 3/5/7 (offline / Stakeholder / slow-3G) deferred to v2 (per CONTEXT recommendation; orchestrator code paths unit-test-covered 398/398).

      All five are documented in the public listing's `marketplace/overview.md ## Known limitations (v1)` section per D-10.

      ### Phase 5 close

      Phase 5 is COMPLETE. The Story Point Calculator extension is live on Visual Studio Marketplace at version 1.0.0. v1 milestone closed.
      ```

    Use exact-match Edit calls for both replacements. Verify post-edit:
    - `! grep -F "*(Populated by Plan 05-05" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` (placeholders gone).
    - `grep -F "## Public Publish Record" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "## Phase 5 Verdict" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` (sections present).
    - `grep -F "1.0.0" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` returns multiple matches.

    Commit message: `docs(05-05): record public publish + 1.0.0 re-verification — Phase 5 verdict`.
  </action>
  <acceptance_criteria>
    - 05-VERIFICATION.md no longer contains the literal `*(Populated by Plan 05-05` placeholder text (greppable: `! grep -F "*(Populated by Plan 05-05" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md`).
    - File contains the 4 per-ROADMAP-criterion verdicts under `## Phase 5 Verdict` (greppable: `grep -E "^[0-9]\\..*PASS|PARTIAL" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` returns at least 4 matches).
    - File contains the public listing URL (greppable: `grep -F "marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md`).
    - File contains the verbatim publish stdout (greppable: `grep -F "[publish:cezari]" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md` OR similar — at minimum, the user-supplied stdout text is embedded).
    - File contains the 1.0.0 cezari smoke transcript (greppable: at least 4 `[sp-calc/` log lines beyond the Plan 05-04 ones — the running count should be >= 8 across the file).
  </acceptance_criteria>
  <verify>
    <automated>! grep -F "*(Populated by Plan 05-05" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "## Public Publish Record" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "## Phase 5 Verdict" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "1.0.0" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md && grep -F "marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator" .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md</automated>
  </verify>
  <done>05-VERIFICATION.md is fully populated. The Public Publish Record + Phase 5 Verdict sections are explicit; per-ROADMAP-criterion verdicts are mapped; PARTIAL declarations cite CONTEXT D-5/D-7 deviations.</done>
</task>

<task type="auto">
  <name>Task 6: Flip REQUIREMENTS.md PKG-02..07 + ROADMAP.md Phase 5 row + STATE.md milestone close</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 60-66 (PKG-02..07 currently `[ ]`; lines 142-147 Traceability table currently `Pending`)
    - .planning/ROADMAP.md line 24 (Phase 5 row `- [ ] **Phase 5:**`); line 154 Progress table Phase 5 row
    - .planning/STATE.md frontmatter (current `progress.completed_phases: 5` per Phase 4 close — wait, STATE.md actually shows 5/6 = 83%; verify the current state and bump completed_phases by 1, total 6/6 = 100%); body's Current Position + Performance Metrics + Accumulated Context sections
    - .planning/phases/04-write-path-edge-cases/04-06-PLAN.md Task 5 (the precedent — same structural template, different phase number)
    - .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md (the verdict basis)
  </read_first>
  <action>
    **Step A — REQUIREMENTS.md PKG-02..07 checkboxes:** Use the Edit tool with exact-match `old_string` / `new_string` per requirement. Each `- [ ] **PKG-XX**` flips to `- [x] **PKG-XX**`. Six edits (one per requirement). For PKG-04 and PKG-07, the wording stays unchanged but a footnote-style note is APPENDED noting the PARTIAL declared deviation:
    - PKG-04 line gains: `*(Verified Phase 5 cezari 2026-05-XX — install + Apply verified at the publishing user's level; Contributor explicit test PARTIAL per CONTEXT D-7)*`
    - PKG-07 line gains: `*(Verified Phase 5 Marketplace public listing 2026-05-XX; cross-process smoke (Scrum + Agile + CMMI) on cezari per D-5; PARTIAL — single-org coverage per D-5 deviation)*`

    For PKG-02, PKG-03, PKG-05, PKG-06, append: `*(Verified Phase 5 — see .planning/phases/05-polish-marketplace-publish/05-VERIFICATION.md)*`

    **Step B — REQUIREMENTS.md Traceability table:** Update the rows for PKG-02..07. Each `Pending` becomes `Complete`. Six edits.

    **Step C — ROADMAP.md Phase 5 line:** Edit line 24 (or wherever the line currently is):
    - old_string: `- [ ] **Phase 5: Polish & Marketplace Publish** - Bundle size gate, listing assets, private smoke install, then public publish on Visual Studio Marketplace verified on Agile and CMMI orgs`
    - new_string: `- [x] **Phase 5: Polish & Marketplace Publish** - Bundle size gate, listing assets, private smoke install, then public publish on Visual Studio Marketplace verified on Agile and CMMI orgs (completed YYYY-MM-DD)`

    **Step D — ROADMAP.md Phase 5 Plans block:** Replace `**Plans**: TBD` (or whatever current state, line ~139) with `**Plans**: 5 plans` and replace the (currently empty) Plans subsection at the end of Phase 5 details with:

    ```
    Plans:
    **Wave 1**
    - [x] 05-01-PLAN.md — Publisher verification gate (PKG-06) — file 05-VERIFICATION.md skeleton + verification request if needed
    - [x] 05-02-PLAN.md — Build infrastructure trio: GitHub Actions CI workflow, scripts/check-bundle-size.cjs (250 KB gzipped gate), scripts/publish-cezari.cjs replacing scripts/dev-publish.cjs
    - [x] 05-03-PLAN.md — Listing-asset bundle: vss-extension.json delta (content/links/repository/screenshots), marketplace/overview.md, README v1, light + dark screenshots, private re-publish to cezari for Marketplace render confirmation

    **Wave 2** *(blocked on Wave 1 completion)*
    - [x] 05-04-PLAN.md — Cross-process cezari smoke: add Agile + CMMI projects; D-6 smoke per process; record verdicts in 05-VERIFICATION.md ## Cross-Process Smoke

    **Wave 3** *(blocked on Waves 1+2 + publisher verification confirmed)*
    - [x] 05-05-PLAN.md — Public publish: version 0.2.x → 1.0.0 + package.json sync + public:true flip + tfx publish; post-publish 1.0.0 re-verification on cezari; REQUIREMENTS/ROADMAP/STATE close
    ```

    **Step E — ROADMAP.md Progress table:** Update the Phase 5 row:
    - old: `| 5. Polish & Marketplace Publish | 0/TBD | Not started | - |`
    - new: `| 5. Polish & Marketplace Publish | 5/5 | Complete    | YYYY-MM-DD |`

    **Step F — STATE.md frontmatter:** Edit the YAML frontmatter:
    - `progress.completed_phases`: `5` → `6` (was 5/6 after Phase 4; now 6/6 since Phase 5 is the 6th tracked phase counting from Phase 0).

    Wait — re-read STATE.md carefully: the current STATE.md shows `total_phases: 6` and `completed_phases: 5`. Phase 5 close brings completed_phases to 6 = 100%.

    - `progress.completed_phases`: 5 → 6
    - `progress.total_plans`: 14 → 19 (14 prior + 5 Phase 5)
    - `progress.completed_plans`: 14 → 19
    - `progress.percent`: 83 → 100
    - `last_activity`: today's date with note "Phase 5 closed by Plan 05-05 — public Marketplace publish 1.0.0; v1.0 milestone complete"
    - `last_updated`: today's ISO-8601 timestamp
    - `stopped_at`: "Phase 05 complete; v1.0 milestone CLOSED"
    - `status`: "executing" → "milestone-complete" (or whatever the project's milestone-close status enum is; if STATE.md schema is strict, keep `executing` but add a body note)

    **Step G — STATE.md body:**
    - `## Current Position`: bump to "Phase: 05 (polish-marketplace-publish) — COMPLETE"; "Plan: 5 of 5 complete"; Status: "Phase 05 complete; v1.0 milestone CLOSED; ready for /gsd-complete-milestone".
    - `## Performance Metrics > By Phase`: add a Phase 5 row.
    - `## Performance Metrics > Recent Trend`: append "; Phase 5 closed via 5 plans (1 publisher gate + 1 build infra + 1 listing assets + 1 cross-process smoke + 1 public publish); v1.0 milestone complete; extension live on Marketplace at https://marketplace.visualstudio.com/items?itemName=TsezariMshvenieradzeExtensions.story-point-calculator".
    - `## Accumulated Context > Decisions`: append the Phase 5 D-IDs that became locked decisions (D-1 through D-11) with one-line summaries each. Phase 4 has the precedent shape.
    - `## Blockers/Concerns`: REMOVE the Phase 5-related blocker entries (the existing STATE.md has 3 Phase 5 entries — custom SP fields, cross-process coverage, dev-publish.cjs broken; all 3 are now resolved per Phase 5 plans). Add a new note: `Phase 5 closed; v1.0 shipped; v2 backlog items carried (custom SP fields, Esc dismissal, network-failure scenarios, dual-org PKG-07).`

    **Step H — Final commit:** Combine REQUIREMENTS / ROADMAP / STATE updates into ONE commit:
    ```
    docs(05-05): close Phase 5 and v1.0 milestone — PKG-02..07 [x]; PARTIAL PASS verdict
    ```

    The verification record (`05-VERIFICATION.md`) commit (Task 5) is the prior commit; this cross-cutting docs commit is the closing commit of Phase 5.
  </action>
  <acceptance_criteria>
    - REQUIREMENTS.md has 6 instances of `[x] **PKG-` for PKG-02..07 (greppable: `grep -E "^- \\[x\\] \\*\\*PKG-0[2-7]\\*\\*" .planning/REQUIREMENTS.md` returns 6 matches).
    - REQUIREMENTS.md Traceability table has PKG-02..07 marked Complete (greppable: `grep -E "PKG-0[2-7] \\| Phase 5 \\| Complete" .planning/REQUIREMENTS.md` returns 6 matches).
    - ROADMAP.md Phase 5 line is `- [x]` (greppable: `grep -E "- \\[x\\] \\*\\*Phase 5:" .planning/ROADMAP.md` returns 1 match).
    - ROADMAP.md Phase 5 Plans section lists all 5 plans with `[x]` prefix (greppable: `grep -E "^- \\[x\\] 05-0[1-5]-PLAN\\.md" .planning/ROADMAP.md` returns 5 matches).
    - ROADMAP.md Progress table Phase 5 row shows `5/5 | Complete` (greppable: `grep -E "Phase 5.*5/5.*Complete" .planning/ROADMAP.md`).
    - STATE.md frontmatter has `completed_phases: 6` AND `percent: 100` (greppable: `grep -E "completed_phases:\s+6" .planning/STATE.md && grep -E "percent:\s+100" .planning/STATE.md`).
    - STATE.md Current Position references Phase 05 COMPLETE / v1.0 milestone (greppable: `grep -F "v1.0 milestone" .planning/STATE.md`).
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^- \\[x\\] \\*\\*PKG-0[2-7]\\*\\*" .planning/REQUIREMENTS.md | wc -l | grep -E "^\s*6\s*$" && grep -E "- \\[x\\] \\*\\*Phase 5:" .planning/ROADMAP.md && grep -E "completed_phases:\s+6" .planning/STATE.md && grep -E "percent:\s+100" .planning/STATE.md && grep -F "v1.0 milestone" .planning/STATE.md</automated>
  </verify>
  <done>REQUIREMENTS.md PKG-02..07 are checked; ROADMAP.md Phase 5 row is Complete with the 5-plan listing; STATE.md reflects 6/6 phases (100%) and the v1.0 milestone close. Phase 5 is closed; v1.0 has shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User publish action → Marketplace public catalog | The `npm run publish:public` invocation registers a permanent, public-facing artifact. Once 1.0.0 is published, the version number can never be re-used (Marketplace registers every publish attempt). |
| Verification status (Plan 05-01) → public flip (Task 2) | Verification status freshness matters because Microsoft's verification can be revoked between Plan 05-01 and Wave 3. Task 1 re-checks live to mitigate. |
| `vss-extension.json` → tfx-cli → Marketplace | The manifest's `public:true` flag is the actual gating mechanism. tfx reads the manifest; Marketplace's API rejects the publish if publisher unverified. |
| Cross-cutting docs (REQUIREMENTS / ROADMAP / STATE) → next-milestone planner | If v2 is planned, the planner reads these files. PARTIAL verdicts must be explicit so v2 backlog correctly prioritizes the deviations (PKG-04 / PKG-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | I (Information disclosure) | Public Marketplace listing exposes the extension to the world; the `marketplace/overview.md` content is now public | accept | Intentional — Phase 5 IS the public-distribution phase. The content is reviewed in Plan 05-03 Task 7 visual confirmation; nothing private leaks. |
| T-05-05-02 | T (Tampering) | The flip commit (Task 2) accidentally introduces a manifest field change beyond version + public | mitigate | Acceptance criterion verifies all earlier listing fields are still present (sanity grep on `marketplace/overview.md`, `screenshot-calculator-light.png`). The diff for Task 2's commit should be ≤6 lines (3 in vss-extension.json, 1 in package.json, plus closing brackets). |
| T-05-05-03 | E (Elevation of privilege) | Adding any scope beyond `vso.work_write` would force re-consent across every install | mitigate | The manifest has not changed `scopes` since Phase 0. Acceptance is defensive: `grep -F '"vso.work_write"' vss-extension.json` confirms the only-scope; `! grep -E '"scopes":\s*\[[^]]*,' vss-extension.json` would catch a multi-element scope array (the literal regex matches a comma inside the scopes brackets — false positive if formatting changes; visual review of the diff in Task 2's commit is the primary check). |
| T-05-05-04 | T (Tampering) | The Phase 5 close commit could flip checkboxes for requirements that didn't actually pass | mitigate | The Phase 5 Verdict in 05-VERIFICATION.md must list per-criterion verdicts; the 05-05 SUMMARY restates them; the cross-cutting docs commit message includes the verdict. Audit chain: REQUIREMENTS [x] → ROADMAP [x] → STATE.md → 05-VERIFICATION.md verdict → cezari evidence. Same chain as Phase 4 close (Plan 04-06 §Audit chain). |
| T-05-05-05 | R (Repudiation) | If a Marketplace user reports a bug post-launch, the audit trail must show what was verified pre-launch | mitigate | 05-VERIFICATION.md is committed to git; commit history preserves the pre-launch state. The `## Public Publish Record` section names the publish timestamp and the cezari verification timestamp. |
| T-05-05-06 | D (Denial of service) | Marketplace validation could reject 1.0.0 (e.g., asset path bug; sanitizer flag; spam heuristic on a generic icon) | mitigate | Plan 05-03 Task 7 visual confirmation pre-validates the listing render on the private `0.2.x` build using the same code path. The 1.0.0 build is byte-identical except for version + public flag, so a sudden validation rejection is unlikely; if it happens, Task 3's resume keyword `public-publish-error` triggers diagnosis. |
</threat_model>

<verification>
- Publisher verification status is `verified` at Wave 3 start (Task 1 live re-check; not relying on Plan 05-01's possibly-stale timestamp).
- Manifest version is 1.0.0; package.json synced to 1.0.0; `public: true`.
- `npm run publish:public` succeeded; the public listing is visible on Marketplace at the canonical URL.
- 1.0.0 cezari Scrum smoke PASS (canonical happy-path verified post-publish on the public build).
- 05-VERIFICATION.md `## Public Publish Record` + `## Phase 5 Verdict` populated; per-ROADMAP-criterion verdicts mapped.
- REQUIREMENTS.md PKG-02..07 [x]; Traceability Complete; PKG-04/07 footnotes cite PARTIAL with D-5/D-7 deviations.
- ROADMAP.md Phase 5 row [x]; Plans section lists 5 plans with [x]; Progress table Complete.
- STATE.md 6/6 phases (100%); v1.0 milestone closed; Blockers/Concerns updated.
</verification>

<success_criteria>
- All 4 ROADMAP Phase 5 success criteria are evaluated with explicit per-criterion verdicts in 05-VERIFICATION.md `## Phase 5 Verdict`.
- All 6 PKG-XX requirements (PKG-02..07) are marked Complete in REQUIREMENTS.md (PKG-04 + PKG-07 with PARTIAL declared-deviation footnotes per CONTEXT D-5 / D-7).
- The Story Point Calculator extension is live and publicly installable from Visual Studio Marketplace at version 1.0.0.
- The v1.0 milestone is COMPLETE; 5/5 phases (counting from 0) shipped.
- Phase 5 Verdict (PASS / PARTIAL PASS / FAIL) is recorded; expected outcome is PARTIAL PASS due to declared D-5/D-7 deviations on PKG-04/07.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-marketplace-publish/05-05-SUMMARY.md` per the standard template. Capture:
- Wave 3 verification gate re-check outcome (verified-still | verified-now | other).
- The flip-commit hash (`feat(05-05): bump to 1.0.0 and flip public:true`).
- Verbatim publish stdout from `npm run publish:public`.
- Public Marketplace listing URL.
- 1.0.0 cezari re-verification result.
- The 4-criterion verdict mapping (per ROADMAP).
- Cross-cutting doc-update commit hash.
- v1.0 milestone close declaration.
- v2 backlog carry-forward list (custom SP fields per D-1, Esc dismissal, dual-org PKG-07, network-failure D-17 scenarios) for the next milestone planner to read.
</output>
