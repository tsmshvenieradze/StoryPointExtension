---
phase: 04-write-path-edge-cases
plan: 06
type: execute
wave: 4
depends_on: [04-05]
files_modified:
  - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md
  - vss-extension.json
autonomous: false
requirements: [APPLY-04, APPLY-05, APPLY-06, APPLY-07, APPLY-08, APPLY-09]
must_haves:
  truths:
    - "All 8 D-17 scenarios were exercised end-to-end on cezari (Scrum process) with the Phase 4 implementation built; pass/partial/fail verdicts captured per scenario with verbatim console transcripts and Network-tab evidence"
    - "Scenario 1 (happy path) proves comment-first → field-write order, modal post-success state, and reopen-pre-fill (D-16) on a real ADO surface"
    - "Scenario 8 (HTML-format sentinel preservation) confirms the Plan 04-01 A1 verdict was correct — sentinel hidden by renderer + parser round-trips on next reopen"
    - "If real-world fixes are needed (mirror Phase 03-04 §Real-world Corrections pattern), they're back-ported via atomic fix(04-XX) commits to the originating plan; the verification file lists them"
    - "REQUIREMENTS.md APPLY-04..09 checkboxes flip from [ ] to [x] reflecting the cezari evidence; ROADMAP.md Phase 4 row marks Complete; STATE.md updated"
  artifacts:
    - path: ".planning/phases/04-write-path-edge-cases/04-VERIFICATION.md"
      provides: "Full Phase 4 verification record: Spike Results (Plan 04-01), Manual Verification Checklist (8 D-17 scenarios), Real-world Corrections (if any back-ports were needed), Phase verdict (PASS / PARTIAL PASS / FAIL with reasons)"
      contains: "## Manual Verification Checklist"
    - path: "vss-extension.json"
      provides: "Manifest version walked further from 0.2.0 (Plan 04-05) per the cezari publish loop"
      contains: "\"version\":"
  key_links:
    - from: ".planning/phases/04-write-path-edge-cases/04-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md APPLY-04..09 checkboxes"
      via: "Phase 4 close commits flip the checkboxes only after the verification record sign-off"
      pattern: "APPLY-0[4-9].*Phase 4.*Complete"
---

<objective>
Empirically verify Phase 4's full write path on cezari (Scrum org) by executing the D-17 8-scenario manual checklist; capture verbatim console transcripts + Network-tab evidence + screenshots into `04-VERIFICATION.md`; back-port any real-world bugs via atomic `fix(04-XX)` commits to the originating plan (mirror Phase 03-04 fix-back pattern); produce the final Phase 4 verdict (PASS / PARTIAL / FAIL); update REQUIREMENTS.md APPLY-04..09 from `[ ]` to `[x]`; update ROADMAP.md Phase 4 status; update STATE.md.

Purpose: The project standard ("manual QA does UI testing per company standard; only formula logic is unit-tested" — CLAUDE.md) makes this checkpoint plan the SOLE empirical evidence that Phase 4 satisfies its 5 ROADMAP success criteria. Phase 03-04's pattern surfaced 6 real-world bugs that were back-ported; Plan 04-06 mirrors that with `fix(04-XX): patch <plan-id> regression — <reason>` commits if needed.

Output: Filled `04-VERIFICATION.md`; flipped checkboxes; updated cross-cutting docs; Phase 4 ready for `/gsd-transition` to Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-write-path-edge-cases/04-CONTEXT.md
@.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md
@.planning/phases/03-modal-ui-read-path/03-VERIFICATION.md
@.planning/phases/03-modal-ui-read-path/03-04-SUMMARY.md
@scripts/dev-publish.cjs
@vss-extension.json

<interfaces>
<!-- Plan 04-06 reads from prior-plan summaries; writes the final verification record. -->

The 8 D-17 scenarios (verbatim from `04-CONTEXT.md` lines 100–108):
1. **Happy path, no current SP** — open modal on a PBI with empty SP; calc; Apply; observe in Network tab: `addComment` POST succeeds first, then `setFieldValue` + `.save()` writes the field; modal auto-closes (or stays open with Saved ✓ + Esc-hint per A4 verdict); SP field on the form shows the new value; reopening the modal pre-fills from the just-written sentinel; rendered comment view shows ONLY the human-readable line (sentinel hidden by HTML format if A1 was PRESERVED-HIDDEN).
2. **Overwrite confirm path** — open modal on a PBI with existing SP=3; pick a trio that yields SP=5; click Apply; confirm panel renders with "Current 3 / New 5"; click Back → calculator re-renders with selections preserved; click Apply again → confirm panel; click Confirm Apply → write sequence proceeds.
3. **Comment POST failure** — temporarily mangle the URL in `postComment.ts` to force a 404 (or run with network offline); observe in-modal error banner with the D-11 status copy + Retry button; click Retry with the URL fixed → recovers; verify NO field write occurred when the comment failed.
4. **Field-write 412** — open modal; immediately edit the SP field on the form to a different value (this dirties the form/version); click Apply through to Confirm; observe persistent banner per D-09; comment is in the audit log; Retry attempts the field write only.
5. **Stakeholder/read-only** — change the cezari user license to Stakeholder (or use a Reader-only user on a public-area work item); reopen the work item; click toolbar; observe the read-only branch (calculator hidden, message visible) per D-06.
6. **isReadOnly probe failure** — temporarily make `isReadOnly()` reject (e.g., throw inside a wrapper used in dev builds) and confirm the warning banner from D-07 appears + calculator stays usable + Apply still attempts the write.
7. **Saving overlay** — slow-network throttle in DevTools; click Apply; observe the saving overlay + disabled buttons; verify the user cannot interact with dropdowns mid-flight.
8. **HTML-format sentinel preservation** (D-02) — inspect the just-posted comment in (a) the rendered Discussion view (no `<!-- -->` visible), (b) the GET response body via the Network tab (sentinel intact, raw or entity-encoded), (c) parser correctness on next reopen (pre-fill works).

The Phase 03-04 fix-back pattern (per `03-04-SUMMARY.md` lines 92–101): each real-world fix is committed atomically with a `fix(04-06): patch <plan-id> regression — <reason>` label so the originating plan retains the audit trail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + publish the Phase 4 implementation VSIX to cezari</name>
  <read_first>
    - `vss-extension.json` (current version after Plan 04-05 — should be `0.2.0` or higher)
    - `scripts/dev-publish.cjs` (Windows-retry caveat — fall back to direct `npx tfx extension publish --share-with cezari --no-wait-validation --override`)
    - `.planning/phases/03-modal-ui-read-path/03-VERIFICATION.md` §"Setup notes" (publish workflow used during Phase 3)
    - `package.json` scripts (verify `dev:publish` exists)
  </read_first>
  <action>
    1. Run `npm run build` to produce the production webpack bundle. Verify `dist/toolbar.html`, `dist/modal.html`, `dist/toolbar.js`, `dist/modal.js` all exist.

    2. Run `npm run typecheck && npm test` once more — must exit 0 (full Phase 4 suite green).

    3. Bump `vss-extension.json` `"version"` if the value is still `0.2.0` (Plan 04-05 left it there) — bump to `0.2.1` for the cezari publish. Each subsequent re-publish during the verification loop bumps another patch (`0.2.2`, `0.2.3`, ... ) per the Phase 03-04 walk pattern.

    4. Publish to cezari. Try `npm run dev:publish` first; if the wrapper aborts on Windows (per Phase 03-04 known issue), fall back to:
       ```
       npx tfx extension publish --share-with cezari --no-wait-validation --manifest-globs vss-extension.json --override
       ```
       The Marketplace publisher PAT is in `.env.local` (Phase 0 setup); the CLI picks it up via `TFX_AUTH_TOKEN` or its standard fallbacks.

    5. Confirm publish succeeded: stdout from the publish command shows "Published" or equivalent; cezari's Manage Extensions page shows the newly-published version. Print the published version string for the user to refer to in Task 2.

    6. The user navigates to cezari, may need to refresh the extension version in the work item form (uninstall + reinstall the dev version, or wait for the version reload). Verify by opening DevTools and checking the loaded modal.js bundle's content (search for `runApplySequence` to confirm the new code is live, NOT the old stub).
  </action>
  <acceptance_criteria>
    - `vss-extension.json` version is `>= 0.2.1` (greppable: `grep -E '"version":\s*"0\.[2-9]\.[1-9]"' vss-extension.json` returns a match — version walked from 0.2.0).
    - `dist/modal.html` + `dist/modal.js` exist after `npm run build`.
    - `npm run typecheck && npm test` exits 0.
    - The publish command's stdout includes a success message with the published version.
    - User confirms in chat that the cezari Manage Extensions page shows the newly-published version (verify by visiting https://cezari.visualstudio.com/_settings/extensions or by re-opening the work item).
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build && npm test && grep -E '"version":\s*"0\.[2-9]\.\d+"' vss-extension.json</automated>
  </verify>
  <done>The Phase 4 implementation VSIX is live on cezari at version 0.2.1+. The user is ready to run the D-17 8-scenario manual checklist (Task 2).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the D-17 8-scenario manual verification checklist on cezari</name>
  <files>(none — human-verify checkpoint; user runs cezari, captures console transcripts + screenshots, pastes back into chat for Tasks 3 + 4)</files>
  <read_first>
    - .planning/phases/04-write-path-edge-cases/04-CONTEXT.md D-17 scenarios 1–8 (lines 100–108) — verbatim scenario list
    - .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md ## Spike Results (the A1/A4/A5 verdicts that determine expected behavior in scenarios 1, 7, 8)
    - .planning/phases/03-modal-ui-read-path/03-VERIFICATION.md §Setup notes (DevTools Console + Network tab workflow, cezari publish)
    - .planning/REQUIREMENTS.md APPLY-04..09 (the 6 requirements being closed by this checkpoint)
  </read_first>
  <action>
    This is a human-verify checkpoint. After Task 1 publishes the Phase 4 implementation VSIX to cezari, Claude waits while the user executes the 8 D-17 scenarios verbatim. The detailed scenario-by-scenario instructions are in <what-built>; the capture protocol (DevTools Console transcripts, Network-tab evidence, screenshots) is in <how-to-verify>. The user pastes outputs back into chat; Task 3 (if any fix-backs needed) and Task 4 (transcription into 04-VERIFICATION.md) consume them.
  </action>
  <what-built>
    The user runs the 8 D-17 scenarios verbatim from `04-CONTEXT.md` lines 100–108 against the cezari Phase 4 build (Task 1's publish). Each scenario produces verbatim console transcripts (capture from DevTools Console with the `[sp-calc/...]` prefixes), Network-tab evidence (POST/PATCH/GET request URLs + status codes + response bodies for the relevant calls), and screenshots at the key UI states (modal open, confirm panel, saving overlay, error banner, saved indicator, read-only message).

    Scenario-by-scenario expected behavior recap (the user verifies these on cezari):
    1. **Happy path** — Open PBI with empty SP. Trio: pick C=Easy, U=Easy, E=Easy → SP=1 (or whatever calc returns). Click Apply → modal goes through saving → saved. Network tab shows: POST `/comments?api-version=<spike A5>` 200 BEFORE PATCH/save. Form's SP cell updates to 1 without page reload. Re-open modal: dropdowns pre-fill Easy/Easy/Easy. The just-posted comment in cezari Discussion view shows ONLY the human-readable line "Story Points: 1 (Complexity=Easy, Uncertainty=Easy, Effort=Easy)" — sentinel `<!-- ... -->` is HIDDEN per spike A1 verdict (PRESERVED-HIDDEN).
    2. **Overwrite confirm** — On a PBI with existing SP=3 (set manually if needed), open modal. Pick trio that yields SP=5 (e.g., C=Hard, U=Medium, E=Easy). Click Apply → confirm panel renders centered with "Current Story Points: 3" + "New Story Points: 5" + Back / Confirm Apply. Click Back → calculator re-renders with the SAME trio still selected (Hard/Medium/Easy). Click Apply again → confirm panel re-renders. Click Confirm Apply → saving sequence proceeds; both writes succeed.
    3. **Comment POST failure** — Open browser DevTools, set Network throttling to "Offline" (or temporarily edit `postComment.ts` source to use a bogus URL — but easier: just switch to Offline). Open modal; pick trio; click Apply. The CommentFailBanner appears with the D-11 generic copy "Could not save audit comment. Could not save. (HTTP n/a)" or similar (the offline mode produces a network error → status null → generic copy). Bottom ButtonGroup is `[Cancel]` only. Click Retry with Network back online → comment POSTs succeed → modal goes saving → saved. Important: in the Offline state, NO setFieldValue is attempted (the form's SP cell does NOT change).
    4. **Field-write 412** — Open the work item form. Open modal. While the modal is loading, edit the SP field DIRECTLY on the work item form (set it to some other value). The work item is now "dirty" with a different value than what the modal will try to write — this conflicts. Open modal; pick trio; click Apply through Confirm. Saving → the postComment succeeds, BUT setFieldValue triggers a rule conflict OR `.save()` rejects with a "RuleValidationException"-like error → mode goes to fieldFail. The FieldFailBanner says "Audit comment recorded. The Story Points field could not be updated. Work item changed since the modal opened — reload and try again. (HTTP 412)" or similar. Verify in Discussion that the comment IS posted (audit kept). Click Retry; the field write retries; succeeds (the form's dirty state may need to be undone first by user — the user notes whether that worked).
    5. **Stakeholder / read-only** — In cezari, change the user's license to Stakeholder (Org Settings → Users → assign Stakeholder license). OR use a different user account that has read-only access to a particular work item. Re-open the work item with the limited-permission user. Click toolbar → modal opens → mode goes to "readonly" → ReadOnlyMessage replaces the calculator. The context line still shows current SP. Click Esc → modal closes. Re-issue: revert license back to the dev user's full license for subsequent scenarios.
    6. **isReadOnly probe failure** — Temporarily edit `bridge.ts::getIsReadOnly` to throw on every call (e.g., `throw new Error("synthetic probe failure");` at the start of the try block). Rebuild + republish (version walks +1). Open modal; observe: PermissionWarnBanner appears in the banner stack ("Could not verify your permissions — Apply may fail if this work item is read-only."). The calculator stays usable. Click Apply through; the write succeeds (or fails with the actual permission error if the user lacked write permission). Revert the synthetic throw, rebuild, republish.
    7. **Saving overlay** — In DevTools Network panel, set throttling to "Slow 3G" or use the "Custom" preset to add ~3s latency. Open modal; pick trio; click Apply through Confirm. The saving overlay appears (translucent dim + Spinner + "Saving…"). Try to click on dropdowns — pointer events blocked. Try to Tab into dropdowns — focus does NOT enter (RESEARCH Pitfall 7 a11y mitigation working). After ~3s, the comment POST resolves; then setFieldValue runs; mode goes to saved. Reset throttling.
    8. **HTML-format sentinel preservation (D-02 verbatim)** — Re-run scenario 1 cleanly. Inspect the just-posted comment in cezari Discussion view: the rendered text shows ONLY the human-readable line (no `<!-- sp-calc:v1 ... -->` markup visible). Open DevTools Network panel, find the GET `/comments?api-version=7.1-preview.4` from the next modal-open's read path; inspect the response body — the comment's `text` field contains the sentinel (raw `<!-- -->` OR entity-encoded `&lt;!--`; both are fine — the parser handles both). Re-open the modal; the pre-fill banner appears with the trio from scenario 1. This is the empirical verification that Plan 04-01's A1 verdict was correct.

    Each scenario MUST capture:
    - DevTools Console transcript (full text, with `[sp-calc/apply]`, `[sp-calc/postComment]`, `[sp-calc/adoFetch]`, `[sp-calc/comments]`, `[sp-calc/bridge]`, `[sp-calc/modal]` log lines visible).
    - Network tab screenshot or summary (which URLs were hit, in what order, with what status codes).
    - At least 1 screenshot at the key visual state (e.g., scenario 2: confirm panel; scenario 5: ReadOnlyMessage; scenario 7: SavingOverlay).
    - Verdict: PASS / PARTIAL / FAIL with a one-line reason.

    If any scenario surfaces a real-world bug (e.g., banner copy is wrong, atomicity ordering is broken, isReadOnly probe doesn't fire), the user notes it. Task 3 (Real-world Corrections back-port) handles them.
  </what-built>
  <how-to-verify>
    1. Open cezari.visualstudio.com/Cezari with the user's normal license. Navigate to a Product Backlog Item (or whichever work-item-type was used in Phase 3 verification — pick one that has the StoryPoints field).
    2. Open DevTools Console BEFORE opening the toolbar. Filter by `[sp-calc/`.
    3. For each of the 8 scenarios, perform the steps described above. Copy the full console transcript + take screenshots + note Network tab evidence.
    4. After all 8 scenarios, paste the transcripts, screenshots, and per-scenario verdicts into the chat (or write into a temporary scratch file Claude can read).
    5. Note any real-world bugs surfaced — Claude back-ports them in Task 3 atomically.
  </how-to-verify>
  <resume-signal>Paste all 8 scenario transcripts + screenshots + verdicts; type `verification-data-captured` when done</resume-signal>
  <verify>
    <automated>MISSING — this is a human-only execution gate; the build is automated by Task 1 above. After this resumes, Task 3 + Task 4 commit the captured outputs into 04-VERIFICATION.md.</automated>
  </verify>
  <done>User has captured verbatim transcripts + Network-tab evidence + screenshots for all 8 D-17 scenarios. Verdicts (PASS / PARTIAL / FAIL) per scenario are recorded. Any real-world bugs are listed for Task 3 fix-backs.</done>
</task>

<task type="auto">
  <name>Task 3: Real-world fix-backs (per Phase 03-04 pattern, only if Task 2 surfaced bugs)</name>
  <read_first>
    - The user-pasted scenario transcripts from Task 2 — identify any FAIL or PARTIAL verdicts that point to a code defect rather than a verification-environment quirk
    - `.planning/phases/03-modal-ui-read-path/03-VERIFICATION.md` §"Real-world Corrections" — the back-port pattern (origin file, symptom, fix, phase implication)
    - `.planning/phases/03-modal-ui-read-path/03-04-SUMMARY.md` lines 92–101 — the atomic commit naming pattern: `fix(04-06): patch <plan-id> regression — <reason>`
    - The originating Phase 4 plan(s) for any failing scenario:
      - Scenario 1, 2, 7, 8 failures → likely `04-05` CalcModal mode machine OR `04-04` UI components OR `04-03` postComment/adoFetch
      - Scenario 3, 4 failures → likely `04-05` apply.ts orchestrator OR `04-02` errorMessages
      - Scenario 5, 6 failures → likely `04-03` bridge.ts getIsReadOnly OR `04-04` ReadOnlyMessage / PermissionWarnBanner
  </read_first>
  <action>
    For each FAIL or PARTIAL scenario from Task 2 that points to a code defect:

    1. **Identify the originating plan.** The plan that owns the file containing the bug. (E.g., banner copy bug → Plan 04-04; atomicity bug → Plan 04-05; api-version bug → Plan 04-03.)

    2. **Diagnose the root cause** from the console transcript / Network evidence. State the symptom + cause + fix in 2–3 sentences.

    3. **Apply the fix** to the originating file. The fix MUST be the minimum change to make the scenario PASS — do NOT refactor, rename, or expand scope. Phase 03-04 fixes were 1–10 line patches (e.g., audit/parse.ts entity decoder, Dropdown3 selectOnFocus:false).

    4. **Commit atomically** with the label per Phase 03-04 pattern:
       ```
       fix(04-06): patch <plan-id> regression — <one-line reason>
       ```
       Examples: `fix(04-06): patch 04-04 regression — CommentFailBanner missing aria-live attribute`. `fix(04-06): patch 04-05 regression — runApplySequence didn't reset applyError on retry`.

    5. **Bump manifest version + republish to cezari** so the user can re-run the affected scenario.

    6. **Re-run the affected scenario** and verify PASS.

    7. **Loop:** if a fix surfaces another bug, repeat.

    If Task 2 produced ZERO failures (all 8 scenarios PASS or attestable PARTIAL): SKIP this task and go to Task 4. Document in the SUMMARY that no fix-backs were needed.
  </action>
  <acceptance_criteria>
    - For each fix-back commit, `git log --oneline | grep -E "^\w+ fix\(04-06\): patch 04-\d+ regression"` returns at least 1 match per fix-back applied.
    - Each fix-back commit touches files belonging to the originating plan only (no cross-plan refactors).
    - After all fix-backs land, the originally-failing D-17 scenario re-runs PASS.
    - `npm run typecheck && npm run build && npm test` exits 0 after each fix-back.
    - If zero fix-backs were needed, this task's done state is "no real-world bugs surfaced; skipped."
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build && npm test</automated>
  </verify>
  <done>All real-world bugs surfaced in Task 2 are fixed via atomic `fix(04-06)` commits; the affected scenarios re-run PASS; the codebase remains green.</done>
</task>

<task type="auto">
  <name>Task 4: Fill 04-VERIFICATION.md ## Manual Verification Checklist + ## Real-world Corrections + Phase verdict</name>
  <read_first>
    - `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` (the file Plan 04-01 Task 3 created with the `## Spike Results` section + stub for `## Manual Verification Checklist` + stub for `## Real-world Corrections`)
    - The user-pasted scenario transcripts + screenshots from Task 2
    - The fix-back commits from Task 3 (if any)
    - `.planning/phases/03-modal-ui-read-path/03-VERIFICATION.md` §"Checklist Results" + §"Real-world Corrections" — formatting + content shape to mirror
    - `.planning/ROADMAP.md` Phase 4 §"Success Criteria" (5 criteria — list them + per-criterion verdict)
  </read_first>
  <action>
    Update `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md`:

    **Step A — Fill `## Manual Verification Checklist (D-17 — Plan 04-06)`:**

    Replace the stub section with one ### subheading per scenario (1–8). For each scenario:
    - Title: e.g., `### 1. Happy path, no current SP — PASS`
    - Body: 2–4 paragraphs covering:
      - What was tested (verbatim from CONTEXT D-17 description)
      - Console transcript evidence (`[sp-calc/...]` log excerpts pasted verbatim — full transcript NOT needed; pick the diagnostic-relevant lines)
      - Network tab evidence (URLs, status codes, ordering)
      - Screenshot reference (if applicable)
      - Verdict: PASS / PARTIAL / FAIL with reason
    - Use the Phase 03-04 §"Checklist Results" structure as the formatting template (each scenario heading + body + verdict line).

    **Step B — Fill `## Real-world Corrections`:**

    For each fix-back from Task 3:
    - ### Heading: `### Fix-back N — patched <plan-id>: <one-line reason>`
    - Body:
      - Origin file: which file had the bug
      - Symptom: console/network signature
      - Fix: 1–3 line patch description
      - Phase implication: does this affect Phase 5? (likely no; document if yes)
      - Commit hash: from `git log` after the fix-back commit

    If zero fix-backs: write `*(No real-world corrections required — Phase 4 implementation passed all 8 D-17 scenarios on first cezari verification.)*`

    **Step C — Fill `## Phase 4 Verdict`:**

    Add a final section:
    ```markdown
    ## Phase 4 Verdict

    **Verdict:** <PASS | PARTIAL PASS | FAIL>

    **Per ROADMAP success criteria:**
    1. Confirm panel on existing SP → <verdict per scenario 2 evidence>
    2. setFieldValue + .save() + addComment via REST in atomicity order → <verdict per scenario 1 + Network-tab evidence>
    3. Read-only branch when user lacks write permission → <verdict per scenario 5 + scenario 6 evidence>
    4. Status-code-specific error toast on field-write fail; comment-fail toast → <verdict per scenarios 3 + 4 evidence>
    5. Form's SP value updates without page reload + reopen pre-fill → <verdict per scenario 1 evidence>

    **Notes:** <any caveats — e.g., "PARTIAL because cezari runs Scrum only; CMMI verification deferred to Phase 5 per CONTEXT D-19" or "PARTIAL on scenario 6 because synthetic probe-failure injection required a dev-only build mod">

    **Cross-process coverage:** Phase 4 verified on Scrum/PBI only. Phase 5 (PKG-04, PKG-07) extends to CMMI (Microsoft.VSTS.Scheduling.Size fallback path) on a separate trial org per Phase 0 D-14.
    ```

    Commit message: `docs(04-06): record Phase 4 verification — D-17 8-scenario checklist + verdict`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` contains the literal heading `## Manual Verification Checklist (D-17 — Plan 04-06)` (or close — the section is no longer the stub) (greppable: `grep -F "## Manual Verification Checklist" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` returns 1 match).
    - File contains 8 scenario subheadings (greppable: `grep -E "^### [1-8]\\." .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` returns 8 matches).
    - File contains a `**Verdict:**` line for each scenario (greppable: `grep -c "\\*\\*Verdict:\\*\\*" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` returns >= 12 — 4 in Spike Results + 8 in Checklist).
    - File contains `## Phase 4 Verdict` section (greppable: `grep -F "## Phase 4 Verdict" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` returns 1 match).
    - File contains `## Real-world Corrections` section (filled or with the no-corrections-needed note) (greppable: `grep -F "## Real-world Corrections" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` returns 1 match).
    - File contains at least 4 verbatim `[sp-calc/` log line excerpts from the cezari run (greppable: `grep -c "\\[sp-calc/" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` returns >= 4).
    - The Phase 4 Verdict section has explicit per-criterion verdicts mapping to the 5 ROADMAP criteria.
  </acceptance_criteria>
  <verify>
    <automated>grep -F "## Manual Verification Checklist" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md && grep -E "^### [1-8]\\." .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md | wc -l | grep -E "^\\s*8\\s*$" && grep -F "## Phase 4 Verdict" .planning/phases/04-write-path-edge-cases/04-VERIFICATION.md</automated>
  </verify>
  <done>04-VERIFICATION.md is fully populated with Spike Results (Plan 04-01) + Manual Verification Checklist (Plan 04-06) + Real-world Corrections (any fix-backs) + Phase verdict. The file is the canonical record of Phase 4's empirical evidence.</done>
</task>

<task type="auto">
  <name>Task 5: Flip REQUIREMENTS.md APPLY-04..09 checkboxes + update ROADMAP.md Phase 4 status + update STATE.md</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 51–57 (APPLY-04..09 — currently `[ ]`)
    - `.planning/REQUIREMENTS.md` Traceability table lines 135–140 (status column "Pending" → "Complete")
    - `.planning/ROADMAP.md` line 23 (`- [ ] **Phase 4: Write Path & Edge Cases**` — flip to `[x]` + add completion date)
    - `.planning/ROADMAP.md` Plans block lines 113 (`**Plans**: TBD` — replace with the actual 6 plans + their wave structure)
    - `.planning/ROADMAP.md` Progress table lines 138 (`Phase 4 ... 0/TBD ... Not started ... -` → `6/6 ... Complete ... <date>`)
    - `.planning/STATE.md` (current state — update `current_position`, `completed_phases`, `progress`, `last_activity`)
    - `.planning/phases/04-write-path-edge-cases/04-VERIFICATION.md` Phase 4 Verdict section (the basis for flipping to Complete)
  </read_first>
  <action>
    **Step A — REQUIREMENTS.md APPLY-04..09:** Use `Edit` tool with exact-match `old_string` / `new_string`. Flip each `- [ ] **APPLY-XX**` to `- [x] **APPLY-XX**`. The wording itself is unchanged for APPLY-04..08; APPLY-09's wording was already rewritten by Plan 04-02 — flip its checkbox too.

    **Step B — REQUIREMENTS.md Traceability table:** Update the rows for APPLY-04..09. Replace `Pending` with `Complete` for each. Use `Edit` tool per row.

    **Step C — ROADMAP.md Phase 4 line:** Edit line 23:
    - old_string: `- [ ] **Phase 4: Write Path & Edge Cases** - Apply writes the field and posts the audit comment using the chosen atomicity order, with permission checks, overwrite confirmation, and friendly error handling`
    - new_string: `- [x] **Phase 4: Write Path & Edge Cases** - Apply writes the field and posts the audit comment using the chosen atomicity order, with permission checks, overwrite confirmation, and friendly error handling (completed <yyyy-mm-dd>)`

    **Step D — ROADMAP.md Phase 4 Plans block:** Replace `**Plans**: TBD` (line 113) with `**Plans**: 6 plans` and replace the (currently empty) Plans subsection at the end of Phase 4 details with:

    ```
    Plans:
    **Wave 1**
    - [x] 04-01-PLAN.md — Empirical spike on cezari (D-01/D-05/D-10 assumptions resolved into 04-VERIFICATION.md ## Spike Results)
    - [x] 04-02-PLAN.md — errorMessages.ts pure module + REQUIREMENTS.md APPLY-09 rewrite

    **Wave 2** *(blocked on Wave 1 completion)*
    - [x] 04-03-PLAN.md — adoFetch helper + postComment writer + comments refactor + bridge getIsReadOnly + types extension
    - [x] 04-04-PLAN.md — 7 new UI leaf components (ConfirmOverwritePanel, ReadOnlyMessage, PermissionWarnBanner, CommentFailBanner, FieldFailBanner, SavingOverlay, SavedIndicator)

    **Wave 3** *(blocked on Wave 2 completion)*
    - [x] 04-05-PLAN.md — apply.ts two-leg orchestrator + CalcModal 9-mode state machine + toolbar lightDismiss=false

    **Wave 4** *(blocked on Wave 3 completion)*
    - [x] 04-06-PLAN.md — Manual cezari verification (D-17 8-scenario checklist) + 04-VERIFICATION.md write-up + REQUIREMENTS/ROADMAP/STATE updates
    ```

    **Step E — ROADMAP.md Progress table:** Update the Phase 4 row (line 138):
    - old: `| 4. Write Path & Edge Cases | 0/TBD | Not started | - |`
    - new: `| 4. Write Path & Edge Cases | 6/6 | Complete    | <yyyy-mm-dd> |`

    **Step F — STATE.md:** Update the frontmatter + body:

    - `progress.completed_phases`: increment from 4 to 5
    - `progress.total_plans`: from 8 to 14 (8 prior + 6 Phase 4)
    - `progress.completed_plans`: from 8 to 14
    - `progress.percent`: 5/6 = 83
    - `last_activity`: today's date with note "Phase 04 closed by Plan 04-06 manual cezari verification — <verdict from 04-VERIFICATION.md>"
    - `last_updated`: today's ISO-8601 timestamp
    - `stopped_at`: "Phase 04 complete; Phase 05 (Polish & Marketplace Publish) ready to plan"
    - `## Current Position`: bump to "Phase: 05 (polish-marketplace-publish) — NOT STARTED"; reset Plan to 0 of TBD; Status: "Phase 04 complete; Phase 05 ready for /gsd-discuss-phase 5".
    - `## Performance Metrics > By Phase`: add a Phase 4 row.
    - `## Performance Metrics > Recent Trend`: append "; Phase 4 closed via 6 plans (1 spike + 1 pure module + 1 ADO surface + 1 UI components + 1 orchestrator + 1 manual verify); <num> real-world corrections back-ported".
    - `## Accumulated Context > Decisions`: append the Phase 4 D-IDs that became locked decisions (D-01..D-20). Phase 03-04 has a precedent shape (lines 70–78).

    Use `Edit` tool for each section.

    **Step G — Final commit:** Combine the REQUIREMENTS / ROADMAP / STATE updates into a single commit:
    ```
    docs(04-06): close Phase 4 — APPLY-04..09 complete; ROADMAP/STATE updated; <verdict> verdict
    ```

    The verification record (`04-VERIFICATION.md`) is the prior commit; the cross-cutting docs update is the closing commit.
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` has 6 instances of `[x] **APPLY-` for APPLY-04 through APPLY-09 (greppable: `grep -E "^- \[x\] \*\*APPLY-0[4-9]\*\*" .planning/REQUIREMENTS.md` returns 6 matches).
    - `.planning/REQUIREMENTS.md` Traceability table has APPLY-04..09 marked `Complete` (greppable: `grep -E "APPLY-0[4-9] \| Phase 4 \| Complete" .planning/REQUIREMENTS.md` returns 6 matches).
    - `.planning/ROADMAP.md` Phase 4 line is `- [x]` (greppable: `grep -E "- \[x\] \*\*Phase 4:" .planning/ROADMAP.md` returns 1 match).
    - `.planning/ROADMAP.md` Phase 4 Plans section lists all 6 plans with `[x]` prefix (greppable: `grep -E "^\\- \\[x\\] 04-0[1-6]-PLAN\\.md" .planning/ROADMAP.md` returns 6 matches).
    - `.planning/ROADMAP.md` Progress table Phase 4 row shows `6/6 | Complete` (greppable: `grep -E "Phase 4.*6/6.*Complete" .planning/ROADMAP.md | wc -l` returns >= 1).
    - `.planning/STATE.md` frontmatter has `completed_phases: 5` (greppable).
    - `.planning/STATE.md` Current Position references Phase 05 (greppable: `grep -F "Phase: 05" .planning/STATE.md` returns 1 match).
    - `.planning/STATE.md` last_activity references the Phase 4 close (greppable for current date + Phase 04).
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^- \\[x\\] \\*\\*APPLY-0[4-9]\\*\\*" .planning/REQUIREMENTS.md | wc -l | grep -E "^\\s*6\\s*$" && grep -E "- \\[x\\] \\*\\*Phase 4:" .planning/ROADMAP.md && grep -F "Phase: 05" .planning/STATE.md</automated>
  </verify>
  <done>REQUIREMENTS.md APPLY-04..09 are checked; ROADMAP.md Phase 4 is marked Complete with the 6-plan listing; STATE.md reflects the milestone progression. Phase 4 is closed; Phase 5 is the next stop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User cezari run → 04-VERIFICATION.md | The user pastes verbatim console transcripts, Network-tab evidence, and screenshots into chat; Claude transcribes them into 04-VERIFICATION.md. The transcripts may include `SDK.getUser()` output (descriptors / display names) and work item IDs. Treat as low-sensitivity (private repo; same user is the producer + consumer). |
| Fix-back commits → originating plans | Each `fix(04-06): patch 04-XX regression` commit modifies files belonging to the targeted plan only. The git audit trail preserves which plan owns which fix. |
| REQUIREMENTS.md / ROADMAP.md / STATE.md updates | These are project-state docs read by the verifier (Phase 4 close gate) and the next-phase planner (Phase 5 discuss). Wrong values can mis-route Phase 5; mitigated by acceptance criteria verifying every flipped checkbox. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | I (Information disclosure) | Pasted SDK.getUser() output in 04-VERIFICATION.md may include user descriptor / displayName | accept | Phase 03-04 verification record contains the same shape; project is private repo; no external publish planned for Phase 4 docs. Phase 5 listing-asset prep would scrub this if needed. |
| T-04-06-02 | T (Tampering) | Fix-back commit could expand scope beyond the originating plan | mitigate | Acceptance criterion: each fix-back commit modifies files belonging to the originating plan only. The commit message format `fix(04-06): patch 04-XX regression — <reason>` is searchable for review. |
| T-04-06-03 | T (Tampering) | Phase 4 close commit could flip checkboxes for requirements that didn't actually pass | mitigate | The Phase 4 Verdict section in 04-VERIFICATION.md must list per-criterion verdicts; the 04-06 SUMMARY restates them; the cross-cutting docs commit message includes the verdict. Any future audit traces the chain: REQUIREMENTS [x] → ROADMAP [x] → STATE.md → 04-VERIFICATION.md verdict → cezari evidence. |
| T-04-06-04 | I (Information disclosure) | Network-tab POST/GET URLs in 04-VERIFICATION.md include the cezari org name + project ID + work item IDs | accept | Same shape as Phase 03-04 verification record. Cezari is the user's dev org; project IDs are not PII. |
| T-04-06-05 | E (Elevation of privilege) | Scenario 5 (Stakeholder/read-only) requires changing user license; reverting it after the test is critical | mitigate | Task 2 scenario 5's "how-to-verify" step explicitly says "revert license back to the dev user's full license for subsequent scenarios." |
</threat_model>

<verification>
- All 8 D-17 scenarios are exercised on cezari with verdicts captured.
- Real-world fix-backs (if any) are committed atomically with `fix(04-06)` labels referencing the originating plan.
- 04-VERIFICATION.md contains: Spike Results (from Plan 04-01) + Manual Verification Checklist (8 scenarios) + Real-world Corrections + Phase 4 Verdict (with per-ROADMAP-criterion verdict).
- REQUIREMENTS.md APPLY-04..09 checkboxes are flipped to `[x]`; Traceability table shows Complete.
- ROADMAP.md Phase 4 is `[x]`, Plans listed, Progress table updated.
- STATE.md frontmatter + body reflects the Phase 4 close + Phase 5 readiness.
- Final commit message references the Phase 4 verdict.
</verification>

<success_criteria>
- Phase 4 is empirically verified end-to-end on cezari (Scrum org).
- All 6 phase requirements (APPLY-04 through APPLY-09) flip from Pending to Complete.
- 04-VERIFICATION.md is the canonical record; the Phase 4 Verdict section maps to ROADMAP success criteria 1–5.
- Cross-cutting project docs (REQUIREMENTS / ROADMAP / STATE) reflect the milestone progression.
- Any real-world bugs are fixed via atomic back-port commits; the codebase remains green at the close commit.
- Phase 5 (Polish & Marketplace Publish) is unblocked — `/gsd-discuss-phase 5` can run.
</success_criteria>

<output>
After completion, create `.planning/phases/04-write-path-edge-cases/04-06-SUMMARY.md` per the standard template. Capture: cezari run summary table (8 scenarios × verdict × evidence-file references), fix-back inventory (if any), the Phase 4 Verdict, references to the cross-cutting doc updates (REQUIREMENTS / ROADMAP / STATE), and Phase 5 readiness checklist (e.g., "CMMI verification deferred to Phase 5 per CONTEXT D-19; cross-process coverage extends in Phase 5 PKG-04 / PKG-07").
</output>
